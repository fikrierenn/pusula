<Project Sdk="Microsoft.NET.Sdk">

  <!-- Şube kapsamı UÇTAN UCA testi — plan 48 V-06.
       `tools/vardiya_kapsam_denetimi.py` METİN denetimidir: süzgecin doğru kolona
       bağlandığını göremez. Bu proje o boşluğu kapatır — gerçek uygulama, gerçek
       oturum, gerçek veritabanı.

       ⚠ ATLAMA BAYRAĞI YOK (Solum'un duman testi deseni): panel DB yoksa test
       KIRMIZI döner ve mesajı ne yapılacağını söyler. Sessizce atlanan bir test,
       olmayan testtir. -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <IsPackable>false</IsPackable>
    <RootNamespace>BkmVardiya.Tests</RootNamespace>
    <InvariantGlobalization>false</InvariantGlobalization>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Testing" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\vardiya-app\BkmVardiya.csproj" />
  </ItemGroup>

</Project>
