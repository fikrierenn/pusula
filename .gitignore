node_modules/
dist/
.env
*.js.map

# Secrets (SMTP credentials, API keys)
.secrets/
*.secret.json

# Local cache & generated artefacts
.cache/
Pazartesi-Brifingi-*.html
Pazartesi-Brifingi-*.txt

# Paralel oturum lock dosyalari (ADR-002)
.claude/locks/*.lock
.claude/locks/.current

# Security: plaintext credentials icerebilir (ADR-002 paralel oturum uyarisi)
claude_desktop_config*.json
fix-mcp-config.*

# Geçici handoff scriptleri ve içerik dosyaları
_*.ps1
_*.sh
_plan-first-content.md

# --- Codex CLI tooling (makine-local, CLAUDE.md/skills mirror — çift-kaynak tutma) ---
.agents/
.codex/
AGENTS.md

# --- Claude Code (template bootstrap) ---
.claude/worktrees/
.claude/settings.local.json
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
__pycache__/
*.pyc

# Mobil/PWA sertifikalar (mkcert — makineye özel, private key git'e girmez)
dashboard/cert/
tools/cert/

# Rapor çıktı artifactları (regenerate edilebilir, git'e girmez)
briefings/olu-stok*.xlsx

# RaporApp build çıktıları (dotnet publish artifacts)
sorgular/03-kampanya/RaporApp/bin/
sorgular/03-kampanya/RaporApp/obj/
sorgular/03-kampanya/dist/


# Tahmin motoru runtime ciktisi (plan-15, regenerate edilebilir) + kisisel kayit/cache
dashboard/data/forecast/
dashboard/data/takvim-cache.json
dashboard/data/tahmin-kayitlari.json
dashboard/data/ic-kartlar.json
run_forecast.log
send_brief.log

# Rapor çıktıları (regenerable — script ile üretilir)
raporlar/
~$*
kontrol.xlsx

# Kök stray dosyalar (repo içeriği değil — çalışma dosyaları)
/Haziran.26 Mizan.xlsx
/WhatsApp Image*.jpeg
/Dosyalar* tara tasar*.zip
/bkm temmuz*.xlsx
/ik raporlar*/

# Cert private key (CA imzalama anahtari — ASLA commit etme) + yedekler
dashboard/cert/ca.key
dashboard/cert/*.bak

# Kişisel veri içeren rapor çıktıları (KVKK — isimli maaş/bordro/personel dökümü).
# Script'ler commit'lenir, ÜRETTİĞİ dosya commit'lenmez.
briefings/**/*aa*[Ll]istesi*.xlsx
briefings/**/beyaz-yaka-*.json
briefings/**/beyaz-yaka-*.xlsx
briefings/personel-kart-anomali.xlsx
send_brief.log

# Sezon personel kisi listesi (KVKK - ad soyad + personel no)
briefings/**/sezon-personel-listesi*.xlsx

# Sezon kadro TEK DOSYA (kisi listesi sayfalari dahil - KVKK)
briefings/**/sezon-kadro-2026*.xlsx

# Kisi-duzeyi personel listesi iceren Excel (KVKK)
briefings/**/*KISILI*.xlsx
briefings/**/*KISILI*.json

# sqlcli.json COMMIT EDİLİR: içinde şifre yok, yalnız ${ENV} yer tutucusu
# (sqlcli v2.2 bunları ortamdan/.env'den doldurur). Profillerin paylaşılması tekrar
# üretilebilirlik için gerekli.
