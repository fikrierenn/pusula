# Curator Dry-Run — 2026-09-09

> Mod-1 (dry-run). **Hiçbir dosya değiştirilmedi** — bu rapor dışında yazma yok.
> Önceki tam dry-run: `REPORT-2026-09-01.md` (8 gün). Tetik: `session-handoff` Adım 4.6
> (vadesi 1 gün geçmişti, önceki handoff'ta atlanmıştı).

## Özet

| Tarama | Bulgu | Ağırlık |
|---|---|---|
| Sema stale (tarih aşımı) | **5** kayıt — hepsi `queries.yaml` | ⚠ ama **ölçüldü: hepsi koşuyor** |
| Sema `last_verified` **YOK** | **16** kayıt (bridges 9 · codes 4 · entities 2 · metrics 1) | 🔴 decay ölçülemiyor |
| Çakışan köprü (aynı from→to) | **1** çift | 🟡 |
| `teyit bekliyor` / `SÜPERSEDED` duran | **9** yer | 🟡 |
| Eski/tarihsiz açık TODO (≥30g) | **93** / 95 açık madde | 🔴 |
| Değişmez koşumu | 34/34 **yeşil**, 0 kırık | ✅ |
| Katalog dumanı | 15/15 **yeşil**, 0 kırık | ✅ |

**En önemli bulgu stale değil, ölçülemezlik:** 16 sema kaydında `last_verified` hiç yok →
bayatlayıp bayatlamadığı **hesaplanamıyor**. `ttl_days` bayrağı bunlar için hiç çalışmıyor.
Aralarında sık kullanılan `products-code-stkid` (EncoreMerkez↔DerinSIS ana köprüsü) ve
`urnbrkd-barkod` (barkod zinciri) var.

---

## 1. Stale sema (5) — tarih aşmış

`last_verified + ttl_days < bugün`. `confidence: 1.0` muaf (kalıcı PK/FK).

| id | dosya | last_verified | aşım | ttl |
|---|---|---|---|---|
| `aylik-magaza-net-ciro` | queries.yaml | 2026-04-15 | **57 g** | 90 |
| `eticaret-marka-satis` | queries.yaml | 2026-06-09 | 2 g | 90 |
| `cod-teslim-performans` | queries.yaml | 2026-06-09 | 2 g | 90 |
| `depo-hareket-kategori` | queries.yaml | 2026-06-10 | 1 g | 90 |
| `depo-toplama-verim` | queries.yaml | 2026-06-10 | 1 g | 90 |

**ÖLÇÜLDÜ — beşi de ayakta.** `python tools/sema_sorgu_dumani.py`: 15 sorgu denendi,
**0 kırık, 0 sarılamaz** (`aylik-magaza-net-ciro` 40 satır · `eticaret-marka-satis` 1.632 ·
`cod-teslim-performans` 4 · `depo-hareket-kategori` 21 · `depo-toplama-verim` 3).

→ Bu beşi için stale bayrağı **karşılığı olmayan bir alarm**: duman testi zaten koşuyor ve
geçiyor. Öneri: `last_verified` bugüne çekilsin (duman testi = yeniden doğrulama).

## 2. `last_verified` YOK (16) — decay ölçülemeyen kayıtlar

Bu kayıtlar için yaşlanma **hesaplanamıyor**; TTL mekanizması sessizce devre dışı.

### bridges.yaml (9)
| id | confidence | not |
|---|---|---|
| `products-code-stkid` | 0.99 | **EncoreMerkez↔DerinSIS ana köprüsü** — panelin çoğu buna dayanıyor |
| `urnbrkd-barkod` | 0.90 | barkod → stkID zinciri (ODAK köprüsünün parçası) |
| `palet-irs` | 0.95 | WMS palet → irsaliye |
| `emirayr-emir` | 0.90 | depo emir satırı |
| `bekleyen-siparis-il` | 0.90 | e-tic il kırılımı |
| `hedef-kategori` | 0.95 | BKMDATA hedef |
| `salescampaign-sales` | 0.95 | POS kampanya |
| `sinav-fis-encore` | 0.95 | Sınav ↔ POS fiş |
| `siparisdetay-sales` | 0.85 | en düşük güven, tarih yok |

### codes.yaml (4)
`depo.paletIcHrk.pHrkTip` · `depo.paletIcHrkTip` · `depo.paletTip` · `mekanID`
(kod kümeleri — `sqlcli lookup --count-from` ile bir koşuda tarihlenebilir)

### entities.yaml (2)
`EncoreMerkez` (compat bloğu) · `pdks_saatlik_personel`

### metrics.yaml (1)
`net_ciro` — **panelin en merkezi metriği**, tarih damgası yok.

> ⚠ `net_ciro` bu hafta B-162/B-164 ile fiilen DEĞİŞTİ (kanonik formül
> `eTip 100 − 101 + 4 − 5`). Kayıt güncellendi ama `last_verified` konmadı → sonraki oturum
> güncel mi bayat mı bilemez.

## 3. Çakışan köprü (1)

| from → to | kayıtlar | durum |
|---|---|---|
| `depo.paletUrnTnm.pUStkID` → `dbo.urn.stkID` | `paleturn-stk`, `wms-depo-stok` | aynı bağ iki kayıtta |

Tek-doğruluk-kaynağı ihlali. İkisi farklı bağlam anlatıyorsa (biri palet tanımı, biri depo
stoğu) `note` ayrışmalı; aynı şeyi anlatıyorsa biri umbrella'ya taşınıp diğeri
`süperseded by` işaretlenmeli. **Karar ölçüm ister** — hangisinin nerede kullanıldığı taranmadı.

## 4. `teyit bekliyor` / `SÜPERSEDED` duran (9)

| yer | içerik | not |
|---|---|---|
| `entities.yaml:534` | `frmIadeKural` 0/1/2 kod ANLAMI | **B-150 + B-167(c)** — muhasebeye tek soru, aylardır açık |
| `entities.yaml:423` | görev-tanımı eşleme köprüsü (planlı) | Mosaik modülü gelmedi |
| `metrics.yaml:111` | "GENEL Giriş" `eTip 16` — e-tic iade mi merkez intake mi | depo akış sözlüğünde boşluk |
| `metrics.yaml:266` | `SÜPERSEDED 16.06` → `tum_ciro_kdv_kargo_haric` | eski karar hâlâ dosyada duruyor |
| `metrics.yaml:183` | `superseded` asılı sipariş filtresi | bu bir SQL yorumu, kayıt değil — **temiz** |
| `metrics.yaml:370` | `frm.frmBagID`/`frmGrup1..5` hepsi 0 (grup alanı beslenmiyor) | ölçülmüş boşluk |
| `metrics.yaml:432` | Bölüm ≠ Mağaza açık toplamı (15 vs 14) | ölçülmüş çelişki, açıklanmadı |

## 5. Eski / tarihsiz açık TODO (93 / 95)

Açık madde **95**, bunun **93'ü** ≥30 gün veya tarihsiz. Yani TODO'nun neredeyse tamamı
"eski" kovasında → eşik ayırt etmiyor, sinyal vermiyor.

En yaşlı tarihli 4:

| satır | yaş | madde |
|---|---|---|
| 238 | 77 g | B-122 Kontrol paneli SP perf |
| 247 | 70 g | B-128 Muhasebe Kontrol — CAR evrak drill |
| 192 | 67 g | 🔥 B-130 Otonom muhasebe app (plan-27) |
| 227 | 67 g | B-131 Dashboard hedef-widget fix |

**Tarihsiz 14 madde** (B-06, B-12/B-13, B-114, B-115, B-116, B-117, B-118, B-40, H-10, H-11,
M-10, M-13, "TEMİZLİK:") — yaş ölçülemiyor, `todo-verification` kuralının file:line şartını da
taşımıyorlar.

## 6. Koşan denetimler — ikisi de yeşil

- `python tools/sema_degismez.py` → **34 değişmez, 2 hedef (erp+zirve), 0 kırık.**
  Öne çıkanlar: `wms-hucresel-stok-kaynagi-ayakta` 3.807.156 (≥1.000.000) ·
  `stok-snapshot-tazeligi` 26 gün (≤45) · `etip4-magaza-satis-istyolu-anlamli` 48 (≥20).
- `python tools/sema_sorgu_dumani.py` → **15 sorgu, 0 kırık, 0 sarılamaz.**

Yani **koşan** katman sağlıklı; sorun **koşmayan** (tarihsiz) kayıtlarda.

---

## Önerilen aksiyonlar (ONAY BEKLİYOR — hiçbiri uygulanmadı)

- [ ] **A1 — 5 stale sorgunun `last_verified`'ı bugüne çekilsin.** Gerekçe: duman testi
      15/15 geçti, beşi de ayakta. Tek dosya (`queries.yaml`), düşük risk.
- [ ] **A2 — 16 tarihsiz kayda `last_verified` + `ttl_days` eklensin.** Sırayla değil,
      **ölçerek**: her biri için tek doğrulama sorgusu koşulup o günün tarihi yazılır.
      En kritik üçü önce: `net_ciro` (metrics) · `products-code-stkid` · `urnbrkd-barkod`.
- [ ] **A3 — `net_ciro` kaydı B-162/B-164 formülüyle hizalı mı denetlensin** (kanonik
      `eTip 100 − 101 + 4 − 5` yazılı mı), sonra tarihlensin.
- [ ] **A4 — `paleturn-stk` ↔ `wms-depo-stok` çakışması çözülsün.** Önce kullanım taraması
      (hangisi kodda/sorguda geçiyor), sonra umbrella veya `note` ayrıştırma.
- [ ] **A5 — `metrics.yaml:266` SÜPERSEDED bloğu archive bölümüne taşınsın** (silme yok).
- [ ] **A6 — 14 tarihsiz TODO maddesine tarih veya arşiv kararı.** Yaşı ölçülemeyen madde
      curator'ın göremediği maddedir.
- [ ] **A7 — `frmIadeKural` sorusu kapatılsın** (B-150/B-167c). Muhasebeye tek soru:
      hangi kod "iade hakkı VAR"? Cevap gelmeden iade-hakkı olan ürün "ölü stok" sayılmaya
      devam ediyor.

> Curator hard-rule: **stale = bayrak, otomatik aksiyon değil.** Archive/birleştirme/`[x]`
> yalnız kullanıcı onayıyla; silme hiçbir durumda yok.
