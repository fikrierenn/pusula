# Curator Dry-Run — 2026-09-20

> **MUTASYONSUZ.** Bu rapor hiçbir dosyayı değiştirmedi. Aksiyonlar onay bekliyor.
> Önceki tam tarama: 2026-09-09 (11 gün önce). Tetik: V-22.

## Özet

| Eksen | Sonuç |
|---|--:|
| sema gerçek kaydı (confidence \| evidence taşıyan) | 484 |
| `confidence: 1.0` MUAF (kalıcı, yaşlanmaz) | 103 |
| **STALE (`last_verified + ttl_days < bugün`)** | **0** |
| ⚠ **`last_verified` YOK → decay'e GÖRÜNMEZ** | **49** |
| `status: superseded` ama hâlâ duran | 4 |
| TODO açık madde | 105 |
| TODO ≥30 gün dokunulmamış | 21 |
| ⚠ TODO tarih damgası yok → yaş ÖLÇÜLEMİYOR | 59 |
| Asistan belleği (`PanelAsistanBellek`) stale | 0 (aktif 2 kayıt) |

## 1. Stale sema — 0

Süresi dolmuş kayıt **yok**. Türetme `sema/README.md` § Decay ile birebir
doğrulandı (1.0 muaf · 0.9-0.99 → 180g · 0.5-0.8 → 90g · 0.3-0.5 → 30g), yani
bu "0" gevşek bir varsayılandan gelmiyor.

## 2. ⚠ ASIL BULGU — decay'e görünmez 49 kayıt

`last_verified` alanı olmayan bir kayıt **hiç bayatlamaz**: TTL'i başlatan tarih
yoktur, dolayısıyla curator onu ne bugün ne bir yıl sonra görür. Bu, decay
mekanizmasının sessiz kör noktasıdır — tam da `test-discipline.md`
§ *yazılı kural ≠ uygulanan kural* sınıfı: kural var, o kayıtlar için koşmuyor.

Dosya dağılımı: `metrics.yaml` 26 · `codes.yaml` 10 · `bridges.yaml` 8 ·
`entities.yaml` 4 · `_contract.yaml` 1.

### 2a. En riskli sekiz — confidence TAŞIYOR ama tarihi YOK

Bunlar bir iddia ileri sürüyor (0.85-0.99) ama ne zaman doğrulandıkları yazılı değil:

| köprü | conf | from → to |
|---|--:|---|
| `products-code-stkid` | 0.99 | `EncoreMerkez.dbo.Products.Code` → `urn.stkID` |
| `palet-irs` | 0.95 | `depo.paletIcHrk.piIrsID` → `dbo.irs.eID` |
| `hedef-kategori` | 0.95 | `BKMDATA.dbo.Hedef.ktgId` → `urnKtgr2.ktgrID` |
| `salescampaign-sales` | 0.95 | `SalesProductCampaigns.SalesId` → `Sales.Id` |
| `sinav-fis-encore` | 0.95 | `snv.SinavSiparisFisEncore.InvoiceNo` → `Sales.DocumentNo` |
| `emirayr-emir` | 0.90 | `depo.emirAyr.emPaletID` → `depo.emirSat.satPID` |
| `urnbrkd-barkod` | 0.90 | `SalesProducts.BarcodeNo` → `urnBrkd.urnBarkod` |
| `siparisdetay-sales` | 0.85 | `BKM.snv.SiparisDetay.FisId` → `Sales.DocumentNo` |

⚠ `products-code-stkid` ve `urnbrkd-barkod` **stkKod=barkod vakasının tam
merkezindeki** köprüler. Bayatlarlarsa belirtisi sessiz ciro kaybı olur.

### 2b. Geri kalan 41

`confidence` da taşımıyor (çoğu `codes`/`metrics` tanımı). Bunlar için `evidence`
tarihi var ama TTL başlatan alan yok.

## 3. Superseded ama duran — 4

| kayıt | gerekçe |
|---|---|
| `bridges[36] bekleyen-siparis-il` | "Kayıt bir JOIN değil, bir KURAL anlatıyordu" |
| `bridges[46] maliyet-marj-cikis` | "`to` alanı join hedefi değil, bir FORMÜLdü" |
| `entities._sema_dogrulama_2026_08_18` | elle kolon kontrolüydü; `tools/sema_kolon_cek.py` devraldı |
| `metrics.asiri_stok_esigi_kategori_bazli` | politika tablosu kullanıcı kararıyla iptal |

Dördü de **bilinçli** superseded ve gerekçesi yazılı — `semantic-layer.md`'nin
"silme, superseded yap" kuralına uygun. **Aksiyon önerilmiyor**, sayım için burada.

## 4. TODO — 21 eski madde (≥30 gün)

| yaş | madde |
|--:|---|
| 263g | **K-29** `bkm.IrsTipGrupMap` boş duruyor |
| 153g | **B-20** Pazartesi maili SPAM'a düşüyor olabilir |
| 129g | **B-04** `register-scheduled-task.ps1` çalıştır |
| 102g | **M-06** `sql_table_dependencies` discovery tool |
| 101g | **B-21** Pazartesi brief Task Scheduler tetikleme |
| 96g | **H-11** WS-1 telemetri sidecar *(ERTELE etiketli — bilinçli)* |
| 95g | **C-12** README multi-project · **TEMİZLİK** B-NEW-00..06 arşive |
| 78g | **B-131** Dashboard hedef-widget fix |
| 37g | **B-136/137/138/140** Bulunurluk + satınalma (14.08 kümesi) |
| 33g | **B-139/141/147/148/149/151** (18.08 kümesi) |
| 32g | **B-158** mutabakat doğrulama (plan-35) |

⚠ **59 açık maddede tarih damgası YOK** → yaşları ölçülemiyor. Bu, TODO tarafının
decay kör noktası; sema'daki `last_verified` eksikliğinin kardeşi.

## 5. Asistan belleği — temiz

`PanelAsistanBellek`: 3 kayıt (2 aktif, 0 pinned), **stale 0**.

---

## Önerilen aksiyonlar (ONAY BEKLİYOR — hiçbiri uygulanmadı)

- [ ] **A1 — Sekiz köprüye `last_verified` ver.** Canlı doğrula, tarihi bugüne
      yaz. En az `products-code-stkid` + `urnbrkd-barkod` (stkKod=barkod merkezli).
      Yol: `sema-ogren`. Bu, 8 kaydı decay'e **görünür** kılar.
- [ ] **A2 — `_contract.yaml`'a kural: confidence taşıyan kayıt `last_verified`
      ZORUNLU.** Sözleşme + koşucu denetimi olmadan bu kör nokta yeniden büyür
      (`yazılı kural ≠ uygulanan kural`). Koşulabilir hâli: `sema denetle`ye
      "confidence var, last_verified yok" kontrolü.
- [ ] **A3 — Kalan 41 kayda ya `last_verified` ya `decay_exempt: <gerekçe>`.**
      İkincisi meşru olabilir (saf kod sözlüğü yaşlanmaz) ama YAZILMALI.
- [ ] **A4 — TODO: 59 tarihsiz maddeye tarih damgası** veya kapanış. Yaş
      ölçülemeyen madde curator'a görünmez.
- [ ] **A5 — K-29 (263 gün)**: hâlâ geçerli mi? Değilse `## Arşiv`'e.
- [ ] **A6 — "TEMİZLİK B-NEW-00..06" (95 gün)**: maddenin kendisi "arşive taşı"
      diyor ve 95 gündür duruyor. Ya yap ya sil.

> **Hiçbir aksiyon otomatik uygulanmaz** (`consolidate-sema` hard-rule).
> Silme yok — en yıkıcı işlem arşive taşımadır.

---

# UYGULANDI — 2026-09-20 (GMY onayı: "hepsini yap")

## A1 ✅ Sekiz köprü CANLI ölçüldü (tarih damgalanmadı, ÖLÇÜLDÜ)

| köprü | ölçüm | sonuç |
|---|---|---|
| `products-code-stkid` | 871.570 Products → 838.505 eşleşti | ⚠ **11.09'un sonucu DÜZELTİLDİ** |
| `urnbrkd-barkod` | son 200.000 kalem → 191.131 | %95,6 |
| `salescampaign-sales` | 3.173.629 satır → öksüz 0 | %100 · conf 0.95→**0.99** |
| `palet-irs` | belgeli 505.563 → öksüz 1.347 | %99,73 (1.292.669 sentinel) |
| `hedef-kategori` | 128.220 → öksüz 0 | %100 · conf 0.95→**0.99** |
| `emirayr-emir` | palet düzeyinde 12.311/14.393 | %85,5 · conf 0.90→**0.85** |
| `sinav-fis-encore` | 16.264 → öksüz 19 | %99,88 |
| `siparisdetay-sales` | dolu 199.755 → öksüz 0 | %100 · conf 0.85→**0.99** |

### İki ölçüm, iki ÖNCEKİ SONUCU düzeltti

**1. `products-code-stkid` — "iddia bayatlamış" sonucu EKSİK ÖLÇÜMDÜ.**
11.09 kaydı *"%99,98'den %96,20'ye düştü, İDDİA BAYATLAMIŞ"* diyordu. Bugün aynı
%96,2 çıktı — ama eşleşmeyen 33.065'in **31.727'si SİLİNMİŞ/PASİF** ürün
(`IsDeleted=1 & IsActive=0`). **Canlı ürünlerde eşleşme %99,84** (öksüz 1.338).
Köprü bayatlamamış; ölçümün kapsamı silinmişleri eliyordu.

**2. `emirayr-emir` — satır düzeyinde ölçüm köprüyü BOZUK gösteriyordu.**
Satır düzeyinde öksüz %43,9. Yanıltıcı: `emirSat` 12.351 satırlık bir boyut
tablosu, join çok-bire. **Palet düzeyinde %85,5** — bu da iddia edilen 0.90'ın
altında, o yüzden confidence **düşürüldü**. Öksüzler 2021-2026'ya yayılı, eski
veri artığı değil.

## A3 ✅ Kalan 51 kayıt damgalandı — tarih UYDURULMADI

- **40 kayıt**: kendi `evidence`/`measurement`/`note` metnindeki en yeni tarihten
  türetildi. O gerçekten son teyit tarihidir.
- **11 kayıt**: metinde tarih yoktu → **bugün canlı doğrulandı** ve damgalandı.
  Bu doğrulama iki gerçek üretti:
  - ⚠ `Mosaik.dbo.SopDocuments` **ERP sunucusunda YOK, PANEL sunucusunda** (2 satır).
    ERP'den `OBJECT_ID` NULL döner ve "tablo yok" sanılır — `OBJECT_DEFINITION NULL
    ≠ şifreli` dersinin aynısı: NULL'un üç sebebi var, ayırt edilmeli.
  - ⚠ **Üç tablo MEVCUT ama 0 SATIR**: `J_ORDER_DETAILS_COMP` ·
    `FifoFallbackFiyatlari` · `OrtalamaAylikMaliyet`. Boş tablo "yok" değildir;
    ona dayanan sorgu sessizce boş döner ve "veri yok" diye okunur.

**Sonuç: `damga_yok` 59 → 0.**

## A2 ✅ Kural `uyari` → `kirik` yükseltildi + KIRMIZI KİP koştu

`_contract.yaml`'daki `last_verified` kuralı zaten vardı ama seviyesi `uyari`ydı —
yani sözleşme yazıyor, kapı kırmıyordu. Sıra önemliydi: **önce 59 kayıt damgalandı,
sonra seviye yükseltildi**; tersi olsaydı kapı ilk gün kapanırdı.

**Kırmızı kip:** `products-code-stkid`'den `last_verified` kaldırıldı →
`KIRIK damga_yok 1`, kaydın adı yazıldı, **çıkış kodu 1**. Geri alındı → **0**.

## A4 ✅ 71 tarihsiz TODO maddesi — `git blame` ile

Tarih uydurulmadı: her maddenin TODO.md'ye **gerçekten eklendiği** commit tarihi
`git blame --line-porcelain` ile alınıp `⟨eklendi DD.MM.YYYY⟩` olarak yazıldı.
Artık 71 maddenin yaşı ölçülebilir; daha önce curator'a görünmüyorlardı.

## A5 ✅ K-29 yeniden ölçüldü — hâlâ geçerli

`bkm.IrsTipGrupMap`: tablo **VAR**, satır **0** (263 gündür). Bulgu doğru ama
tabloyu kullanan tek yer bir arşiv sorgusu — boş olması bugün hiçbir şeyi kırmıyor.
Arşivlenmedi; "doldur ya da yapılmayacak diye kapat" kararı GMY'de.

## A6 ✅ "TEMİZLİK B-NEW-00..06" kapatıldı

Ölçüldü: iş **zaten yapılmış** — TODO'da `### Mayıs %50 kitap kampanyası tahmini
(B-NEW-00..06) — arşiv 17.06` başlığı duruyor. Madde 95 gündür done-but-`[ ]`
bekliyormuş; `commit-discipline` S1'in önlemeye çalıştığı birikimin ta kendisi.

## Durum

`sema_denetim.py` **KIRIK 0** (`damga_yok` maddesi tamamen kalktı) ·
`todo_denetimi.py` geçti (241 madde · 88 açık · 153 kapalı).

⚠ **Bu raporun kendi sınırı:** `last_verified` artık ZORUNLU ama *doğru* olduğunu
hiçbir şey denetlemiyor. Biri bugünün tarihini ölçmeden yazarsa kapı yeşil kalır.
Tarih bir NİYET beyanıdır; ölçümün kendisi `measurement` alanındadır.
