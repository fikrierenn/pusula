# Oturum Günlüğü — 2026-06-14

> (Oturum 13.06 akşam başladı, 14.06 gecesine sarktı — B-41 + grafik UX + PWA/mobil tek akış.)

## Ana Konu
B-41 (JOKER kargo SQL) çekirdeği dashboard'a entegre edildi (il teslimat · aylık çıkış trendi + gün drill · COD iade maliyeti), çıkış süresi **iş günü** hesabı + **veriden otomatik tatil** keşfi. Ardından tüm grafiklere oran/değer etiketleri + mobil-dostu tooltip. Son blok: **PWA + mobil app görünümü (alt tab-bar) + LAN HTTPS (mkcert)**. 18 commit.

## Tamamlananlar

### B-41 — Kargo panelleri (çekirdek 3/7 + derinleştirme)
- **İl teslimat performansı** (32f9bbf): `EticQueries.GetIlTeslimatAsync` — şehir × çıkış (takvim+iş günü) × teslim. Köprü `J_ORDER_DELIVERY_ADDRESS.LOGICALREF=J_ORDERS.DELIVERYREF` (DCITY). **Bulgu:** çıkış il-bağımsız ~4,1g takvim (depo darboğazı), teslim il-bağımlı (İstanbul 1,4g · Doğu 2,7-2,8g).
- **Aylık çıkış trendi + gün drill** (2b24877, 77bbb16): son 13 ay × çıkış. Aya tıkla → o ayın günleri (gün adı Pazartesi/Salı, 7c84da4). Yoğun ay = kapasite darboğazı (Aralık 158K sip → takvim 4,4/iş günü 3,1).
- **İş günü çıkış** (c6936bf): takvim − hafta sonu − tatil. Takvim 4,14g → iş günü 2,79g (depo gerçek). Depo Pzt-Cuma (Cmt 5/Paz 0 doğrulandı).
- **VERİDEN OTOMATİK TATİL** (8cb5867): holidays.rest ücretli ($29/ay), Nager.Date dini bayram vermiyor → depo'nun çalışmadığı hafta-içi günleri veriden bul (resmi+DİNİ+grev otomatik). 0-kargo dini bayram dahil (Kurban 2025 Haz 5-9, 2026 May 25-30 — hicri kayma). Sıfır bakım, statik cache 6h. `EticQueries.TatilValuesAsync`.
- **İki performans** (6e45f9c): Çıkış Takvim (müşteri algısı gri) + Çıkış İş Günü (depo gerçek renkli) yan yana. İstanbul takvim 4,1 / iş günü 1,8 / toplam algı 5,5.
- **COD iade maliyeti** (e85e2b7): `GetCodOzetAsync` PAYDEFREF=-3. İade=2×CARGOPRICE (BKM yutar). Mayıs %10,8 iade / 52K₺ iade maliyeti (~628K₺/yıl). SERVICEPRICE pass-through.
- sema + queries.yaml: il_teslimat_perf, il-teslimat/ay-trend/COD doğrulanmış SQL, iş günü kritik kural.

### Grafik UX (chartjs-plugin-datalabels)
- Datalabels (6691088) → format fix '4.2B'→'4.215' + tooltip 0-bug (76a109c) → bar oran hint'e (f0c60fa) → mobil tooltip intersect:false (f8bbeff).
- Donut dilim içi %, bar üstü sade değer, oran hover/tooltip. scatter/barDual datalabel kapalı.

### PWA + Mobil App (3435c44, 5d0bd82, c1771dc)
- **PWA**: manifest (standalone, corporate mavi #4063e6) + ikon 192/512 + apple meta + minimal SW (SignalR'a karışmaz).
- **Mobil alt tab-bar**: `btm-nav lg:hidden` 5 sayfa (Genel/E-tic/Operasyon/Envanter/Müşteri), büyük ikon 1,6rem. **Masaüstü DEĞİŞMEZ** (lg:hidden → sidebar 7 link aynen). Hamburger mobilde kaldırıldı, Asistan üst header ikonu, Görevler bell.
- **LAN HTTPS**: mkcert cert (192.168.1.61:5443), appsettings Kestrel. Program.cs HttpsRedirect KAPALI (http+https paralel) + `/ca.crt` endpoint (doğru MIME). launchSettings 0.0.0.0. cert/ gitignore (private key).

## Build / Test Durumu
- Build: yeşil (tüm commit'ler).
- Smoke: preview (desktop+mobil 390px) — il teslimat/ay drill/COD render, datalabels+tooltip, alt nav (mobil fixed bottom 5 sayfa / desktop none + sidebar 7 link). Drill preview-mobilde AÇILIYOR.
- MCP doğrulama: il teslimat, ay trend, COD, iş günü, veriden tatil hepsi MCP ile birebir.

## Commit Durumu
- Bu oturum: 18 commit (c6936bf → c1771dc).
- Uncommitted: 11 (oturum dışı: asistan/*, tools/diskscan, briefings eski). Handoff dokunmadı.

## İşe YARAMAYANLAR
- **holidays.rest** ($29/ay key) + **Nager.Date** (dini bayram YOK) → API tatil çözümü yetersiz. VERİDEN otomatik kazandı.
- **mkcert LAN HTTPS Android'de install/SignalR vermez** → kullanıcı-yüklü CA Android'de "güvenli değil" (kırmızı X). Bu hem PWA install'ı hem **WebSocket Secure (SignalR)** bağlantısını engeller → sayfa açılır (SSR) ama interaktivite/drill ÇALIŞMAZ. Gerçek cert (tünel) şart.
- **Kargo kar/zarar** yapılamaz: `KargoMaliyetiniHesapla` linked server'da remote çağrılamaz ("Remote function reference not allowed").
- **JS element.click()** Blazor @onclick tetiklemez → dispatchEvent(MouseEvent bubbles) gerekir.

## Kararlar
- Çıkış süresi = İŞ GÜNÜ (takvim 2x abartı). İki performans göster (takvim algı + iş günü gerçek).
- Tatil veriden otomatik (API/hardcode reddedildi — dini bayram + bakım).
- Bar oran datalabel'da DEĞİL hover hint'te (kullanıcı: sabit etiket kalabalık).
- Mobil alt nav lg:hidden (masaüstü bozulmaz — kullanıcı şartı).
- Kargo geliri paneli İPTAL (kullanıcı: gereksiz) → ay→gün drill eklendi.

### Kullanıcı feedback verbatim
> "bayram haziranda değildi ama" — 2025 Haz / 2026 May (hicri kayma); veri ikisini de yakaladı
> "yok yok daisy kullanıyoruz tamamen / özel renk yok" — ikon corporate primary (#4063e6), BKM kırmızısı değil
> "app yap ama normal masaüstü bozulmaz değil mi" — lg:hidden responsive
> "üst menü neden duruyor alt iconlar daha büyük olmalı" — hamburger kaldır + ikon 1,6rem

## Dikkat Edilmesi Gerekenler
- **DRILL MOBİL HTTPS'TE ÇALIŞMIYOR** = güvensiz mkcert cert → WSS/SignalR kurulamıyor → interaktivite yok. Kanıt testi: HTTP'den (http://192.168.1.61:5112) aç → drill çalışmalı. Kalıcı: gerçek cert (Cloudflare tunnel) → WSS+install+drill hepsi.
- mkcert cert makineye özel (gitignore). Başka makinede `tools/cert/mkcert.exe -install` + cert üret gerek.
- LAN run: `dotnet run --urls "https://0.0.0.0:5443;http://0.0.0.0:5112"` (launchSettings http profili 0.0.0.0).

## Yarına Başlangıç Noktası
1. **DRILL MOBİL FIX** — HTTP test (drill çalışır mı = cert/SignalR kanıt). Çözüm: Cloudflare tunnel (gerçek cert → WSS + PWA install + standalone). Kullanıcı tüneli 2× reddetti ama mkcert Android'de hem install hem SignalR vermiyor → tünel tek yol. VEYA iç sunucu + Let's Encrypt.
2. **Mağaza kartı drill** netleştir — TOPLAM/E-tic = modal (kategori); mağaza kartları = /magaza/{id} SAYFA nav. Kullanıcı "diğerleri açılmıyor" dedi → mobilde sayfa nav mı SignalR mı, teşhis.
3. B-41 kalan 4 SQL (günlük detay → ay-drill ile karşılandı · v2 · çıkış-teslim → il'de var) düşük değer/örtüşme.

## Yardımcı Dosyalar
### Yeni
- `dashboard/wwwroot/manifest.json` · `service-worker.js` · `icon-192/512.png` — PWA
- `dashboard/cert/` (gitignore) + `tools/cert/mkcert.exe` — LAN HTTPS
### Değişen
- `EticQueries.cs` (il teslimat/ay/gün/COD/tatil/iş günü) · `PeriodModels.cs` (IlTeslimat/AyKargo/GunKargo/CodOzet)
- `Eticaret.razor` (yeni paneller + gün drill modal) · `charts.js` (datalabels/tooltip/mobil)
- `MainLayout.razor` (alt nav + hamburger) · `App.razor` (PWA head+SW) · `app.tailwind.css` (btm-nav)
- `Program.cs` (ca.crt endpoint + https redirect off) · `appsettings.json` (Kestrel) · `launchSettings.json` (0.0.0.0)
- sema `metrics.yaml`/`bridges.yaml`/`queries.yaml`

---

## Oturum 2 — Mobil tasarım iyileştirme (asistan-ui skill)

## Ana Konu
Kullanıcı: "tasarım ve mobil görünüm berbat, amatörce". asistan-ui skill (mobil UI danışman) devreye alındı. KPI bandı mobil-kompakt + renk/hedef bug fix. **Bu blok değişiklikleri henüz BUILD/COMMIT EDİLMEDİ** (kullanıcı handoff dedi).

## Tamamlananlar (kod yazıldı, commit'siz)
### Mobil polish (Home.razor)
- **KPI bandı mobil-kompakt**: dikey istif (3 metrik tam satır) → net ciro üstte tam + İşlem/İade alt 2-grid ayraçlı (border-top). `lg:flex` desktop yatay korunur.
- **Mağaza/TOPLAM/E-tic kartları**: `text-[22px] lg:text-[25px]` + `whitespace-nowrap` (₺ alt satıra kaymıyordu) + chevron ikon + `active:scale-[.98]` dokunmatik + px-4 py-3.5 + text-xs.

### İki kök-neden bug fix
- **"Mavi üstüne siyah yazı"** → KÖK: DaisyUI **corporate temada `--pc` (primary-content) = %12 luminance ≈ KOYU**. bg-primary (mavi) + text-primary-content (koyu) = siyah-üstüne-mavi. ÇÖZÜM: 3 sayfada (Home/Eticaret/Magaza) KPI `text-primary-content` → `text-white` (mavi zemin beyaz, evrensel). border-primary-content/15 → border-white/20.
- **Hedef %10,5 bug** → KÖK: günlük dönemde kart neti DÜNkü (1 gün) ama hedef AYLIK → günlük net/aylık hedef = %10,5 yanıltıcı. ÇÖZÜM: hedef göster `_period == "ay"` guard (Home kart + Magaza KPI "(MTD)" etiketi). Hedef-Gerçekleşen paneli zaten MTD doğruydu.

## Build / Test Durumu
- ⚠️ **Mobil polish BUILD EDİLMEDİ, COMMIT EDİLMEDİ** — kullanıcı son build'i kestirdi. Home/Magaza/Eticaret.razor uncommitted. Sabah: `dotnet build` + mobil teyit + commit.
- Önceki KPI-beyaz + kart düzeni mobil 390px screenshot ile doğrulandı (KPI beyaz yazı, İşlem/İade 2-grid, hedef günlükte gizli). TOPLAM ₺ wrap son fix build bekliyor.

## İşe YARAMAYANLAR
- **DaisyUI corporate text-primary-content mavi zeminde KOYU** → mavi-üstü-siyah. bg-primary üstü yazı için text-white kullan (primary-content tema-bağlı koyu olabilir). Kalıcı ders: bg-primary + text-primary-content varsayma, temada pc koyuysa kontrast bozulur.

## Kullanıcı feedback verbatim
> "tasarım ve mobil görünüm berbat ve çok amatörce hiç kullanıcı dostu değil" — asistan-ui skill tetikledi
> "mavi üstüne siyahta hoş değil sanki" — primary-content koyu bug
> "günlük hedefte neden %10,5 gibi yazıyor" — günlük net/aylık hedef bug

## Yarına Başlangıç Noktası
1. **Mobil polish BUILD + TEST + COMMIT** (Home/Magaza/Eticaret.razor uncommitted). `npm run build:css` (yeni arbitrary olabilir) + dotnet build + mobil 390px teyit (TOPLAM ₺ tek satır mı) + commit.
2. Mobil polish devam: header sıkışık (uzun başlık + 3 ikon mobilde) · diğer sayfalar (Operasyon/Envanter/Müşteri) KPI/kart mobil gözden geçir · dönem seçici tam genişlik mobil.
3. B-47 (mobil drill cert/SignalR) hâlâ açık.

## Yardımcı Dosyalar
### Değişen (commit'siz)
- `Home.razor` (KPI mobil-kompakt + kart polish + hedef guard) · `Magaza.razor` (KPI white + hedef MTD guard) · `Eticaret.razor` (KPI white)

## Compact Snapshot --- 01:28

### Son 5 Commit

- 4554b86 docs: 2026-06-14 handoff Oturum 2 — mobil tasarım polish (KPI beyaz/kompakt + hedef fix, build/commit bekliyor)
- 015ab62 docs: B-49 mağaza detay perf (seri→paralel) TODO
- 79bd419 docs: 2026-06-14 handoff + TODO sync — B-41 çekirdek + grafik UX + PWA/mobil
- c1771dc feat(bkm): mobil alt nav büyük ikon + hamburger kaldır, asistan üst ikon
- 5d0bd82 feat(bkm): mobil app görünümü (alt tab-bar) + PWA + LAN HTTPS

### Uncommitted Dosya Sayısı

- 16 uncommitted dosya

### Aktif Planlar

- 02-fsm-trafik-analiz.md
- 03-brief-otomasyon.md
- 04-gunluk-kar-zarar-maliyet-karsilastirma.md
- 05-envanter-verim-kpi.md
- 06-mcp-server-ayiklama.md
- 07-dashboard-react.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **B-47 ⚡ DRILL MOBİL FIX** — mkcert güvensiz cert (Android kırmızı X) → WSS/SignalR kurulmuyor → mobil HTTPS'te interaktivite/drill YOK (sayfa SSR açılır, tıklama ölü). Kanıt: HTTP'den (http://192.168.1.61:5112) drill çalışmalı. **Çözüm: Cloudflare tunnel** (gerçek cert → WSS+PWA install+standalone+drill hepsi). Kullanıcı tüneli 2× reddetti ama mkcert Android'de yetmiyor. VEYA iç sunucu+Let's Encrypt. **(YENİ 14.06)**
- - [ ] **B-48** Mağaza kartı drill netleştir — TOPLAM/E-tic = modal(kategori); mağaza kartları = `/magaza/{id}` SAYFA nav (B-40). Kullanıcı "diğerleri açılmıyor" → mobilde sayfa-nav mı SignalR mı teşhis (muhtemelen B-47 cert/SignalR ile aynı kök). **NOT: mağaza detay AÇILIYOR ama YAVAŞ (Özlüce geç açıldı) → B-49.** **(YENİ 14.06)**
- - [ ] **B-49** Mağaza detay (`/magaza/{id}`) PERF — 7-8 sorgu SERİ (KPI+UPT+ödeme+kampanya×2+kategori+hedef), kategori cross-db join (Products→urn→urnKtgr2) aylık ağır. Çözüm: **paralel (Task.WhenAll, ayrı conn/sorgu)** → 7×latency → ~1×. + kategori SQL index/sadeleştir. Aynı pattern E-ticaret/Operasyon çok-sorgulu sayfalarda da uygula. **(YENİ 14.06)**
- - [ ] **B-43** Kafe POS DB erişimi araştır — EncoreMerkez'de kafe yok, xlsx kanonik. Kafe ayrı POS sistemi nerede? **(YENİ)**
- - [ ] **B-21** Sonraki Pazartesi brief Task Scheduler ile otomatik tetikleme. ~~JOKER e-ticaret~~ ✅ 11.06 (B-29, commit 739ad0a). Kalan: kitapsepeti + Heykel + kafeler dahil edilmeli + scheduler kaydı (B-04).
- - [ ] **B-NEW-00 ⚡ (restart sonrası):** SQL bağlantı testi `mcp__sqlserver__sql_query SELECT @@SERVERNAME, GETDATE()`. OK ise B-NEW-01'e geç.
- - [ ] **B-NEW-01** Şema keşfi: `urnKtgr2.ktgrAd` LIKE 'KITAP%' kategori isimlerini bul. EncoreMerkez `Sales`/`SalesProducts`/`Products` describe. Products↔urn köprüsü (LinkedProductId veya Barcode↔barkod fallback).
- - [ ] **B-NEW-02** Geçmiş veri sorguları (3 dönem): May25 (01-31), Apr25 (01-30), Apr26 (01-26 — gün-prorate). Filtreler: `IsValid=1`, `DocumentsTypeId IN (1,2,3,6,7,8)`, kitap kategorisi. CampaignId NULL/NOT NULL ayrı çıkar.
- - [ ] **B-NEW-03** Tahmin modeli: `tahmin = may25 × MIN(MAX(apr26/apr25, 0.5), 2.0) × elastikiyet`. 3 senaryo (1.5x / 2.0x / 2.5x adet). Edge case'ler: Apr25=0, May25=0 ayrı handle.
- - [ ] **B-NEW-04** Excel çıktı (xlsx skill): 7 sheet — Yönetim Özeti / Kategori / Top100 Ciro / Top100 Adet / Tüm Kitaplar / Senaryo Karşılaştırma / Doğrulama.
- - [ ] **B-NEW-05** Doğrulama: top 10 mantıklı mı, kategori dağılımı sağlık check, outlier flag.
- - [ ] **B-NEW-06** (Opsiyonel) Stok ihtiyacı türevi: tahmin × güvenlik − mevcut stok = sipariş öneri.
- - [ ] **B-04** `scripts/register-scheduled-task.ps1` çalıştır → Task Scheduler kaydı (her Pazartesi 09:00). **Tek görev mimarisi:** 14.05.2026'da `send_brief.bat` v5 self-healing yapıldı (brief.html yoksa `generate_brief.py` ile kendisi üretir → eski generator/sender yarış koşulu bitti, 11.05'te bu yüzden mail gitmemişti). `register-brief-generator-task.ps1` deprecated edildi (artık sadece eski `BKM-Brief-Generator` görevini kaldırıyor); `register-scheduled-task.ps1` çalıştırıldığında o eski görevi de otomatik temizler. **Kullanıcı sadece `register-scheduled-task.ps1`'i çalıştıracak.**
- - [ ] **B-20** Pazartesi maili **fikri.eren@bkmkitap.com**'a SPAM'a düşüyor olabilir (4 kez gönderildi 20.04'te, kullanıcı "gelmedi" dedi). Kontrol: BKM webmail spam klasörü. Çözüm: BKM whitelist veya SPF/DKIM doğrulama veya gönderici adresini `fikrieren@gmail.com`'dan BKM SMTP'sine değiştirmek. **(YENİ)**
- - [ ] **B-05** SsmsExcelExporter build & test (`dotnet publish -c Release -r win-x64`) — Ctrl+Shift+E + Ctrl+Shift+W çalışmalı.


---

## Oturum 3 — Mobil app mockup tasarımı (kısa, kod yok)

### Ana Konu
Kullanıcı mevcut mobil görünümü "berbat, amatörce, kullanıcı dostu değil" buldu. asistan-ui skill + visualize mockup ile sıfırdan modern app ana sayfa mockup'ı tasarlandı (HTML/CSS/JS, show_widget). Kullanıcı çok beğendi ("işte bu") → Blazor'a uyarlama kararı (B-51). Geç saat → implementasyon SABAHA ertelendi (kullanıcı "dur yapma sabah dedik").

### Tamamlananlar
- **Mockup v1** (`bkm_mobil_app_ana_sayfa`) — status bar + greeting + dönem segment + gradient hero KPI + 2 stat (İşlem/İade) + ikon-kart mağazalar (trend oklu) + e-tic gradient kart + floating alt nav.
- **Mockup v2** (`bkm_mobil_app_hero_carousel`) — hero'yu **swipe carousel** yaptık: scroll-snap 4 kart (Ciro→İşlem→Sepet Ort→İade) + JS dot gösterge. Kullanıcı isteği: "toplam ciro kısmı slider gibi değişir toplam fiş/sipariş/hedef".

### Build / Test Durumu
- Build: çalıştırılmadı (kod yok, sadece mockup)
- Uncommitted: 16 (önceki B-50 mobil polish + bu oturumdan değişiklik YOK)

### Yarım Kalan / Yarına
### Üst öncelik
- **B-50 COMMIT** — önceki mobil polish (Home/Magaza/Eticaret.razor) hâlâ commit'siz, 16 uncommitted. Build:css + dotnet build + commit.
- **B-51 IMPLEMENTE** — mockup → Blazor. Önce Home (gradient hero carousel + ikon-kart + trend WoW). Tier 3 → plan-first. **WoW verisi yok** → Queries.cs storeSql'e geçen-dönem net eklenecek (StoreCard.Wow).

### Kullanıcı feedback verbatim
> "ama tasarım ve mobil görünüm berbat ve çok amatörce hiç kullanıcı dostu değil" — mevcut mobil
> "işte bu hatta bu yapıyı web e uyarlayalım" — mockup onayı
> "toplam ciro kısmı toplam fiş / sipariş slider gibi değişir hedef vs vs" — hero carousel isteği
> "dur yapma sabah dedik" — implementasyon ertelendi

### Yarına Başlangıç Noktası
1. B-50 uncommitted'i build + commit (16 dosya, eşik aşıldı).
2. B-51 plan yaz (Tier 3): gradient hero carousel + ikon-kart + trend WoW + responsive. Onay al.
3. Trend WoW: Queries.cs:22 storeSql → geçen-dönem net JOIN/2. sorgu, StoreCard.Wow alanı.
