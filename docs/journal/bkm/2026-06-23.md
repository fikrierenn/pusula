# Oturum Günlüğü — 2026-06-23

## Ana Konu
Uzun çok-konulu oturum: (1) Z.ai (BigModel) free GLM-Flash LLM sağlayıcı entegrasyonu, (2) Muhasebe domeni — Kontrol paneli (B-117) + Mizan/likidite dashboard (B-118) + Fin_AyKapanis yönetim formu (Ayarlar), (3) iş eşiklerinin hardcode'dan PanelAyar'a taşınması, (4) **kullanıcının gerçek 2025 Excel mizanıyla uçtan-uca doğrulama** → kapanmış-yıl kapanış-fişi sorununun keşfi + **hiyerarşik Excel-replikası mizan ağacı** (ana→alt→en alt). ~14 commit.

## Tamamlananlar

### Z.ai LLM sağlayıcı (önceki oturum 06-22 devamı, bu blok 06-22 commit'leri)
- `ZaiProvider` (OpenRouter klonu, `thinking:disabled` — flash reasoning bütçeyi yiyordu), zincir Z.ai→OpenRouter→Gemini→Groq. Canlı tool-call + content 200 doğrulandı.

### Muhasebe — Kontrol paneli (B-117)
- `Muhasebe.razor` + `MuhasebeQueries` SP sarıcı (`sp_KapanisMudahaleKontrol_v2`), kapanış-sonrası müdahale forensic. SP+Fin_AyKapanis deploy doğrulandı.
- ID karışıklığı temizlendi: B-117 (forensic Kontrol) vs B-118 (mizan mali-tablo) AYRI track; B-115 çakışması (FIFO) düzeltildi (commit c5b90b5 → 06-22).

### Ayarlar / Fin_AyKapanis (B-117/d) — `6d322ac`
- `Ayarlar.razor` (`/ayarlar`) + `MuhasebeQueries` Upsert(MERGE)/Delete/List. **İlk ERP-yazma** (201, yalnız Fin_AyKapanis).

### İş eşikleri ayarlanabilir — `8565d0c`
- `AyarService` + `PanelAyar` (BkmPanel app-local). Devir(1,5/3) · stockout(%5) · risk(100/200/300) · hariç-marka hardcode'dan çıktı. Envanter/Muhasebe/Tedarikçi oradan okur. Varsayılan=eski değer (davranış değişmez).

### Mizan / likidite dashboard (B-118) — `85e886c` → `4848291`
- İlk sürüm (`85e886c`): `mhsMizan_vw` 3-haneli özet + drill. Sonra **Excel doğrulaması sonrası tam rewrite** (`4848291`):
  - **Hiyerarşik ağaç** (Excel replikası): 3-haneli ana → alt (100.10) → en alt/leaf (100.10.001), recursive `MizanDugum` bileşeni.
  - **Kapanmış yıl fix**: kaynak `mhsMizan_vw` → `mhsFis`, "Kapanış" bilanço fişi NOT EXISTS hariç (kesin mizan).
  - `TekDuzenHesap` (79 üç-haneli ad — mhsHsp tutmuyor, Excel'den).
  - Özet kartlar (nakit/cari/KDV) + denge invariant.

### Bug fix'ler
- `a762880` — **3-parçalı isim**: Db.OpenAsync master'a bağlanır, 2-parçalı `bkm.x`/`mhs.x` → "Geçersiz nesne adı" (208). `DerinSISBkm.` prefix. Mizan/Kontrol/Ayarlar üçü de kırıktı.
- `80b10a4` — Fin_AyKapanis Dapper materialization: smallint/tinyint → record int eşleşmez → `CAST AS int`.

### Kural / doküman
- `.claude/rules/erp-write-policy.md` — **ERP/JOKER salt-okuma, tek yazma hedefi `bkm.Fin_AyKapanis`** (kullanıcı direktifi).
- `sql-server-conventions.md` — 3-parçalı isim dersi + Dapper smallint→CAST dersi.

## Build / Test Durumu
- Build: **yeşil** (0/0 son build).
- Smoke: `/mizan` `/ayarlar` `/muhasebe` HTTP 200, startup hatasız. Z.ai canlı 200.
- **Render görsel onay: YAPILMADI** (login-gated). Yazma yolları (Fin_AyKapanis CRUD, ayar kaydet) canlı test edilmedi.
- Veri doğrulama (MCP): denge Borç=Alacak (2025: 28,93 mlr, 2026: 21,48 mlr) ✓; 2025 kesin mizan Excel'le 100/102/120/320/600 **kuruşu kuruşuna**.

## Commit Durumu
- Uncommitted: 1 (`app.css` Tailwind artifact) + bu handoff'ta yeni `sorgular/2026-06-23-mizan-kesin-kapanis.sql` (arşiv).
- Bugün (06-23) commit: 7 — `a762880`, `6d322ac`, `85e886c`, `8565d0c`, `80b10a4`, `255f1ee`, `4848291`.

## İşe YARAMAYANLAR
- **Mizan kaynağı `mhsMizan_vw` (kapanmış yıl)** → her hesap Borç=Alacak=Bakiye 0 (kapanış fişleri view'da dahil). Çözüm: `mhsFis`'ten hesapla, Kapanış fişi hariç.
- **Kapanış'ı ayırmak için `fisTip`** → işe yaramaz (fisTip yalnız 0/2; VİRMAN+Kapanış+normal hepsi 2). Doğru ayraç: ad=`Kapanış` + pozisyon (max yevmiye, her yıl tek).
- **Mizan için tüm `VİRMAN` + Kapanış hariç tutmak** → fazla agresif (6xx bakiyeli "geçici mizan" çıkar, Excel'le tutmaz). Gerçek mizan VİRMAN'ları TUTAR, yalnız bilanço-Kapanış'ı hariç tutar.
- **2-parçalı SQL isim** (app master'da) → 208. **MCP `database=DerinSISBkm` ile test YANILTIR** (orada 2-parçalı çalışır, app'te çalışmaz).
- **SP `SUM(bit)`** (Kontrol OZET) → "sum işleci için bit geçersiz" — `SUM(CAST(... AS int))` gerekir (SP'de, daha düzeltilmedi).

## Kararlar
### Reddedilenler
- ~~Mizan için mhsMizan_vw~~ → kapanmış yıl 0 verir; `mhsFis`+Kapanış-hariç seçildi.
- ~~Kapanış'ı fisAd ile~~ (kırılgan dendi) → pozisyon (max yevmiye) + sistem-adı kombinasyonu; `NOT EXISTS fisAd=N'Kapanış'` (her yıl tek, =max yevmiye doğrulandı).
- ~~Ayar/Fin_AyKapanis aynı dosya~~ → Mizan AYRI dosyalara (Mizan.razor/MizanQueries), Kontrol ile çakışmasın.
### Seçilenler
- **ERP yazma yalnız Fin_AyKapanis** (201); diğer her şey salt-okuma. PanelAyar/görev/bellek = BkmPanel app-local (yazma serbest).
- Mizan = kesin mizan (Kapanış-öncesi), 590 dönem kârı UI dipnotlu.

## Konuşulan ek konular
### 590 Dönem Kârı 5,87M fark — ÇÖZÜLDÜ (veri, formül değil)
- Excel **28.04.2026 10:34 snapshot**. `692/590 HESAP VİRMAN` fişi **30.04.2026** revize (Excel'den sonra) → dönem kârı 20.946.077 → 15.076.077 (−5.870.000). DB güncel/final, Excel bir revizyon eski. Diğer tüm hesaplar birebir.

### Kullanıcı feedback verbatim
> "sadece o tabloya yazacaksın başkası yasak" — Fin_AyKapanis dışı ERP yazma yasağı (→ erp-write-policy.md)
> "başka ayarlar varsa hardcode dan çıkartıp buraya ekleyebiliriz" — PanelAyar iş eşikleri
> "yalnız ayarlar kısmı bkmpanel üstünde olmalı sadece aykapanış 201 de" — mimari ayrım
> "farkı bul kabul edilemez" — 590 farkı izlenip çözüldü (revizyon zamanlaması)
> "mizanı excel in aynısı yapman lazım ana hesap bazında alt hesap bazında en alt kırılım" — hiyerarşik ağaç

## Dikkat Edilmesi Gerekenler
- ⚠️ Tüm yeni sayfaların **render + yazma yolu login-arkası test edilmedi** — kullanıcı doğrulayacak.
- ⚠️ Mizan `mhsFis` sorgusu sirket=4 (2024, 500K satır) için yavaş olabilir (~4s+ NOT EXISTS). Loading spinner var; gerekirse optimize.
- ⚠️ Mizan 590 dönem kârı = kapanış-öncesi güncel (revizyon sonrası) — formal mizandan farklı olabilir (UI dipnotlu).
- Kapanış-hariç mantığı `fisAd=N'Kapanış'`'a bağlı — DerinSIS bu sistem-adını değiştirirse kontrol gerek.

## Yarına Başlangıç Noktası
1. **Kullanıcı `/mizan` (2025), `/ayarlar`, `/muhasebe` görsel + yazma testi** → sorun varsa logdan çek.
2. **Kontrol paneli SP `SUM(bit)` fix** — `sp_KapanisMudahaleKontrol_v2` OZET'te `SUM(SonradanDeg)` → `SUM(CAST(SonradanDeg AS int))` (5 kolon: SonradanDeg/GecGiris/YuvarlakTutar/Mukerrer/GirenOnaylayanAyni). SP repo'da `sorgular/2026-06-22-muhasebe-kontrol-v2.sql`, Fikri deploy eder.
3. Açıksa: B-117 (c) sema mhs/kontrol yazımı tamam mı (255f1ee yapıldı), Mizan perf, 201/FIFO (B-114).

## Yardımcı Dosyalar
### Yeni
- `dashboard/Data/ZaiProvider.cs`, `dashboard/Data/MizanQueries.cs`, `dashboard/Data/AyarService.cs`, `dashboard/Data/TekDuzenHesap.cs`
- `dashboard/Models/MizanModels.cs`, `dashboard/Models/AyarModels.cs`
- `dashboard/Components/Pages/Mizan.razor`, `MizanDugum.razor`, `Ayarlar.razor`, `Muhasebe.razor`
- `.claude/rules/erp-write-policy.md`
- `sorgular/2026-06-23-mizan-kesif.sql`, `sorgular/2026-06-23-mizan-kesin-kapanis.sql`
### Değişen
- `dashboard/Data/MuhasebeQueries.cs` (Fin_AyKapanis CRUD + 3-parça + CAST), `RefQueries.Envanter.cs` (hariç-marka param)
- `dashboard/Program.cs`, `NavRegistry.cs`, `Components/Pages/Envanter.razor` (ayar wiring)
- `.claude/rules/sql-server-conventions.md` (3-parça + Dapper dersleri)
- `plans/25-muhasebe-finans-paneli.md` (B-118 ID + fisBA düzeltme)

> curator-check: 2026-06-23 (6g < 7g — hafif yüzey: bu oturum sema güncel [255f1ee mhs/cari], keşif SQL arşivlendi, çelişki yok; tam dry-run gerekmedi)
