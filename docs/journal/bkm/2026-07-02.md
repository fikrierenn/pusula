# Oturum Günlüğü — 2026-07-02

## Ana Konu
BKM görev-tanımları projesi (`D:\Dev\gorevtanimlari\bkm`) Mosaik'e taşınırken keşfedilen DB gerçekleri `sema/entities.yaml`'a işlendi (continuous-learning). Zirve ERP (BKM_GENEL) + Mosaik portal DB semantik katmana eklendi.

## Tamamlananlar
- **`sema/entities.yaml` — databases:** `BKM_GENEL` (Zirve ERP — bordro/personel/SGK; ZIRVE\ZRVSQL2008, port 64507) + `Mosaik` (reporthub portal DB, BT-FIKRI\SQLEXPRESS).
- **entities:**
  - `BKM_GENEL.dbo.perbilgi` (ham personel master) + **`BKM_GENEL.dbo.vw_PersonelDepartman`** (İK KANONİK — aktif=`Ict IS NULL`=271, temiz Unvan/Departman; Mosaik sp_Ik* raporları bunu kullanır). ⚠ ham perbilgi yerine vw tercih; Personeldurumu DEĞİL Ict IS NULL.
  - `Mosaik.dbo.OrgPositions` · `SopDocuments`/`SopVersions` · görev-tanımı modülü tabloları (GorevDocuments/Versions/ZirveMap/PersonelMap).
- Confidence/evidence/last_verified ile; `updated: 2026-07-02`. PyYAML parse doğrulandı.

## Build / Test Durumu
- entities.yaml PyYAML ile geçerli. Şema gerçekleri MCP (zirve/portalhub) canlı sorgularla doğrulandı.

## Commit Durumu
- **Uncommitted** — `sema/entities.yaml` değişti. Commit kullanıcı kararı (atılmadı).

## Yarım Kalan / Yarına
- Görev eşleme köprüsü (GorevPersonelMap ↔ vw_PersonelDepartman) doldukça `bridges.yaml`'a köprü kaydı.
- Zirve `Personelno` = alfanümerik string ("4634-BKM").

## Dikkat
- Kanonik proje repo: `D:\Dev\gorevtanimlari\bkm` (DECISIONS G/H, git değil). Kod: `D:\Dev\reporthub\Mosaik.Modules.GorevTanimlari`.
