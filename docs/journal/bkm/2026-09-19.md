# Oturum Günlüğü — 2026-09-19

## Ana konu: Vardiya Yönetim Uygulaması (plan 48) — ilk sürüm TAMAM

GMY kararı 18.09 gecesi: *"vardiya yönetimi için ayrı bir program yazmalıyız"*.
Bugün Adım 2-7 kapandı. Uygulama ayakta, 17 kullanıcı açık, onay akışı çalışıyor.

## Tamamlananlar

| Adım | Ne | Commit |
|---|---|---|
| 2 | Ortak kütüphane `lib/Bkm.Shared` — `Db` · `SqlErrorClassifier` · `VardiyaQueries` · modeller | `3faa6c1` |
| 3 | `vardiya-app` iskeleti → Blazor'dan **Razor Pages**'e çevrildi + Solum bağlandı | `5ff45f2` · `2e58852` |
| 4 | Solum kimlik · şube kapsamı (b)-tam · zamansal ACL · 17 kullanıcı seed | `593c985` · `6db40c8` · `b8b3537` |
| 5 | Mesai raporu taşındı, beş partial'a bölündü | `e5a99ad` · `d5776a8` |
| 6 | Onay akışı + atomik denetim izi | `d95f131` |
| 7 | Panel salt-okuma özete indi | `24489ec` |

## Kararlar (GMY)

- **Ayrı uygulama, ortak DB + kütüphane** — panel içinde modül değil.
- **Razor Pages** — `Solum.Web` Razor Pages/MVC kütüphanesi (ölçüldü: `src/` altında
  `.razor` 0). Blazor iskeleti atıldı, bir saatlik kayıp.
- **Domain hesabı YOK** → kullanıcı adı + şifre (Solum `DapperUserStore`).
- **Personel rolü ilk sürümde KAPALI** → hesap sayısı 362'den ~17'ye indi, KVKK yükü ertelendi.
- **Şube boğazı (b)-tam** — kapsam veritabanında çözülür, uygulama şube kimliği taşımaz.
- **Sistemden bağımsız yetki · bölge yok · geçmiş önemli** → ACL `kullanıcı↔şube`,
  zamansal (`GecerliBas`/`GecerliBit`), hiyerarşi yok.
- **Panel salt-okuma kalsın** — yazma yolu söküldü.
- **Kod İngilizce** (dört kez hatırlatıldı): tip, metot, property, DTO alanları çevrildi;
  SQL kolonları Türkçe kaldı, arada alias var. UI metinleri Türkçe.

## ⚠ İKİ KAPSAM SIZINTISI — ikisi de kendi eklediğim süzgeçten

1. **`GetStayBandsAsync`** — sekiz sorguya elle kapsam ekledim, biri atlandı.
2. **`GetSummaryAsync` devir alt-sorgusu** — kapsam vardı ama alt-sorguda yoktu.
   Şube müdürü kendi döneminin 334 saatini ama **tüm şirketin 1.858 saatlik devrini**
   görüyordu. Ekranda 2.192,6 saat yazıyordu ve **makul görünüyordu**.

Ders `sql-server-conventions.md`'ye yazıldı: **kapsam her alt-sorguda ayrı.**
Kapı: `tools/vardiya_kapsam_denetimi.py` (ilk hâli ikinciyi görmüyordu, güçlendirildi).

## Solum tarafında bulunan İKİ kütüphane kusuru

Üçüncü tüketici olmamızın somut karşılığı — ikisi de *"derleme temiz, çalışma anında
hiç çalışmıyor"* sınıfı:

1. **`0025_SolumAuditTrail.sql` hiçbir SQL Server'da koşmuyordu** — `Changes
   NVARCHAR(8000)`, T-SQL sınırı 4000 (Hata 2717). `NVARCHAR(MAX)` ile onarıldı.
2. **`Solum.Identity` hiçbir sorgusunda parametre bağlamıyordu** — `Command(spec,
   object? parameters, ct)`; kullanıcı tanımlı implicit dönüşüm hedef `object` iken
   **uygulanmaz**, `ParameterSet` Dapper'a POCO olarak gidiyordu ve public property'si
   olmadığı için sıfır parametre bağlanıyordu. İmza `DynamicParameters` yapıldı.

Solum ikisi için de **gerçek SQL Server'a karşı koşan** duman testi yazdı
(`tests/Solum.Smoke.Tests`). Ölçümleri: o ana kadar SQL Server'a karşı koşan test
**sıfırdı** — EF testleri SQLite açıyor, Identity testlerinin bağlantısı hiç açılmıyordu.

⚠ Bypass yazmadım: `PasswordHasher`'ı doğrudan çağırıp kullanıcı yazabilirdim ama giriş
yine aynı store'dan geçeceği için kırık kalırdı — yarım çözüm gerçek durumu gizlerdi.

## Ölçümler

**Kadro (Zirve `vw_PersonelDepartman`, işten çıkışı olmayanlar):** 17 kişi —
GMY 2 · İK 3 · şube sorumlusu 12. Kafeler müdürü **üç** kafeden sorumlu (ACL'nin
çok-a-çok olmasının ilk gerçek kullanımı).

**Kapsam (uçtan uca, gerçek oturum):**

| Kullanıcı | Kişi-gün | Eksik (dönem+devir) |
|---|---|---|
| resul.cil (FSM) | 793 | 369,0 saat |
| murat.sadik.erbas (3 kafe) | 956 | 291,3 saat |
| ceren.bilmis (İK, tümü) | 6.113 | 6.118,4 saat |

Üçü de SQL ile birebir. Kimliksiz istek → 0 şube, 0 kesim (fail-closed).

**Onay akışı:** FSM müdürü kendi şubesine → kaydedildi · ŞURA'ya → *"kapsamınızda
değil"* · GMY (onay yetkisi yok) → `/AccessDenied`, raporunda onay bağı 0.

**Denetim izi atomik:** iz tablosunun adı bozuldu → onay tablosuna **0 kayıt**,
kullanıcıya generic hata. Geri alındı.

## Kurulan kapılar (hepsinin kırılabilirliği kanıtlandı)

| Kapı | Ne yakalar |
|---|---|
| `tools/vardiya_kapsam_denetimi.py` | atlanmış kapsam süzgeci · alt-sorgu sızıntısı · panelde yazma yolu |
| `tools/mesai_esik_denetimi.py` | C#↔Python mevzuat eşiği çatallanması |
| `tools/panel_kolon_denetimi.py` (genişletildi) | `AS` kullanmayan SQL artık görünüyor; vardiya çifti eklendi |

Hook tetiği `SatisAnalizi` → `SatisAnalizi|Vardiya|Vrd` genişletildi — kapı kuruluydu
ama **çağrılmıyordu**.

## Yarına / açık kalanlar

- **Seed kadro listesi koda gömülü** — Zirve'den canlı okunmalı ama şube adları birebir
  eşleşmiyor (`GENEL YÖNETİM` ↔ `GENEL MÜDÜRLÜK`); eşleme tablosu Solum plan-21'i bekliyor.
- **`ciktilar/` altındaki geçici şifre dosyası** — İK dağıtımdan sonra silmeli.
- **Personel rolü** (Faz 2) · **vardiya planı girişi** ve **izin günü tanımı** hâlâ kapsam dışı.
- Solum kabuğu (`_SolumLayout`) henüz devrede değil — sade düzen kullanılıyor.
- `tools/vardiya_kapsam_denetimi.py` **metin denetimi**: süzgecin doğru kolona bağlı
  olduğunu görmez.

<!-- otomatik iz · 02:25 · tur 408 · 3 dosya degisti
   ISTEKLER:
     > adım 6'ya geç
     > panelde salt-okuma özet kalsın
     > devam
     > v-06 yap
     > solum cevabını bekle
     > geldi mi
   DOSYALAR:
     dashboard/Components/Pages/Vardiya.razor
     tools/turkce_tanimlayici_denetimi.py
     .claude/hooks/pre-commit-antipattern.sh
     (hook yazdi — KARAR ve GEREKCE hala elle yazilir)
-->

## Compact Snapshot --- 05:32

### Son 5 Commit

- 53421d1 chore(bkm): test projesi bin/obj gitignore
- 5477bec feat(bkm): TURKCE TANIMLAYICI KAPISI + uctan uca kapsam testi iskeleti (V-06)
- 00cf990 docs(bkm): journal 19.09 + kapsam/ayrilmis-kelime dersleri + TODO V-01..06
- 24489ec feat(bkm): panel SALT-OKUMA ozete indi (plan 48 Adim 7) — ilk surum TAMAM
- d95f131 feat(bkm): ONAY AKISI + DENETIM IZI (plan 48 Adim 6) · rapor sayfasi bolundu

### Uncommitted Dosya Sayısı

- 1 uncommitted dosya

### Aktif Planlar

- 02-fsm-trafik-analiz.md
- 03-brief-otomasyon.md
- 04-gunluk-kar-zarar-maliyet-karsilastirma.md
- 05-envanter-verim-kpi.md
- 06-mcp-server-ayiklama.md
- 07-dashboard-react.md
- 08-apexcharts-migration.md
- 09-dashboard-rafine.md
- 10-dashboard-icerik-genisleme.md
- 17-python-eksik-portlar.md
- 18-ic-kart-tek-filtre.md
- 20-bkm-asistan-pwa.md
- 21-asistan-mail-takvim.md
- 23-fifo-maliyet-sema-entegrasyon.md
- 25-muhasebe-finans-paneli.md
- 26-zai-provider.md
- 27-banka-ekstresi-siniflandirici.md
- 28-homev2-dark-theme.md
- 29-mcp-app-pilot.md
- 30-gider-merkezi-mizan-matris.md
- 31-gelir-tablosu-gider-merkezi.md
- 32-satinalma-dashboard.md
- 33-sube-bulunurluk-takibi.md
- 34-satinalma-hesap-sorma-derinlesme.md
- 35-sinav-paneli-hizli-kazanimlar.md
- 36-hediye-ceki-paneli.md
- 37-patron-sorulari-paneli.md
- 38-kadro-paneli.md
- 41-irstipgrupmap.md
- 42-satis-analizi-paneli.md
- 43-wms-erp-defter-esitleme.md
- 44-sema-sozlesme-ve-temellendirme.md
- 45-sema-cekirdek-ogrenen-repo-otesi.md
- 46-siparis-onerisi-panelde.md
- 47-vardiya-eksik-fazla-sql.md
- 48-vardiya-yonetim-uygulamasi.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **V-01 Geçici şifre dosyası SİLİNECEK** — `ciktilar/vardiya-ilk-sifreler-*.txt`
- - [ ] **V-02 Seed kadro listesi koda gömülü** — `vardiya-app/Security/Seed.cs`. Kadro
- - [ ] **V-03 Solum kabuğu devreye alınmadı** — `_SolumLayout` (yan menü, üst çubuk)
- - [ ] **V-04 Personel rolü (Faz 2)** — şemada tanımlı, hiçbir hesaba atanmadı.
- - [ ] **V-05 Vardiya planı girişi + izin günü tanımı** — ilk sürüm kapsamı dışında
- - [ ] **V-06 Kapsam denetimi metin tabanlı** — süzgecin DOĞRU kolona bağlandığını
- - [ ] **B-170 Satış Analizi — YENİ ÜRÜN kör noktası + yolda stok + kart fiyatı ile değerleme** — 09.09, stkID 1739183 incelemesinden doğdu (Maxx MX-617 Tahta Kalem Seti, kart açılışı 04.09.2026, 5 günlük ürün).
- - [ ] **B-173 Ağu-Eyl 2026 alım röntgeni — ALICIYA SORULACAKLAR (ölçüm bitti, karar bekliyor)**
- - [ ] **B-180 SİPARİŞ KARŞILANMA BAĞI — hâlâ ölçülemiyor (sema'daki borç)**
- - [ ] **B-176 "Muhtelif Ürün" / "İskonto ve Fiyat Farkı" ERP'de `urnTip=0` — BAŞKA analizler?**
- - [ ] **B-177 KAYIP FORMÜLÜ bugünkü fiyatla şişiyor (ÖLÇÜLDÜ 12.09, düzeltilmedi)**
- - [ ] **B-172 Satış Analizi — DANIŞMAN İTİRAZLARI (satinalma-danisman 10.09, UYGULANMADI)** —
- - [ ] **B-172(c2) AŞIRI STOK KARTININ KÖR NOKTASI — sezon kapısı (ÖLÇÜLDÜ 12.09, KARAR BEKLİYOR)** —
- - [ ] **B-171 Satış Analizi — BEYAN EDİLDİ ama DÜZELTİLMEDİ (bilinçli borç listesi)** — 10.09.
- - [ ] **B-167 Satış Analizi — kullanıcı kararı bekleyen üç eşik/adlandırma** — 09.09. (a) KPI **"Bağlanan Stok"** adı maliyeti ima ediyor ama **satış fiyatı** gösteriyor (ölçülen bir üründe 4,6× fark) → ad mı taban mı değişecek? (b) ✅ **eşikler artık ÖLÇÜLDÜ (10.09)** — aşırı stok **5× → 3×** (2024 stoğunun sonraki 12 ayda satılan oranı 3×'te 1,0'ın altına düşüyor: 29.647 çeşit / 309,9M ₺ etiket · 107,3M ₺ maliyet) ve sezon açığı **0,50 doğrulandı** (aynı talep bandında gerçekleşme <0,25→0,34 · 0,25-0,50→0,61 · 1,00-1,50→0,93); türetme `sorgular/2026-09-10-esik-turetme-asiri-stok-ve-sezon.sql`, sema `asiri_stok_esigi_turetildi`/`sezon_hazirlik_esigi_turetildi`. **Confound beyan edildi:** ters nedensellik + ortalamaya dönüş giderilmedi. ✅ **KATEGORİ BAZLI EŞİK KAPANDI 10.09** (Kırtasiye 2× · Hazırlık Kitapları 8× · kalan sekiz 3×, ölçümle); `bkm.OneriSiparisKtg3Ondeger` politika tablosu kullanıcı kararıyla İPTAL edildi. AÇIK KALAN: sipariş noktasındaki %50 emniyet payı hâlâ ÇIKARIM. (c) ✅ **iade hakkı KISMEN çözüldü (10.09)** — `frmIadeKural` kodunun anlamı hâlâ teyitsiz (B-150) ve ölçüm koddan anlam çıkmadığını gösterdi, ama tedarikçinin **gözlenen iade oranı** vekil olarak çalışıyor: aşırı stoğun %25,2'si (77,6M ₺) iadesi %2 altındaki tedarikçilerden. Panelde kırılım YAPILMADI (vekilin dört sınırı var: hak≠davranış · tedarikçi düzeyi · ters nedensellik · %14,8 tedarikçisi bilinmeyen), kapsam bandında beyan edildi.


---

## Oturum 2 — V-06 testi KOŞTU · Türkçe tanımlayıcı kapısı · curator-check

### Türkçe tanımlayıcı kapısı (`tools/turkce_tanimlayici_denetimi.py`)

GMY aynı şeyi **altı kez** söylemek zorunda kaldı ("hâlâ Türkçe isim kullanmaya devam
ediyorsun"), sonunda: *"ya buna bir kapı hook ne koyacaksan koy artık"*. Kural yazılıydı
(`turkish-ui.md`) ve okunmuştu; **çiğneyeni gören yoktu** — `test-discipline.md` § yazılı
kural ≠ uygulanan kural sınıfının onbirinci vakası.

Kapı `vardiya-app/ · lib/Bkm.Shared/ · tests/` altındaki `.cs`/`.cshtml` dosyalarında
tanımlayıcı adlarını tarar; **yorum ve UI metni Türkçe KALIR**.

**En değerli bulgu kapının KENDİ KÖR NOKTASIYDI.** İlk hâlde yorum deseni
`//.*$` + `re.S` ile derlenmişti: `re.S` `.`'ı satır sonuna da uydurduğu için ilk `//`
yorumundan **dosya sonuna kadar her şey** siliniyordu. Yani hiçbir C# dosyası gerçekte
denetlenmiyordu ve kapı sessizce yeşil veriyordu. Yalnız kırmızı kip koşulduğu için
görüldü; `//[^\n]*` yapılınca **43 gerçek ihlal** buldu (hepsi temizlendi).

> Ders: yeni kapının ilk koşusu yeşilse bu bir sonuç değil, bir **şüphedir** —
> kırmızı verdiği görülmeden kapı kurulmuş sayılmaz.

Hook tetiği: `.claude/hooks/pre-commit-antipattern.sh` (staged `.cs`/`.cshtml`).

### V-06 — test KOŞTURULDU, İKİ kusur çıktı (biri ÜRÜNDE)

Dün iskelet yazıldı ama **koşturulmadı**. Bu oturumda koşturuldu:

| # | Nerede | Ne |
|---|---|---|
| 1 | testin kendisi | antiforgery token **kazınıyor ama POST gövdesine konmuyordu** → 400. Hata `Giriş başarısız` diye okunuyordu: kapsamı hiç ölçmeden **yanlış teşhis**. |
| 2 | **ÜRÜN** | `VardiyaQueries`te SQL hâlâ `@sube` istiyordu, C# tarafı yeniden adlandırmadan sonra `branch` gönderiyordu → `Must declare the scalar variable "@sube"`. |

İkincisi **üç sorguyu birden** vuruyordu (`GetOvertimeSourceAsync` · `GetStayBandsAsync` ·
`GetRowsAsync`): şube süzgeci seçili her istekte sayfa "Rapor okunamadı" veriyordu.
**Derleme temizdi, dört kapının hiçbiri görmedi** — derleyici SQL metnini bilmez,
kapsam kapısı da metin sayar, parametre adı eşleşmesine bakmaz.

> Bu, dün yazılan "kapsam süzgeci metin denetimidir, doğru kolona bağlandığını görmez"
> sınırının kardeşi: **parametre adının eşleştiğini de hiçbir şey görmüyordu.** Onu
> gören tek şey uçtan uca testti — yazıldığı gün koşturulsaydı dün yakalanırdı.

**Kırılabilirlik kanıtlandı:** `GetSummaryAsync` kapsam süzgeci bilerek `1=1 OR …`
yapıldı → müdür tüm şirketi gördü → test **KIRMIZI**; geri alındı → **3/3 yeşil**.

Commit `8fd07b3`.

### Kapsam nüfusu (Solum'un S1 sorusuna ölçüm)

`VardiyaQueries.cs`: **10 sorgu · 10'u da kapsamlı · 12 süzgeç yüzeyi**
(`GetSummaryAsync` tek başına 3 taşıyor: ana sorgu + iki devir alt-sorgusu).
Payda 10 değil **12**; sızıntı riski metot sayısıyla değil **yüzey** sayısıyla ölçülür.

### Solum'un üç sorusu — cevaplar

- **S1** (kapsam nüfusu) → yukarıdaki ölçüm.
- **S2** (`0025_SolumAuditTrail.sql` nasıl koştu) → **düzeltilmiş sürüm alındı, betik
  elle yamalanmadı.** Yerel defterde Solum'un kendi özeti duruyor; sonraki yükseltmede
  `ContentChanged` beklenmiyor. *Migration riski YOK.*
- **S3** (orantısızlık sezgisi mekanikleşti mi) → **HAYIR.** İkinci kapsam sızıntısını
  bulan şey insan sezgisiydi; bugün ekrandaki toplamı kapsam kümesinin toplamıyla
  otomatik karşılaştıran bir mekanizma **yok**. TODO V-07 olarak açıldı.

### Curator-check — 2026-09-19 (vade 10 gün geçmişti)

> curator-check: 2026-09-19

Hafif tarama (mutasyonsuz, hiçbir dosya değişmedi):

| Tarama | Bulgu |
|---|---|
| Stale sema (tarih aşmış) | **8** — hepsi FIFO/urn kümesi, aşım 2-4 gün (taze bayat) |
| `last_verified` YOK | **26** (10'u gerçek kayıt · 16'sı `_contract.yaml` şema kuralı, muaf sayılmalı) |
| `teyit bekliyor` duran | 6 |
| `SÜPERSEDED` duran | 8 |
| Açık TODO | **109** (32 tarihli · 77 tarihsiz · ≥30g olan 17) |

09.09 raporunun ana bulgusu (`last_verified` yokluğu) **kapanmadı** ama sayı 16 → 10'a
indi. Otomatik aksiyon alınmadı (curator hard-rule: stale = bayrak).

### Solum'dan araç araştırması — kapsam kapısına doğrudan değiyor (oturum sonrası not)

- `Microsoft.CodeAnalysis.BannedApiAnalyzers` (RS0030/RS0031, `BannedSymbols.txt`):
  Dapper çağrısı bir **boğaz sınıfı dışında yasaklanabilir** → sorgu tek yerden geçmek
  zorunda kalır, "elle süzgeç eklemeyi unutmak" derlenmez hâle gelir. Bizim metin
  kapımızın yerine değil **altına** girer.
- ⚠ Ama **ikinci sızıntımızı çözmez.** "Kapsamlı tabloya dokunduysan süzgeci de yazmış
  olacaksın" bir **birlikte-bulunma** gereksinimidir; ne yasak listesi ne mimari testi
  (ArchUnitNET / NetArchTest) bunu ifade edebiliyor — hepsi bir çağrının varlığını ölçer,
  iki şeyin birlikteliğini değil. Tek çözüm **yapı**: ham tablo adını yazan yol hiç
  kalmasın, kapsamlı tabloya erişen tek yol `@userId` alıp TVF birleşimini kendi kuran
  metot olsun. O zaman alt-sorgu da otomatik kapsanır.
- Elenenler (kovalanmayacak): **Testcontainers** — bizim "şema + 9 şube + kişi-gün verisi
  kurmak pahalı" sorunumuzu çözmüyor, "makinede SQL Server"ı "makinede Docker"a taşıyor
  (CI kurulursa değer kazanır; imaj Developer edition). **NetArchTest** 2021'den beri
  yayınsız. **ArchUnitNET** aktif ama kapsam sızıntısına faydası yok.

### V-09 KAPSAM BOĞAZI kuruldu — süzgeci unutmak artık mümkün değil (commit `a2157c4`)

Kapsam bugüne kadar **her sorguda elle** yazılıyordu. Aynı gün dört ad/süzgeç kusuru
çıktı ve dördünde de derleme temizdi; dördüncüsü boğaz kurulurken ortaya çıktı:

| # | Nerede | Ne |
|---|---|---|
| 1 | `GetStayBandsAsync` | süzgeç elle eklenirken **atlandı** |
| 2 | `GetSummaryAsync` devir alt-sorgusu | kapsam vardı, alt-sorguda yoktu (334 saat ↔ 1.858 saat) |
| 3 | 3 sorgu | SQL `@sube` ↔ C# `branch` |
| 4 | **`SaveApprovalAsync`** | SQL `@girisDk/@cikisDk/@ekMesaiDk/@kaydeden` ↔ C# `inMin/outMin/extraShiftMin/savedBy` — **onay yazma yolu hiç çalışmıyormuş** |

**Yapı** (`lib/Bkm.Shared/Data/VrdSql.cs`):
- `VrdSql.PersonDays` / `.Carryover` — süzgeç **kaynağın içinde**. Alt-sorgu da aynı
  sabiti kullandığı için otomatik kapsanır: kapsam **tablo adının değil kaynağın** üstünde.
- `VrdParams` — parametre adı tek yerde. Anonim nesne kaldırıldı; orada ad = yerel
  değişken adıydı ve yeniden adlandırma onu sessizce ayırıyordu.
- Her çağrıda SQL `@token` kümesi ↔ parametre kümesi **iki yönlü** karşılaştırılıyor.
  3 ve 4 artık DB'ye hiç gitmeden, çağrının kendisinde patlar.

**Kapı** (`tools/vardiya_kapsam_denetimi.py`) yeniden yazıldı: metin saymıyor, yapıyı
koruyor — boğaz tekeli (176 üretim dosyası) · boğaz atlanmıyor · kapsam çapası · panel
salt-okuma. Yakalama sözleşmesi dosyanın başında.

**Kırmızı kip üç kez** koşuldu (hepsi KIRIK, hepsi geri alındı): boğaz dışında ham
`FROM bkm.Vrd_KisiGun` · doğrudan `cn.QueryAsync` · `VrdParams.For(userId)` sökülmesi.

> ⚠ Dürüst sınır: bu bir **tip değil, boğaz**. C# geçmeye zorlamıyor. İki bilinen atlatma
> hem `VrdSql` dokümantasyonunda hem kapının sözleşmesinde yazılı, ikisini de kapı
> yakalıyor. "Yapı + kapı birlikte" — yapı tek başına yeter denmiyor. (V-10 = derleyiciye
> zorlatma.)

Test 8/8 yeşil: 3 uçtan uca + 5 boğaz sözleşmesi (DB'siz).

### Solum deposu bir süre derlenmez oldu — kök neden bizde bulundu

`dotnet test` restore aşamasında `Geçersiz çerçeve tanımlayıcısı ''` verdi. Belirti
yanıltıcıydı: `TargetFrameworks` doğru çözülüyordu (`net8.0;net10.0`), patlayan iç
build'di. TFM'ye pinleyince gerçek hata çıktı:

```
BannedSymbols.txt(5,1): error RS0031: '' sembolü yasaklanan API'ler listesinde
birden çok kez listelenmiş
```

`BannedSymbols.txt` **hiçbir yorum biçimini desteklemiyor**; yorum satırları yinelenen
boş sembol sayılıyor ve `TreatWarningsAsErrors` ile tüm depoyu derlenmez yapıyor.
⚠ İlk teşhisim "bare `#` satırları" idi ve EKSİKTİ — Solum ölçtü, `# ---` de kırıyor.
Tek güvenilir yol: dosyada yalnız girdi bırakmak, gerekçeyi yanına taşımak. Solum'a bildirildi; TODO **V-10**'a uyarı olarak da yazıldı — biz aynı paketi
kuracağız ve aynı tuzağa düşmemeliyiz.

> Sınıfı ilginç: kapı çalıştı — **ilk yakaladığı şey kendi yapılandırma dosyası oldu.**

### V-10 — boğaz artık DERLEYİCİ tarafından zorlanıyor (commit `704c508`)

V-09'un dürüst sınırı şuydu: *"bu bir tip değil, boğaz — C# geçmeye zorlamıyor."*
V-10 o cümleyi kapattı. `Microsoft.CodeAnalysis.BannedApiAnalyzers` + tek girdilik
`BannedSymbols.txt` (`T:Dapper.SqlMapper`) ile **Dapper çağrısı `VrdSql` dışında
derlenmiyor** (RS0030). Muafiyet iki dosya: boğazın kendisi ve `AuditTrail.cs`
(kapsamlı tablo okumaz). Kapsam bilinçli dar: yalnız `lib/Bkm.Shared`.

Üç ölçüm çıktı ve üçü de not edilmeye değer:

**1. Boğazı atlayan çağrı derlemeyi durdurdu.** `cn.QueryAsync` eklendi → RS0030,
build BAŞARISIZ; geri alındı.

**2. Şiddet `WarningsAsErrors` ile VERİLEMİYOR.** İlk kurulumda `<WarningsAsErrors>`
kullanıldı ve **boğazın kendisi derlenemedi**: derleyici düzeyindeki RS0030,
`.editorconfig` dosya muafiyetlerini eziyor. Yani kapı kurulmuş ama geçilecek kapı
kalmamıştı. Şiddet `.editorconfig`e taşındı.

**3. YASAK LİSTESİ SESSİZCE ÖLEBİLİYOR** — en değerli bulgu. Sembol adı yanlış
yazılınca (`T:Dapper.SqlMapperYOK`) analizör **hiçbir şey demiyor**: ban ölüyor,
boğazı atlayan çağrı derleniyor, build yeşil. Yani V-10 bir kapıdır ve **onun da bir
kapısı gerekir**. `tools/vardiya_kapsam_denetimi.py`ye dördüncü denetim eklendi
(liste var mı · beklenen girdi duruyor mu · yorum satırı var mı) ve kırmızı kiple
doğrulandı: bozuk sembol → KIRIK, dosya silinmiş → KIRIK.

> Aynı desenin üçüncü tekrarı: **kapının kendisi de bir varsayımdır.** Türkçe kapısı
> kendi regex'i yüzünden hiçbir şey denetlemiyordu · Solum'un yasak listesi kendi
> biçimi yüzünden depoyu kırdı · bizim yasak listemiz bir harf hatasıyla sessizce
> ölebiliyor. Üçünde de "kapı kurdum" demek yetmiyor, kapının kırmızı verdiğini
> görmek gerekiyor.

**Solum'un yorum tuzağı ölçüldü ve notum düzeltildi:** AYNI yorum satırı iki kez →
RS0031 (derleme kırılır); FARKLI yorum satırları → **sessizce yok sayılır**. İkincisi
daha tehlikeli: dosya açıklamalı görünür ama o satırlar hiçbir şeyi yasaklamaz.

Build 0 hata · test 8/8 · dört kapı yeşil.

### V-11 — boğaz `vardiya-app`e genişletildi; ölçüm varsayımı çürüttü (commit `2c07557`)

V-11 bir soruydu ("genişletilsin mi?") ve cevabı ölçmeden verilebilirdi: *kimlik/ACL
tarafı riski düşük, orada kapsamlı veri yok.* Önce ölçtüm.

**Ölçüm:** 5 Dapper çağrısı / 2 dosya; tablolar `Vrd_Roles · Vrd_UserRoles ·
Vrd_KullaniciSube · Vrd_Sube · SolumPermissionGrant`. Kişi-gün verisi gerçekten yok.

**Ama ölçüm varsayımı çürüttü:** `Seed`in ACL yazması SQL'de `@sube` isterken C#
`branch` gönderiyordu — **aynı kusurun beşinci örneği**. Seed, Türkçe→İngilizce
yeniden adlandırmadan **önce** koşmuştu; kusur koşumdan **sonra** girdi ve bir daha
koşturulmadığı için sessizce kırık kaldı. Yani "riski düşük" değildi, **ölçülmemişti**.

Kurulan yapı:
- `lib/Bkm.Shared/Data/SqlContract.cs` — ortak çekirdek (`Verify` + `SqlParams`).
  `VrdSql` de buna taşındı: iki kopya olsaydı biri bayatlar, bayatlayan taraf sessizce
  koruma bırakırdı.
- `vardiya-app/Security/AuthSql.cs` — kimlik/ACL boğazı.
- `vardiya-app/BannedSymbols.txt` + `.editorconfig` — Dapper `AuthSql` dışında derlenmiyor,
  **tek** muafiyet boğazın kendisi.

Solum'un ön koşulu karşılandı: **yasak var olan boğazı zorunlu kılar, yoktan boğaz
yaratmaz** — önce `AuthSql` yazıldı, ban ondan sonra kondu. Kırmızı kip koşuldu
(boğaz dışı Dapper → RS0030 ×2 → geri alındı).

**Yasak listesinin sessiz ölümüne ikinci katman:** `BanListLivenessTests` her girdiyi
**yansımayla** gerçek bir .NET tipine çözüyor (Solum'un aynı gün ödeyerek öğrendiği
ders — onlarda bir harf hatası korumayı tümden öldürmüştü ve kapı silinmişti).
Python kapısı yalnız metne bakıyor; ikisi ayrı katman: biri ucuz + pre-commit, öteki
kesin + test koşumunda. İki kırmızı kip: bozuk sembol → KIRIK · yorum satırı → KIRIK.

Build 0 hata · **test 11/11** · dört kapı yeşil.

> Beş ad kaymasının dağılımı artık şunu söylüyor: kusur "vardiya sorguları"na özgü
> değil, **anonim nesne kullanan her yere** özgü. `SqlContract` o yüzden ortak
> çekirdeğe çıkarıldı — bir sonraki uygulamada sıfırdan öğrenilmesin.

İki yeni borç doğdu (V-12 ACL yazması denetim izsiz · V-13 seed düzeltmeyle yeniden
koşturulmadı, DB'deki ACL satırları doğrulanmalı).

### V-13 — ACL yazma yolu KOŞTURULARAK doğrulandı (commit `f59b784`)

Borç "seed yeniden koşturulmadı" diye yazılmıştı. Kapatmaya oturunca **borcun kendisi
yanlış tanımlanmıştı**: seed mevcut kullanıcıyı atlıyor (`continue`), yani yeniden
koşmak ACL yazma satırını **çalıştırmıyor bile**. Yeşil bir seed koşumu o satır
hakkında hiçbir şey söylemezdi — ölçüm hiç koşmuyor ama koşmuş *gibi* görünüyordu.

Üç adım:

1. **Mevcut veri ölçüldü** (tahmin edilmedi): panel DB'de **14 ACL satırı / 12
   kullanıcı**, üç şubeli kafe müdürü 3 satır — beklenen kadroyla birebir (11×1 + 1×3).
   Yani beşinci ad kayması mevcut ACL'i **bozmamış**; kusur yeniden adlandırmayla
   koşumdan *sonra* girmişti. "Muhtemelen bozulmuştur" yazıp geçilebilirdi.
2. **Yazma yolu tek metoda çıktı** — `Seed.GrantBranchAsync`, `bkm.Vrd_KullaniciSube`'ye
   yazan tek üretim yolu. Gömülü hâlinde **test edilemiyordu**: testi mümkün kılmak için
   yapıyı değiştirmek gerekti.
3. **`SeedAclWriteTests`** gerçek DB'ye karşı: yazıyor mu · **idempotent** mi (aynı şube
   ikinci kez yeni satır açmamalı; mükerrer satır zamansal ACL'i bozardı) · yazılan satır
   gerçekten o şube mi. Kırmızı kip: `.Add("sube",…)` → `.Add("branch",…)` → test KIRMIZI
   ("SQL'de var, verilmemiş: sube") → geri alındı.

### Asıl sürpriz: test fikstürü bir YARIŞ taşıyormuş

Yeni test eklenince **başka** bir test kırmızı döndü — `Manager_sees_only_own_branch`,
"KPI bulunamadı" diyerek. **Kapsamla ilgisi yoktu.** Sebep: iki test sınıfı ayrı fikstür
örneğinde paralel koşuyor ve fikstürün açılış temizliği `zz_test_` önekli *her şeyi*
siliyor → biri ötekinin kullanıcılarını koşum ortasında siliyor, oturum düşüyor, sayfa
boş dönüyor.

> Yani kapsam testi, **kapsamla ilgisi olmayan bir sebeple** kırmızı veriyordu. Bu bir
> kez daha olsa o testin sinyaline güven düşer ve insan "yine o fikstür işi" deyip
> bakmamaya başlar — testin kendi YAKALAMAZ maddesinde yazılı olan tehlike.

Çözüm `VardiyaDbCollection`: tek fikstür, sıralı koşum. **Reddedilen alternatif:**
temizliği kendi damgasına daraltmak — o zaman çökmüş koşumun artığı kalıcı olurdu
(insanların giriş yaptığı bir DB'de bilinmeyen test kullanıcıları). Geniş temizlik
korundu, paralellik kaldırıldı. Açık kalan (V-14): yeni bir DB testi sınıfı koleksiyona
eklenmeyi unutursa yarış geri gelir ve bunu **hiçbir şey yakalamıyor**.

Test 12/12 · dört kapı yeşil.

### V-14 ve V-12 — konvansiyonu mekanikleştirmek, yetki vermeyi izlemek

**V-14 (commit `50658e7`)** — V-13'te bulunan yarışı koleksiyon fikstürü kapatmıştı ama
çözüm bir **konvansiyondu**: yeni bir DB testi sınıfı `[Collection]` koymayı unutursa
yarış geri gelir ve hiçbir şey uyarmaz; testler çoğu zaman yeşil, **ara sıra ve
açıklanamaz** biçimde kırmızı olur. Sızıntıdan kötüsü budur — güvenilmeyen kapı,
bakılmayan kapıya döner. `FixtureIsolationTests` yansımayla ölçüyor: yapıcısında
`VardiyaAppFactory` isteyen her sınıf koleksiyonda mı? Nüfus sıfırsa **KOŞAMADI** der.
Kırmızı kip: etiket söküldü → sınıfı adıyla yazıp KIRIK verdi.

**V-12 (commit `1dddaf2`)** — onay yazması izliydi, **yetki verme değildi**: "kime hangi
şube verildi, kim verdi" sorusunun cevabı yoktu. `GrantBranchAsync` artık grant + izi
**aynı işlemde** yazıyor (ayrı olsaydı "şube verildi ama izi yok" aralığı doğardı — onay
yazmasındaki gerekçenin aynısı) ve iz **yalnız gerçekten yazıldıysa** düşüyor:
idempotent ikinci çağrı bir olay değildir, izi olayla doldurmak denetimi gürültüye boğar
ve gerçek yetki verme kaybolur. Kırmızı kip: iz yazımı kapatıldı → test KIRIK.

> ⚠ Türkçe tanımlayıcı kapısı **kendi kodumu yakaladı**: iz JSON'una alan adını `Sube`
> yazmıştım, kapı commit'i blokladı → `Branch`/`GrantedBy`/`Added`. Kapı GMY dört kez
> söylediği için kurulmuştu; ilk gerçek faydası kuranına oldu.

Test 13/13 · dört kapı yeşil.

> **Günün altı kapı-dersi tek cümlede:** kapı kurmak yetmiyor — kapının kırmızı verdiğini
> görmek, kapının kendi girdisini denetlemek, kapının nüfusunu ölçmek ve kapının
> konvansiyon değil mekanizma olduğunu kanıtlamak gerekiyor.
