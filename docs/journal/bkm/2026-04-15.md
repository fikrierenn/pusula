# Oturum Günlüğü — 2026-04-15 (bkm) — Session 3 + Session 4 ortası

## Ana Konu
Aynı gün iki paralel iş: (a) ikinci SQL Server bağlantısı (192.168.40.66\SQLEXPRESS) için MCP entry kurulumu, (b) e-ticaret trend raporu çalışmasının devam eden sorguları + Grok cross-check.

## Tamamlananlar

### İkinci SQL Server Bağlantısı (Session 3)
- `192.168.40.66\SQLEXPRESS` ikinci sunucusu için Claude Desktop config'e ikinci MCP entry eklendi: `sqlserver-express`.
- Aynı `D:\Dev\sqlserver-mcp-server\dist\index.js` binary'si iki kez ayrı env vars ile çalıştırılıyor (kod değişmedi).
- `CLAUDE.md` güncellendi: "Veritabanı Bağlantısı" bölümü iki ayrı alt başlıkla yeniden yazıldı.
- `sorgular/SESSION.md` güncellendi: header ve Kritik Hatırlatmalar bölümüne MCP sunucu seçim kuralı eklendi.

## Öğrenilen Kritik Bilgiler

### MCP Multi-Instance
- Aynı MCP binary iki kez register edilebilir — farklı `command`+`env` bloklarıyla. Tek process değil, iki ayrı process olarak çalışıyor.
- Tool prefix kuralı: MCP entry adı = tool prefix.
  - `sqlserver` → `mcp__sqlserver__sql_query`
  - `sqlserver-express` → `mcp__sqlserver-express__sql_query`

### Named Instance Port Problemi
- `SQLEXPRESS` dinamik port kullanır default olarak.
- Çözüm: SQL Server Configuration Manager → Protocols for SQLEXPRESS → TCP/IP → IPAll → TCP Port: 1433 sabitlenir; ya da SQL Browser UDP 1434 açık olmalı.

### Config Dosyası Yolu
- `C:\Users\fikri.eren\AppData\Roaming\Claude\claude_desktop_config.json`
- Restart şart: Config değişikliği sonrası Claude Desktop tray'den Quit + tekrar açma.

## Kararlar

### MCP Sunucu Seçimi
- Varsayılan BKM/ERP/envanter soruları = `sqlserver` (201).
- "Express'te", "66'da", "diğer sunucuda" ifadeleri = `sqlserver-express`.
- Emin olunmayan durumda kullanıcıya sorulacak.

## Açık Konular

- [ ] Express sunucusunun kullanım amacı dokümante edilecek (`CLAUDE.md`'de `[DOLDUR]` alanı duruyor).
- [ ] Express'te hangi DB'ler var, `ALLOWED_DATABASES` daraltılmalı mı karar verilecek.
- [ ] İki sunucu arası cross-server sorgu (linked server / OPENROWSET) ihtiyacı çıkarsa değerlendirilecek.
