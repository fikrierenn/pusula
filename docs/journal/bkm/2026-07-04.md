# Oturum Günlüğü — 2026-07-04

> curator-check: 2026-07-01 (3 gün önce, <7g → hafif yüzey; tam dry-run yok)

## Ana Konu
AFCP (Otonom Finansal Kontrol Düzlemi) stratejik tezi tartışıldı → somut, veri-doğrulanmış bir pilota indirildi: **banka ekstresi satır → cari (cKodKarsi) otomatik sınıflandırıcı**. Bunun için **bağımsız muhasebe uygulaması** kuruldu (`D:\Dev\pusula\muhasebe`, .NET 10 Razor Pages + Dapper + Tailwind, **API YOK**, `D:\Dev\operax` mimarisi baz). DerinSIS'in car→muhasebe boru hattı decrypted SP kaynağından tam çözüldü ve `sema/`'ya işlendi.

## Tamamlananlar

### Strateji + danışman skiller
- `afcp-danisman` skill + `TEZ.md` — AFCP kategori tezi (kontrol düzlemi, mevzuat-as-code, kanıt zinciri, sorumluluk paradoksu → "finansal doğruluğun risk-veri altyapısı" reframe).
- `muhasebe-danisman` skill (`muhasebe/.claude/skills/`) — ürün/kod danışmanı + DerinSIS remap + car→GL boru hattı gerçekleri.

### Bağımsız muhasebe app (`D:\Dev\pusula\muhasebe`)
- Blazor mis-scaffold → **Razor Pages**'e çevrildi (Operax baz, API-yok). Build temiz (0 hata).
- `Program.cs` (Features kök), `Lib/Db.cs` (.env → DerinSISBkm **salt-okuma**), `Lib/BankaSiniflandirmaService.cs` (807K in-memory index, öneri-only), `Features/BankaEkstresi` + `Dashboard` + Tailwind layout.
- **Canlı smoke (gerçek DB, tarayıcı):** motor uçtan uca sınıflandırdı ✅.

### Faz 0 kanıt (scratchpad C# konsol)
- 807.600 geçmiş satır, zaman-bazlı holdout (son 30g). **Saf lexical** (normalize + yön-anahtarlı çoğunluk, embedding YOK).
- Operasyon noktası: **agree≥%97 & n≥5 → AUTO, %98.8 doğruluk, %61 kapsam**.

### Operax mali skiller (kopyalandı + adapte)
- `muhasebe-mevzuat`, `mali-islem-akislari`, `mali-evrak-mevzuat`, `local-llm-integration` → `muhasebe/.claude/skills/`. Operax-nesnesi remap muhasebe-danisman'da.

### DerinSIS car→muhasebe boru hattı (decrypted kaynak — Fikri) → sema'ya işlendi
- `car_ekle` (çift-kayıt, işaret, guard), `car_isle` (açık-kalem cKalan), `mhsEnt_car`/`mhsEntKontrolCar` (GL post), `cariIsle_oto` (zamanlayıcı) çözüldü.
- `sema/bridges.yaml`: `car-karsi-frm`, `frm-mhsent-glhesap`. `sema/codes.yaml`: `car.cFatTip` (carTipF 122). `sema/metrics.yaml`: `banka_ekstresi_siniflandirma` + `car_acik_kalem_ve_gl_zinciri`.
- `plans/27-banka-ekstresi-siniflandirici.md` (Faz 0 kanıt + canlı bulgu + Monday + entegrasyon spec).
- `/learn`: `sql-server-conventions.md` § MCP (OBJECT_DEFINITION NULL ≠ şifreli).

## Build / Test Durumu
- Build: **yeşil** (muhasebe app, 0 hata/uyarı).
- Test: Faz-0 holdout kanıt geçti (%98.8 auto). Canlı browser smoke yapıldı.
- Smoke: yapıldı (preview server, gerçek DB, sınıflandırma çalıştı).

## Commit Durumu
- Uncommitted: 24 (bu oturum yeni app + skiller + sema + plan). Handoff yalnız journal+TODO commit eder.
- Yeni commit: yok (kullanıcı istemedi).

## İşe YARAMAYANLAR
- **`cBA` tek başına perspektif ayracı DEĞİL** — YANIT'ta gerçek taraf cBA=1, BURSA'da cBA=0. Doğru ayraç: **çiftin banka-olmayan tarafı = `cKod` frmTip=5 (banka) olan satırın cKodKarsi'si.**
- **Holdout %92-99 iyimserdi** — ham next-row tahmini iki perspektifi karıştırıyordu; canlı test düzeltti.
- **`OBJECT_DEFINITION('dbo.X')` NULL → "şifreli" sandım** — aslında yanlış şema (`mhs.cariIsle_oto`). Şemadan-bağımsız `sys.sql_modules` teyidi doğru (→ sql-conventions'a yazıldı).
- **"Şule 60/gün elle yazıyor" ilk çıkarımı** — sonra "Excel'den aktarıyor" dendi, timestamp analizi (max 6 evrak/dk) elle-tempo gösterdi. Kesin: Monday Şule'yi izle.

## Kararlar
### Reddedilenler
- ~~Blazor Server~~ → **Razor Pages** (Operax baz, API-yok).
- ~~Embedding/LLM v1~~ → lexical yeter (%98.8). Embedding sadece no-hit (~%8) için F2.
- ~~Ham INSERT ile ERP yazma~~ → **`EXEC car_ekle` sarmalama** (çift-kayıt/işaret/guard'ı DerinSIS yapsın).
- ~~Yeni sema-disiplin skill'i~~ → `sema-ogren` + `consolidate-sema` zaten var (footprint-ladder).

### Seçilenler
- Bağımsız app `muhasebe/` (dashboard'a değil).
- Öğrenme depo = **BkmPanel** (ERP değil, erp-write-policy).
- **F3 yazma yolu (ONAY BEKLİYOR):** `bkm.sp_BankaEkstresiGir` (doğrula+audit+`EXEC car_ekle`) + kısıtlı SQL login (sadece bu SP'ye GRANT) + Şule onayı. → erp-write-policy güncellenecek.

### Kullanıcı feedback verbatim
> "api yok d:\dev\operax mimarisini baz al"
> "iyi incele vardır kalıplra takılma"
> "kendi programımız üstünden yapacağız kendi sp mizi yazarız onu kullanırız ona da yetki veririz"
> "excel den aktarıyor nasıl yapıyor bilmiyorum ama"
> "mcp kullanmaya gerek var mı sqlcli kullansan da olur"

## Dikkat Edilmesi Gerekenler
- **erp-write-policy:** app şu an salt-okuma (öneri-only). ERP'ye yazma YOK; F3'te kendi SP + kısıtlı login onaylanınca kural güncellenir.
- DerinSIS native SP şifreli ama Fikri decrypted kaynak veriyor → gerekince iste.
- Ekstre **oto-import YOK** — Şule (insID 1897) elle, `cKodKarsi`'yi elle seçiyor. Motor tam bu işi otomatikleştirir.

## Parked (context)
- **SEMANTIK_KATMAN.md** insan-özeti senkronlanmadı (sema YAML güncel; küçük borç).
- **MCP vs sqlcli** — gelecek oturum salt-okuma sorgular için sqlcmd/sqlcli tercih edilebilir.
- **Öğrenme tablosu** `PanelBankaOgrenme` (BkmPanel) — kullanıcı "önce tablosuz" dedi; öğrenme döngüsü için gerekli (F3 civarı).

## Yarına Başlangıç Noktası
1. **Şule'yi 5 dk izle** — elle mi giriş, Excel-import mı (before/after snapshot ile footprint teyit; trigger yok).
2. **Ham + işlenmiş Excel** al → motorla üret → gerçekle karşılaştır (gerçek doğruluk).
3. **Perspektif kilidi:** eğitim filtresini `cKod frmTip=5 (banka)` satırlara indir → virman-ayna gürültüsü kalkar, AUTO kapsamı artmalı.
4. **Excel-in/out** (ClosedXML zaten eklendi) + **GL-hesap zenginleştirme** (mhsEntFrm join → öneriye hesap kodu).
5. **F3 yazma yolu kararı** — `bkm.sp_BankaEkstresiGir` sarmalama + kısıtlı login (kullanıcı onayı sonrası).

## Yardımcı Dosyalar
### Yeni
- `muhasebe/` — bağımsız app (Muhasebe.csproj, Program.cs, Lib/Db.cs, Lib/BankaSiniflandirmaService.cs, Features/*, .claude/launch.json).
- `muhasebe/.claude/skills/` — muhasebe-danisman + 4 Operax mali skill.
- `.claude/skills/afcp-danisman/` (SKILL.md + TEZ.md).
- `plans/27-banka-ekstresi-siniflandirici.md`.
- `scratchpad/bankaproof/` — Faz-0 kanıt konsolu (tek seferlik).

### Değişen
- `sema/bridges.yaml`, `sema/codes.yaml`, `sema/metrics.yaml` (banka ekstresi gerçekleri + updated 2026-07-04).
- `.claude/rules/sql-server-conventions.md` (§ MCP — OBJECT_DEFINITION dersi).

---

## Oturum 2 (aynı gün — ilk handoff sonrası; uzun oturum, delta)

### Ana konu
İlk handoff'tan sonra: DerinSIS car→muhasebe boru hattı decrypted çözüldü + sema'ya işlendi; **aktif öğrenme** kuruldu (bkm.BankaOgrenme, canlı+DB doğrulandı); perspektif kilidi doğrulandı+uygulandı; sektör/literatür araştırması; UI (banka dropdown + İstisna cari-arama + Windows auth); F3 ERP-yazma yolu **tasarlandı** (yazılmadı).

### Tamamlananlar
- **Aktif öğrenme ✅** — `bkm.BankaOgrenme` (DerinSIS **bkm şeması**, app-owned; Fikri oluşturdu). Onay/düzelt UI → öğrenilen-index ÖNCELİKLİ (geçmişi ezer) + belleğe anında + kalıcı. Canlı: İstisna→onay→"öğrenildi" AUTO; DB Id=1 (Türkçe nvarchar korundu). Restart sonrası öğrenme geri geldi (DB kalıcılık kanıtlandı). `BankaSiniflandirmaService.OgrenmeKaydet/_ogrenilen/Karar`.
- **DerinSIS car→GL boru hattı decrypted (Fikri kaynak) → sema.** `car_ekle` (çift-kayıt/işaret/guard), `car_isle` (cKalan açık-kalem), `mhsEnt_car`/`mhsEntKontrolCar` (GL post, cBA=0 kanonik), `cariIsle_oto` (zamanlayıcı). /learn: `sql-server-conventions.md` (OBJECT_DEFINITION NULL≠şifreli) + sema bridges (`car-karsi-frm`,`frm-mhsent-glhesap`)/codes (`car.cFatTip`)/metrics (`banka_ekstresi_siniflandirma`,`car_acik_kalem_ve_gl_zinciri`).
- **Perspektif kilidi ✅** — index yalnız `cKod frmTip=5 (banka)` satırlardan (582K/807K). agree≥97%/n≥5 → **%69,6 kapsam · %99,63 doğruluk** (kilitsiz %61/%98,8). Komple Haziran backtest = aynı (stabil). Motora uygulandı, canlı YANIT/BURSA %48-81→%100 AUTO.
- **Operax 4 mali skill kopyalandı** (muhasebe-mevzuat, mali-islem-akislari, mali-evrak-mevzuat, local-llm-integration) → `muhasebe/.claude/skills/`; DerinSIS remap muhasebe-danisman'a.
- **Araştırma (WebSearch):** sektör (Xero/QB/fintech) = yerel katmanlı + düzeltmeden-öğrenme = **bizim yaptığımız, best-practice doğrulandı**. HITL/instance-based literatürde var (Fails&Olsen 2003, Springer 2022). Eksik: tutar sinyali (F1.5), embedding no-hit (F2), L2 skill/kural (HITL rule induction).
- **UI + auth ✅** — Banka hesabı dropdown (cKod seçimi, son-12-ay aktif **286→119**), İstisna cari-arama (`OnGetCariAra` JSON handler + min-JS), Windows auth (Negotiate; dev'de açık/prod'da FallbackPolicy; Kim=domain-user).
- **F3 tasarım (YAZILMADI):** `bkm.sp_BankaEkstresiGir` taslak (car_ekle sarmalar; param haritası 4-satır örnekten kesin: cTip=cFatTip=122, cOnay=1, cDvzID=1, çift-kayıt car_ekle'nin) + test protokol.

### İşe YARAMAYANLAR (Oturum 2)
- **GL-mapping boşluk raporu fikri** → veri çürüttü (305 karşı-cari %100 eşli, boşluk 0).
- **Postalanmamış backlog fikri** → sadece 186/%3 (%97 temiz), zayıf.
- **drn1.insKullanici/insParola ile login** → insKullanici HERKESTE BOŞ (0/2002); 24 kişide 7-char kod (POS/terminal, login değil). DerinSIS = **Windows auth**.
- **`OBJECT_DEFINITION('dbo.X')` NULL → "şifreli" sanmak** → mhs.cariIsle_oto mhs şemasındaydı (şema hatası). car_ekle/mhsEnt_car gerçekten WITH ENCRYPTION.

### Kararlar
- ~~BkmPanel (localhost Express)~~ → **`bkm.BankaOgrenme` (DerinSIS bkm şema)** — kullanıcı "bkm şeması olarak". erp-write-policy güncellendi (app-owned bkm.* yazılabilir).
- ~~drn1 auth~~ → **Windows auth** (DerinSIS'le aynı).
- **cKod (banka hesabı) = SEÇİM (dropdown), tahmin değil** (ekstre tek-hesap, %100 doğru).
- **L2 skill/kural** = HITL rule induction (motor önerir, Şule onaylar) — düzeltme birikince.
- **F3 test protokol:** Şule 1 satır girer → yakala → biz aynısını SP ile → karşılaştır → yanlışsa car_sil.

### Kullanıcı feedback verbatim
> "kendi programımız üstünden yapacağız kendi sp mizi yazarız onu kullanırız ona da yetki veririz"
> "win aut derinsis"
> "derinsiteki ... bkm şeması olarak" (öğrenme tablosu)
> "bir satırı şule girer bakarız test ederiz sonra aynı satırı biz gireriz" (F3 test)

### Dikkat / Parked
- **SQL keşif arşivi borcu:** bu oturum çok MCP keşif yaptı (aging, manuel-tip, GL-gap, drn1, cTip/cFatTip) → `sorgular/2026-07-04-*.sql` arşivlenmedi. Sema güncel; SQL arşiv eksik.
- **SEMANTIK_KATMAN.md** insan-özeti hâlâ senkronsuz (sema YAML canonical).
- **F3 açık kararlar:** kişi insID (panel/robot mu 1897 mi), cEvrakNo (=cID auto?), cGdrMerkez=0 teyit — Monday testinde netleşir.
- **erp-write-policy:** native car yazımı Monday F3-test ile açılacak (SP-only, insan-onaylı). Şu an sadece bkm.* yazılabilir.

### Yarına Başlangıç Noktası (ANCHOR — budanmaz)
1. **F3 test protokol** — Şule 1 satır girer → `SELECT car WHERE cID IN(o,cBag)` yakala → `bkm.sp_BankaEkstresiGir` (Fikri oluşturur) aynı değerlerle → karşılaştır → yanlışsa car_sil. insID/evrakNo/gdrMerkez netleştir. `bkm.BankaYazimAudit` tablosu ver.
2. **Excel-in** — Şule ham Haziran ekstre Excel → ClosedXML oku + kolon eşle (tarih/tutar/açıklama/yön) + IBAN→banka hesabı auto-seç → `ClassifyBatchAsync` → çıktı.
3. **L2 skill/kural katmanı** (düzeltme birikince) + tutar sinyali (F1.5) + embedding no-hit (F2).
4. **SQL keşif arşivle** (`sorgular/2026-07-04-*.sql`) — twin-obligation borcu.

### Yeni/Değişen (Oturum 2)
- Yeni: `muhasebe/Features/BankaEkstresi/*` (dropdown+arama+öğret UI), Windows-auth (Program.cs+Negotiate paketi). `bkm.BankaOgrenme` tablosu (Fikri).
- Değişen: `Lib/BankaSiniflandirmaService.cs` (öğrenme+perspektif+banka-liste+cari-ara), `Lib/Db.cs`, `plans/27`, `sema/*.yaml`, `.claude/rules/{erp-write-policy,sql-server-conventions}.md`, `muhasebe/.claude/skills/muhasebe-danisman`.

> curator-check: 2026-07-01 (3 gün, <7g → hafif yüzey; tam dry-run yok)
