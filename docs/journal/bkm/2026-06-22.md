# Oturum Gunlugu — 2026-06-22

## Ana Konu
Dashboard hızlı kazanım kartları — Operasyon sayfasına 4 yeni kart eklendi (B-112 3/4 + B-111). Tamamen pusula/dashboard tarafı (FIFO'ya dokunulmadı). Sonunda TODO tam-tarama + B-111/B-112 dup düzeltme.

## Tamamlananlar

### B-112 — Operasyon hızlı kazanımlar 3/4 (commit 6df2a39)
- (a) **Ödeme Grubu + önceki-dönem Δ** — `RefQueries.Operasyon.cs:GetOdemeGrupAsync` (SalesPayments, IsChangeAmount=0, para üstü hariç). Nakit payı↑ → nakit-stres uyarısı.
- (b) **İade Sebebi** — `GetIadeSebepAsync` (RefundReasons.Type=0, DocType=3 iade kalemleri). Canlı: Müşteri Memnuniyetsizliği 21.981 kalem hakim.
- (c) **İndirim Kaynağı** — `GetIndirimKaynakAsync` (SPC.Source 0/1/2). Canlı 2026: Kampanya 77,7M / Manuel 8,9M / Legacy 6,2M.
- (d) kasa saatleri = **zaten vardı** (Operasyon "Saat Bazlı Yoğunluk") → atlandı.
- Yeni partial `dashboard/Data/RefQueries.Operasyon.cs` (RefQueries.cs 621 satır kırmızı-çizgi → split disiplini) + 3 record `RefModels.cs`. Dönem-duyarlı, arka planda yükle.

### B-111 — WMS bekleyen doluluk kartı (commit 5884ac6)
- TEMİZ yol: `EticQueries.GetBekleyenDurumAsync` — J_ORDERS aşama split (SENDDATE NULL): 1000=toplanma bekleyen (raflanmayı bekleyen) / 3001,3003,3004=hazırlanan / 3006=temin bekleyen.
- DerinSIS `depo.emirAyr` KİRLİ (18.06 keşfi) → kullanılmadı.
- `BekleyenDurum` record + AppRankBars kart, Operasyon WMS satırında, arka plan (JOKER linked).
- Canlı: toplanma **2** / hazırlanan **4.283** / temin **6.185** (toplam 10.470) → WMS picking güncel, yığılma tedarik tarafında.

### TODO tam-tarama + dedup (commit 95ffc0f)
- 453 satır tarandı. Bulgu: B-111/B-112 her biri 2 yerde (SABAH curated 159/160 + Faz-2 canonical 345/346). SABAH kopyaları gün içinde kapatıldı ama Faz-2 asılları açık kalmıştı → done-but-open dup. 345→[x], 346→[~] senkronlandı.

## Build / Test Durumu
- .NET build: **yeşil** (0 uyarı / 0 hata) — `GmDashboard.csproj`.
- Sorgular: hepsi MCP'de canlı doğrulandı (iade-sebep Type=0, SPC.Source, J_ORDERS bekleyen split).
- Smoke/render: **yapılamadı** — login duvarı (preview ayrı oturum). Kullanıcı kendi tarayıcısında 4 kartı gördü, "buldum tamam" onayladı.

## Commit Durumu
- Uncommitted: 6 (oturum öncesinden: sql-server-conventions.md, .agents/, .codex/, AGENTS.md, plans/23, sorgular/wms-kesif — hiçbiri bu oturuma ait değil, dokunulmadı).
- Yeni commit'ler: 6df2a39 (B-112), 5884ac6 (B-111), 95ffc0f (TODO dedup).

## Yarim Kalan / Yarin'a
### Üst öncelik
- **B-112 kalan 1/4:** (d) kasa saatleri zaten kapsanmış → B-112 fiilen bitik sayılabilir; istenirse gün×saat heatmap olarak Operasyon'a ayrı eklenebilir (düşük değer).
- **FIFO commit** (19.06'dan devreden, D:\Dev\fifo 12 uncommitted) — kullanıcı onayında, 201 kapalı.
- **B-114 plan-23 Faz-2/3** (dashboard maliyet sayfası) — FIFO/201'e bağlı, beklemede.

## İşe YARAMAYANLAR
- Razor `@{ var ... }` markup-içi (else bloğu table'dan sonra) → RZ1010 "Unexpected { after @". Çözüm: değişkeni else'in baş C# bölgesinde tanımla.
- Tuple array'i AppRankBars `Items="@(new[]{(Ad:\"...\")})"` inline → çift-tırnak attribute ile çakışır. Çözüm: diziyi else C# bölgesinde local `var` olarak tanımla, `Items="@bekBars"`.
- Preview ile render doğrulama → login duvarı + yeni şifre bilinmiyor → otomatik giriş yok. Auth/config credential-tarama da classifier'ca bloklandı (doğru davranış).

## Kararlar
- B-111 TEMİZ yol = J_ORDERS e-tic split (emirAyr kirli, 18.06 kararı korundu).
- Yeni sorgular `RefQueries.Operasyon.cs` partial'a (file-size kırmızı-çizgi disiplini), Models.cs'e değil yığma.
- KPI carousel'e bekleyen rozeti eklenmedi (re-init karmaşası) — kart yeterli.

### Kullanıcı feedback verbatim
> "hızlı olanları yaplım" · "şifreyi yine unuttum :)" · "bekleyen doluluk nerde" (işgücü satırında gömülü → bulundu) · "todo kontrol et tam tara".

## Dikkat Edilmesi Gerekenler
- ⚠️ Panel şifre **sıfırlandı** bu oturum (kullanıcı `DELETE FROM BkmPanel.dbo.PanelKullanici` → /setup ile yeni belirledi). Yeni şifre bende yok.
- ⚠️ Bekleyen kartı işgücü/depo satırında gömülü — kullanıcı zor buldu. İstenirse üst KPI şeridine taşınabilir.
- ⚠️ Render hiç doğrulanmadı (login-gated) — kullanıcı görsel onay verdi ama otomatik smoke yok.

## Yarina Baslangic Noktasi
1. İstenirse B-110 (tedarikçi/yayınevi performans paneli, Tier-3, sıradaki büyük dashboard işi) ya da B-112 (d) heatmap.
2. FIFO tarafı: commit + sızan gerçek-gider curation + prod cutover (201 açılınca) — 19.06 handoff'tan devam.
3. B-114 dashboard maliyet sayfası (FIFO/201 bağımlı).

## Yardımcı Dosyalar
### Yeni
- `dashboard/Data/RefQueries.Operasyon.cs` — B-112 3 sorgu (ödeme grubu / iade sebebi / indirim kaynağı), partial RefQueries.
### Değişen
- `dashboard/Data/EticQueries.cs` — `GetBekleyenDurumAsync` (B-111).
- `dashboard/Models/RefModels.cs` — OdemeGrupRow/IadeSebepRow/IndirimKaynakRow.
- `dashboard/Models/PeriodModels.cs` — BekleyenDurum record.
- `dashboard/Components/Pages/Operasyon.razor` — 4 kart + tarih hesabı hoist + EticQueries inject.
- `TODO.md` — B-111 [x] / B-112 [~], dup senkron.

> curator-check: 2026-06-17 (5g < 7g — hafif yüzey: yeni keşif SQL'i yok, sema'da çelişki yok; tam dry-run gerekmedi)
