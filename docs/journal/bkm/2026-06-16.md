# Oturum Günlüğü — 2026-06-16

## Ana Konu
E-ticaret veri katmanı linked-server'dan (ODAKJOKER) doğrudan JOKER (192.168.40.70) bağlantısına taşındı + kategori LOGOGRUP'tan + ürün drill + yıl trendi grafiği. Yanında: kampanya brüt spc fan-out fix, telefon SignalR auto-recovery, CSS düzeltme, mağaza detay GetDetay paralel perf (B-49), mağaza kartı `<a href>` SSR-güvenli nav (B-48). 8 iş, 6 commit. Sonunda Genel Bakış redesign için karar alındı (kart birleştirme + 2 yeni sayfa + tahmin detayı) ve Context7 MCP kuruldu.

## Tamamlananlar

### E-ticaret direkt JOKER (B-74 kalan) — commit b792f4e
- `Db.OpenJokerAsync()` (dashboard/Data/Db.cs) — `.env` `JOKER_HOST=192.168.40.70` ikinci bağlantı (user/pass MSSQL_* ile aynı sa, Fikri teyit).
- EticQueries.cs 10/10 sorgu `ODAKJOKER.JOKER.dbo.*` linked yerine `dbo.*` DIREKT (.70). Linked distributed-query overhead kalktı.
- Kategori: `urn`/`urnKtgr2` join yerine **`J_ITEMS.DERINSIS_LOGOGRUP`** (= DerinSIS Kategori3 grain). Birebir aynı rakam, **0.6s vs linked 4.1s** (7×).
- Kategori → ürün drill modalı: `GetEticKategoriUrunAsync` (top 30, sipariş/adet/net ciro). AppRankBars OnRowClick.
- Kategori yıl trendi: `GetEticKategoriYilAsync` (kategori × ay, mevcut yıl) + yeni `AppStackedBarChart.razor` (ApexCharts Stacked, top 7 + Diğer).
- İl Dağılımı: 3 görünür + native `<details>` akordiyon (kalan iller).
- Modeller: `EticKategoriUrunRow`, `EticKategoriAyRow` (RefModels.cs).

### Kampanya brüt spc fan-out fix — commit 609f3dd (MagazaQueries.cs)
- `SalesProductCampaigns` bir ürün satırına N indirim satırı üretiyor (3al2öde 52 adet = 26 çift = 52 spc). `SUM(sp.TotalPrice)` join fan-out'unda N× sayılıyordu → kampanya brüt/net şişiyordu (~%3.8 agg, çok-adetli satırda 52×).
- Fix: indirim spc'den (doğru), net satır-başı 1× DISTINCT CTE'den ayrıldı. İndirim ve "yük %" zaten doğruydu; brüt+net düzeldi.

### Mağaza detay paralel (B-49 ikinci tur) — commit 609f3dd (MagazaQueries.cs)
- 14.06'da sayfa-seviyesi `Task.WhenAll` yapılmıştı; ama `GetDetayAsync` İÇİNDE 7 sorgu hâlâ SIRALI tek bağlantıdaydı (kpi/upt/odeme/kamp/kampFis/kat/hedef) = ~3.4s.
- Her sorgu kendi bağlantısı + `Task.WhenAll` (lokal `Q` helper). **~3.4s → ~1.2s** (max tek sorgu: kat 1.2s / odeme 1.1s). Rakamlar birebir aynı (aynı sorgular, sadece eşzamanlı).

### Telefon circuit auto-recovery — commit 689e348
- Sunucu restart sonrası mobil "An unhandled error" barında takılıyordu (SignalR devresi ölü, default sadece visibilitychange'de retry).
- `/healthz` endpoint (Program.cs) + App.razor poll (`window.__bkmRecover` — sunucu dönünce `location.reload()`) + error-ui MutationObserver + ReconnectModal `failed` branch çağrısı.

### B-48 mağaza kartı nav `<a href>` — commit 8928ddc (Home.razor)
- Mağaza kartları + Tahmin kartı + "incele" butonu: `@onclick NavigateTo` → gerçek `<a href>`. SignalR/interaktivite olmadan da çalışır (mobil "açılmıyor" kökü). StoreRow `Action onClick` → `string href`.
- Modal'lar (TOPLAM/E-tic/Detaylı analiz kategori) interaktivite-bağlı kaldı → B-47 cert'e bağımlı, ayrı.

### Diğer
- CSS stilsiz görünme: çoklu manuel exe-restart fingerprint manifest'i bayat bıraktı; temiz `dotnet run` (preview) çözdü. Kod değişikliği yok.
- `.gitignore`: `briefings/olu-stok*.xlsx` (commit ec5e302).
- `.mcp.json` (untracked) — Context7 MCP HTTP (`https://mcp.context7.com/mcp`). Endpoint 200 doğrulandı. Aktif olması için oturum restart + onay gerekir.

## Build / Test Durumu
- Build: yeşil (her iş sonrası, 0 hata/uyarı).
- Smoke test: yapıldı (preview_start + preview_eval/screenshot her özellik için — /eticaret kategori+drill+yıl trendi+il akordiyon, /magaza/4477 paralel, / kart anchors). Tüm bölümler render, console hata yok.

## Commit Durumu
- Bugün 6 commit (ec5e302 → 5d718c5).
- Uncommitted: 1 (`.mcp.json` untracked — kasıtlı, context7 config).

## Yarım Kalan / Yarına

### Üst öncelik — Genel Bakış REDESIGN (Tier-3, kararlar onaylı, AskUserQuestion ile)
1. **Kart birleştirme**: TOPLAM + Mağaza Toplam + E-tic + tek mağazalar **hepsi `<a href>` sayfa-nav** (modal yok). E-tic → /eticaret (var).
2. **Yeni `/toplam`** sayfası: 3 mağaza + online birleşik kategori detayı (mevcut `_modalTot` içeriği sayfaya).
3. **Yeni `/magazalar`** sayfası: 3 mağaza yan yana karşılaştırma. "Mağaza Toplam" kartı buraya nav.
4. **/tahmin zenginleştir** + Home tahmin kartı ÖZET (bu-ay projeksiyon + güven; "hiç detay görmüyorum"). Sayfada hesap adımları (YoY taban, MoM ivme, kalan gün) + mağaza/kategori tablo.
- **Yöntem (kullanıcı isteği):** agent + skill kullan. Explore → harita, planner (opus) → `plans/NN-genel-bakis-redesign.md`, dashboard-icerik kalıbı, context7 = ApexCharts/DaisyUI API, sonra code-reviewer + silent-failure-hunter.

### Diğer bekleyen
- **B-47** mobil cert/WSS (Cloudflare tunnel — 2× reddedildi). B-48 nav'ları kısmen kurtardı ama modal/drill hâlâ SignalR-bağlı.
- **B-74** kalan: Sadakat RfmGecis ağır, Stockout CROSS APPLY pre-agg.
- sema kaydı (henüz yapılmadı): JOKER direkt köprü, LOGOGRUP=Kategori3, spc fan-out gotcha → `sema-ogren`.

## İşe YARAMAYANLAR
- `J_ITEMS.DERINSIS_ID → DERINSIS linked → bkm.UrunBilgi.Kategori3` (cross-server join .70'ten) → gereksiz; LOGOGRUP zaten metin kolon olarak Kategori3'ü taşıyor. Linked hop'a gerek yok.
- `J_ITEMS.ANAKATEGORI` / `WEB_ANAKATEGORI` → Kategori3 DEĞİL (daha ince: "Edebiyat Kitapları" vs "Kitap"). Kategori3 = LOGOGRUP.
- `bkm.UrunBilgi.Kat3` kolonu → kitapta BOŞ; doğru kolon `Kategori3` (dolu). `Kat3` ≠ `Kategori3`.
- charts.js'e `stackedBar` (Chart.js) eklemek → proje ApexCharts kullanıyor (charts.js sadece heroDots). Geri alındı, ApexCharts component yapıldı.
- Manuel `GmDashboard.exe` restart (ASPNETCORE_URLS ile) → fingerprint manifest bayatladı, CSS stilsiz. Doğrusu: preview_start (dotnet run) temiz fingerprint.

## Kararlar

### Reddedilenler
- ~~Kategori .201'de linked kalsın (cross-server)~~ → kullanıcı "kategori de oradan olmalı" → LOGOGRUP ile tam .70 direkt.
- ~~Chart.js stackedBar~~ → ApexCharts (proje standardı, Blazor-ApexCharts 6.1.0).
- ~~Superpowers plugin~~ → projede zaten güçlü `.claude/` çatısı var, TDD bu UI+SQL'e ağır. **Context7 seçildi** (canlı sürüm-özel API dökümanı — ApexCharts/DaisyUI/Dapper için değerli).

### Redesign kararları (AskUserQuestion)
- Kart yapısı = **hepsi sayfa-nav `<a href>`** (mobil-güvenli, B-48 ile tutarlı).
- Mağaza Toplam = **3 mağaza birleşik → karşılaştırma sayfası**.
- Tahmin = **Home kartında özet + /tahmin'de tam kırılım**.

### Kullanıcı feedback verbatim
> "kampanya yükündeki rakamlar genel ciro ile örtüşmüyor bir yerde bir sıkıntı var" — spc fan-out tetikledi.
> "linked olmasına gerek yok direk jokere bağlanabilirsin" — JOKER direkt yönü.
> "1 cross-server gerek yok items içinde ilgili alanlar var" → "jıtems logo group olacak" — LOGOGRUP köprüsü.
> "tahmin bu şekilde olmamalı hiç bi detay görmüyorum ben sen çatır çutur hesaplıyorsun ama" — tahmin detayı redesign tetiği.
> "redesing için agent ve skill kullanman daha mantıklı değil mi" — agent/skill akışı zorunlu.
> "ekranda açıkken sunucu kapanırsa bir daha telefonda gelmiyor" — recovery tetiği.

## Dikkat Edilmesi Gerekenler
- Her yeni SQL/model öncesi ŞEMA DOĞRULA (Fact-Force Gate). Bu oturum LOGOGRUP/Kategori3/UrunBilgi kolonları canlı doğrulandı.
- JOKER direkt bağlantı sa şifresi = MSSQL_PASSWORD (aynı). `.env` JOKER_* gitignore'lu.
- `.mcp.json` commit edilmedi (untracked) — context7 için, restart+onay sonrası aktif.
- Redesign Tier-3: plan ONAYLANMADAN kod yazma (plan-first). Önce planner agent.

## Yarına Başlangıç Noktası
1. Context7'yi onayla (oturum restart) → aktif mi test.
2. Explore agent → Home kart/modal/component/routing + veri akışı haritası.
3. planner agent (opus) → `plans/NN-genel-bakis-redesign.md` (4 madde: kart birleştirme, /toplam, /magazalar, /tahmin).
4. Plan onayı → dashboard-icerik kalıbıyla implement (context7 API).
5. (paralel) sema-ogren: JOKER direkt köprü + LOGOGRUP=Kategori3 + spc fan-out.

## Yardımcı Dosyalar

### Yeni
- `dashboard/Components/Shared/AppStackedBarChart.razor` — ApexCharts yığılmış sütun (kategori × dönem), `Series`/`XValue`/`YValue`.
- `.mcp.json` — Context7 MCP (untracked).

### Değişen
- `dashboard/Data/Db.cs` — `OpenJokerAsync()` (.70 direkt).
- `dashboard/Data/EticQueries.cs` — 10 sorgu direkt JOKER + LOGOGRUP kategori + ürün drill + yıl trendi.
- `dashboard/Data/MagazaQueries.cs` — GetDetay 7 sorgu paralel + kampanya CTE (spc fan-out).
- `dashboard/Components/Pages/Eticaret.razor` — ürün drill modalı + yıl trendi kartı + il akordiyon.
- `dashboard/Components/Pages/Home.razor` — kart `<a href>` nav.
- `dashboard/Components/App.razor` + `Layout/ReconnectModal.razor.js` + `Program.cs` — telefon recovery.
- `dashboard/Models/RefModels.cs` — EticKategoriUrunRow, EticKategoriAyRow.
- `.gitignore` — olu-stok xlsx.

---

## Oturum 2 — Genel Bakış Redesign (B-75, plan 11) — Tier-3

### Ana Konu
Genel Bakış (Home) modal-tabanlı drill'i SignalR'sız sayfa-nav'a taşındı. Explore→harita, planner(opus)→`plans/11-genel-bakis-redesign.md`, AskUserQuestion ile 3 karar netleşti, P11-1…P11-8 uygulandı. Sıfır yeni SQL.

### Tamamlananlar
- **`plans/11-genel-bakis-redesign.md`** — tam Tier-3 plan (drill çözümü 3 alternatif → Alt C query-param SSR seçildi; sıfır yeni query doğrulandı).
- **Kararlar (AskUserQuestion):** (1) Mağazalar başlığı 2 buton (Karşılaştır→/magazalar, Kategoriler→/toplam); (2) /toplam **V1 mağaza-only** (online kategori ertelendi — yeni e-tic SQL gerektirir); (3) sidebar'a ekle.
- **Yeni `/toplam`** ([Toplam.razor](dashboard/Components/Pages/Toplam.razor)) — 3 mağaza birleşik kategori (`_modalTot` `SelectMany→GroupBy→Sum` birebir) + kategori→ürün drill `<a href="toplam?d=&kat=">` `[SupplyParameterFromQuery]` server-render. Dönem+kategori ikisi query param → drill href dönemi korur.
- **Yeni `/magazalar`** ([Magazalar.razor](dashboard/Components/Pages/Magazalar.razor)) — 3 mağaza yan yana (net/WoW/fiş/sepet/iade/hedef), her kart `<a href="magaza/{id}">`. WoW penceresi Home'la birebir.
- **Home** ([Home.razor](dashboard/Components/Pages/Home.razor)) — 3 modal (`_modalTot`/`_modalEtic`/`_modalUrun`) + OpenUrun + orphan helper'lar (UrunStok/CoverCell/StokCell/Cover/OrtGun) + `@inject RefQueries` kaldırıldı. Başlık 2 buton, e-tic kartı `<a href="eticaret">`, "Detaylı analiz"→/toplam.
- **`/tahmin`** ([Tahmin.razor](dashboard/Components/Pages/Tahmin.razor)) — Hesap Adımları bloğu (1: YoY taban → 2: ivme → =: tahmin + MTD/geçen-kalan gün/pace).
- **Sidebar** ([MainLayout.razor](dashboard/Components/Layout/MainLayout.razor)) — Mağazalar + TOPLAM Kategori linkleri.

### Build / Test
- Build yeşil (0 hata/uyarı), `npm run build:css` derlendi.
- Smoke (preview 5112): /toplam?d=ay&kat=Oyuncak → 100 ürün + drill ✓; /magazalar → 3 kart doğru href ✓; Home → modaller gitti, 2 nav+etic href ✓; /tahmin → hesap adımları (geçen 16/kalan 14 gün, 30.968.080×1.659=51,37M) ✓. Console temiz.
- **Mutabakat (P11-2):** /toplam kategori toplamı 22.456.725 vs Fiziksel 22.456.348 → fark 377 ₺ (%0,0017), 16 kategori görüntü-yuvarlaması. Eski modal mantığıyla birebir → regresyon değil.

### Commit Durumu
- **Uncommitted** — Toplam.razor, Magazalar.razor (yeni) + Home/Tahmin/MainLayout/TODO/plan/journal + app.css. Commit kullanıcı onayı bekliyor.

### Yarım Kalan / Yarına
- **Commit** — plan 11 işi (8 dosya) commit edilmedi.
- **B-76 sema** — JOKER köprü + LOGOGRUP + spc fan-out (hâlâ açık).
- **/toplam online kategori** (V2) — ertelendi, yeni e-tic kategori SQL (J_ITEMS→DERINSIS_ID→urnKtgr2).
- **Mağaza detay modalleri** (/magaza/{id}) hâlâ SignalR-bağlı (kapsam dışı, B-47).

### Dikkat
- Dönem pills mobilde (SignalR yok) çalışmaz → varsayılan gunluk; drill href dönemi taşıdığından masaüstü interaktif dönem korunur. /toplam,/magazalar ozel dönem desteklemez (`ShowOzel=false`, guard).
- silent-failure-hunter razor değil py hedefli → manuel kontrol: yeni catch blokları `_error` set ediyor (UI'da "Veri alınamadı" görünür, sessiz değil).

---

## Oturum 3 — B-74 Envanter perf (paralel + StreamRendering)

### Ana Konu
B-74 kalan "RfmGecis ağır + Stockout pre-agg" hipotezleri canlı ölçümle test edildi → ikisi de çürük. GERÇEK darboğaz: Envanter sayfası 13.4s. İki kök neden bulundu + çözüldü.

### Bulgular (canlı ölçüm — todo-verification)
- **RfmGecis 494ms** (MCP) — "ağır" hipotezi yanlış, dokunulmadı.
- **Stockout pre-agg** (CROSS APPLY → tek GROUP BY) test edildi: rakam birebir ama **1092ms vs 1021ms — kazanç yok**. CROSS APPLY index-seek zaten verimli → dokunulmadı (churn engellendi).
- **GetInventoryAsync 5 sorgu tek bağlantıda SIRALI** (~3.4s katkı). **GetMarjAsync ~9s `await` ile prerender'ı blokluyor** (satır 224 "arka plan" yorumu SSR'da çökmüş).

### Tamamlananlar
- **GetInventoryAsync paralel** ([RefQueries.cs:57](dashboard/Data/RefQueries.cs#L57)) — lokal `Q` helper (her sorgu kendi bağlantısı) + `Task.WhenAll` (B-49 deseni). SQL'ler birebir aynı. WhenAll grubu **1.1s** (PerfState ile doğrulandı).
- **`[StreamRendering]`** ([Envanter.razor:8](dashboard/Components/Pages/Envanter.razor)) — marj prerender'ı bloklamasın. Sayfa WhenAll'la ~1.2s'te chunked-stream'lenir, marj sonradan eklenir (mobil HTTP'de de, SignalR'sız).

### Build / Test
- Build yeşil. Smoke (preview 5112): Envanter algılanan yükleme **13.4s → 1.2s** (warm, ilk içerik=11 kategori), marj stream sonu 9.4s'te. Console temiz.
- Veri bütünlüğü: stockout 11 kategori, MCP baseline ile birebir (Cesit/Yok/Pct aynı).

### Commit Durumu
- Uncommitted: RefQueries.cs + Envanter.razor + TODO + journal. Commit bekliyor.

### Karar / Ders
- TODO hipotezi ≠ gerçek. "RfmGecis ağır / Stockout yavaş" yazılıydı ama ölçüm ikisini de çürüttü; asıl darboğaz ölçülünce (sayfa-seviyesi 13.4s) bulundu = GetInventory sıralı + GetMarj blocking. **Önce ölç, sonra optimize et** (B-49'la aynı ders).
- `[StreamRendering]` = ağır ikincil kart için doğru desen (SignalR'sız mobilde de progressive paint). Diğer ağır sayfalara da uygulanabilir (Sadakat 2.2s — şimdilik kabul).

---

## Oturum 4 — Hermes adaptasyonu (plan-12 tam) + sistem denetimi + kolay düzeltmeler

curator-check: 2026-06-16

### Ana Konu
Üç eksen: (1) **Plan-11 (B-75) Genel Bakış redesign** tamamlandı + commit (önceki oturumda kod yazılmıştı, bu oturumda 85f65be commit). (2) **NousResearch/hermes-agent derin analizi → plan-12 Hermes adaptasyonu TAM** (7/7 WS): sema/TODO yaşam-döngüsü, narrow-waist, NavRegistry, ErrorClass, agent-rol, handoff-anchor, davranışsal-test. (3) **4-ajan + Context7 sistem denetimi** → bulgular B-77..B-95 TODO'ya, sonra kolay düzeltmeler (B-87 ölü kod, B-85 catch-log, B-88 perf). 21 commit.

### Tamamlananlar

**Plan-11 B-75 redesign — commit 85f65be** (kod önceki oturum, bu oturum commit + smoke)
- /toplam (3 mağaza birleşik kategori + `?d=&kat=` SignalR'sız drill), /magazalar (3 mağaza karşılaştırma), Home modaller→`<a href>`, /tahmin hesap adımları. Mutabakat Fiziksel=kategori %0,0017. Türkçe/boşluklu kategori (`Çocuk Kitabı`) encode Context7-doğrulandı.

**Plan-12 Hermes adaptasyonu — 7/7 WS** (`plans/archive/12-hermes-adaptasyon.md`)
- WS-1 (78c9060): sema `last_verified`/`ttl_days` decay (confidence:1.0 MUAF) + `semantic-layer.md` decay kuralı + sema-ogren format + handoff Adım 4.6 curator-check + `consolidate-sema` skill (dry-run) + TODO state-machine + `## Arşiv`.
- WS-4+6 (86d903e): `agent-usage.md` §7 leaf/orchestrator + max_concurrent=3 + derinlik≤2; handoff iteratif-merge/anchor/anti-thrash.
- WS-2 (72d3f62): `footprint-ladder.md` (yeni) + 7 rule on-demand etiketi + asistan-ui/dashboard-icerik skill referans + CLAUDE.md tier notu. **Compact-survival korundu (paths: yok).**
- WS-5 (1f40896): `SqlErrorClassifier.cs` (transient/fatal, 18456 auth-fatal) + `Db.cs` OpenWithRetryAsync (max-2 backoff loglu) + `scripts/_errors.py` + generate_brief.py bağlandı + error-handling.md. silent-failure-hunter auth-fatal bulgusu düzeltildi.
- WS-3 (e7a5d50): `dashboard/Models/NavRegistry.cs` → MainLayout sidebar+btm-nav türer (B-75 orphan kökü). Razor `section` rezerve→`grp`.
- WS-7 (0da5bda): test-discipline davranışsal-kontrat (anti-snapshot).

**Sistem denetimi (4-ajan opus/sonnet + Context7)** → B-77..B-95 TODO (688ec1b). Güvenlik TEMİZ. En kritik: B-81 iade-netleme (sema'da zaten DÜZELTME BEKLİYOR).

**Kolay düzeltmeler (kolaydan-zora):**
- B-87 (0e70314): ölü kod Operasyon `_pColor`+blok, Gorevler `OncSinif()`.
- B-85 (f47c067): RefQueries SPLH + Envanter marj → ILogger.LogWarning; Operasyon ölü iç-try kaldırıldı.
- B-88 (31cabca): Home GetHedefAsync WhenAll'a; Müşteri GetRfmAsync yk+et paralel (B-74 deseni).

### Build / Test
- Build: yeşil (her kod-WS sonrası). Python compile OK.
- Smoke: /toplam drill 100 ürün, /magazalar 3 kart, NavRegistry sidebar 11+btm 5, ErrorClass Home 200, B-88 Home 927ms/Müşteri 200. Console temiz.
- silent-failure-hunter: WS-5 auth-fatal yanlışsınıflama bulundu+düzeltildi.

### Commit Durumu
- Bugün toplam 21 commit (ec5e302→31cabca). Bu oturum (Oturum 4) ~16 commit.
- Uncommitted: 1 (`.mcp.json` untracked — context7, kasıtlı).

### Yarım Kalan / Yarına (ANCHOR — budanmaz)

**Sistem denetimi kalan düzeltmeler (kolaydan-zora, TODO B-8x/9x):**
- **B-89** hardcode-hex → ApexCharts CSS-var (Context7: `colors:['var(--p)']` çalışır; ilk denetimin "API kısıtı" sonucu YANLIŞ). AppAreaChart/AppBarChart default `#4063e6` + charts.js PAL. **Grafik görsel-verify gerekir.**
- **B-83** exception sızıntısı `_error=ex.Message` UI (tüm sayfa) → generic + log.
- **B-82** kasiyer net `ABS(DiscountTotal)` tutarsızlık (Queries.cs:148-160,333-341) — **MCP mutabakat** gerekir.
- **B-86** GetDepoWmsAsync boş→0 (0 vs veri-yok) — düşük değer.
- **B-90** dosya split: Home.razor 535 (>500 kırmızı), RefQueries 524, Eticaret 411, Queries 395, Magaza 377.
- **B-84** auth YOK (finansal+PII) — mimari karar gerek.
- **B-91** prerender double-render → PersistentComponentState (tüm sayfa masaüstü 2× DB).
- **B-81 🔴 EN KRİTİK — iade-netleme (~%0,9 gerçek para):** irsHrk `ehTip(4,100)` iade `(101,5,3)` düşmüyor — RefQueries devir/marka/cve/rotasyon/marj + EncoreMerkez ReturnAmount (Queries katSql/skatSql, Magaza katSql, GetUrunlerAsync). sema `metrics.yaml` zaten DÜZELTME BEKLİYOR. **Her sorgu MCP eski/yeni mutabakatlı, temiz context'te yapılmalı.**

**Plan-12 faz-2 ertelenenler:** B-92 /toplam online kategori, B-93 rule fiziksel taşıma (topic/ dizin), B-94 sema telemetri, B-95 sema last_verified geriye-doldurma.

### İşe YARAMAYANLAR
- Razor `@foreach (var section ...)` → `section` REZERVE directive (`@section`), RZ2005 hata. Loop değişkeni `grp` yapıldı.
- Bash `@'...'@` PowerShell here-string → bash'te geçersiz, commit mesajı başına `@` sızdı (85f65be amend ile düzeltildi). Sonraki commit'ler `cat > cmsg.txt <<'EOF'` ile.
- Resume sonrası dosya-okuma durumu sıfırlanıyor → Edit "File has not been read" → önce Read şart (toplu rule etiketlemede yaşandı).

### Kararlar
- **Hermes reddedilenler:** multi-provider/gateway/profiles/plugin/cron-daemon/MCP-catalog → BKM tek-kullanıcı/tek-makine, narrow-waist (planda §5 gerekçeli).
- **4 scope kararı (AskUserQuestion):** WS-2 etiketleme-only (fiziksel taşıma B-93) · telemetri ertele (B-94) · consolidate-sema yaz · WS-5 2-3 script.
- **send_mail.py SMTP=DB değil** → ErrorClass kapsamı dışı.
- **B-74 hipotez çürütüldü:** RfmGecis 494ms / Stockout pre-agg kazançsız → dokunulmadı (todo-verification).

### Kullanıcı feedback verbatim
> "tüm sistemi kontrol ettirmekte fayda var" — 4-ajan denetim tetiği.
> "belki var olan yapılarıda daha da güçlü yapabiliriz o gözle de baktın mı" — sertleştirme ekseni.
> "tamamını planlayıp adapte edelim" — plan-12 yön onayı.
> "ama sonra ideal hale getireceksin değil mi ... unutma atlama" — B-81..B-95 TODO'ya kalıcı kayıt (context-survival).
> "kolaydan zora doğru" — düzeltme sırası.

### Dikkat Edilmesi Gerekenler
- **B-81 iade-netleme gerçek para** — sessiz-yanlış-rakam riski en yüksek; temiz context + MCP mutabakat şart, aceleye gelmez.
- `.mcp.json` hâlâ untracked (context7).
- Yeni `ErrorClass` retry mevcut CommandTimeout=240 ile birleşik (üstüne yığılmadı).
- consolidate-sema + curator-check ilk kez aktif — ilk gerçek kullanım handoff'larda.

### Yarına Başlangıç Noktası (ANCHOR — budanmaz)
1. **Kolay tier devam:** B-89 hardcode-hex (ApexCharts CSS-var, grafik visual-verify) + B-83 exception-leak (generic+log).
2. **Orta:** B-82 kasiyer-ABS (MCP mutabakat) + B-90 dosya-split (Home 535).
3. **Zor (taze context):** B-84 auth (mimari) + B-91 prerender (PersistentComponentState).
4. **EN KRİTİK son:** B-81 iade-netleme — tek tek sorgu, her biri MCP eski/yeni mutabakatlı. metrics.yaml encore_irshrk_mutabakat + net_ciro formülü referans.
5. Faz-2 ertelenenler (B-92..B-95) düşük öncelik.

### Yardımcı Dosyalar
**Yeni:** `plans/archive/12-hermes-adaptasyon.md`, `.claude/rules/footprint-ladder.md`, `.claude/skills/consolidate-sema/SKILL.md`, `dashboard/Data/SqlErrorClassifier.cs`, `dashboard/Models/NavRegistry.cs`, `scripts/_errors.py`.
**Değişen (kilit):** `sema/README.md`+`bridges.yaml` (decay), `semantic-layer.md`/`agent-usage.md`/`error-handling.md`/`test-discipline.md`/7 rule etiket, `session-handoff/SKILL.md` (Adım 4.6+anchor), `CLAUDE.md` (tier notu), `Db.cs` (retry), `MainLayout.razor` (NavRegistry), `generate_brief.py`, Home/Operasyon/Gorevler/Envanter/RefQueries (B-85/87/88).

## Compact Snapshot --- 14:26

### Son 5 Commit

- d54a45c feat(bkm): Genel Bakış redesign — Hero nav + Tahmin özet + Toplam filtre + Mağazalar bar + Kat tahmin (plan: 13)
- cf6df1a docs: 2026-06-16 Oturum 4 handoff + TODO sync — plan-12 Hermes tam + denetim + B-85/87/88
- 31cabca perf(bkm): Home hedef + Müşteri RFM paralel (B-88)
- f47c067 fix(bkm): sessiz catch'lere log + ölü iç-try kaldır (B-85)
- 0e70314 refactor(bkm): ölü kod sil — Operasyon._pColor + Gorevler.OncSinif (B-87)

### Uncommitted Dosya Sayısı

- 3 uncommitted dosya

### Aktif Planlar

- 02-fsm-trafik-analiz.md
- 03-brief-otomasyon.md
- 04-gunluk-kar-zarar-maliyet-karsilastirma.md
- 05-envanter-verim-kpi.md
- 06-mcp-server-ayiklama.md
- 07-dashboard-react.md
- 08-apexcharts-migration.md
- 09-dashboard-rafine.md
- 10-dashboard-icerik-genisleme.md
- 13-genel-bakis-redesign.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **B-47 ⚡ DRILL MOBİL FIX** — mkcert güvensiz cert (Android kırmızı X) → WSS/SignalR kurulmuyor → mobil HTTPS'te interaktivite/drill YOK (sayfa SSR açılır, tıklama ölü). Kanıt: HTTP'den (http://192.168.1.61:5112) drill çalışmalı. **Çözüm: Cloudflare tunnel** (gerçek cert → WSS+PWA install+standalone+drill hepsi). Kullanıcı tüneli 2× reddetti ama mkcert Android'de yetmiyor. VEYA iç sunucu+Let's Encrypt. **(YENİ 14.06)**
- - [ ] **B-81 🔴 KRİTİK iade-netleme (~%0,9 ciro şişik)** — irsHrk sorguları `ehTip IN (4,100)` alıp iade `(101,5,3)` DÜŞMÜYOR: `RefQueries.cs` devir(84-88)/marka(115)/cve(312-317)/rotasyon(505-508)/marj(478) + EncoreMerkez `ReturnAmount` netleme `Queries.cs` katSql/skatSql + `MagazaQueries.cs` katSql + `RefQueries.GetUrunlerAsync`. sema `metrics.yaml net_ciro`/`encore_irshrk_mutabakat` zaten "DÜZELTME BEKLİYOR". **Her sorgu MCP eski/yeni mutabakatlı** düzeltme — dedike iş, aceleye gelmez. (En yüksek değer.)
- - [ ] **B-82** ORTA — Kasiyer net `ABS(DiscountTotal)` formülü diğer sorgulardan farklı (Queries.cs:148-160, 333-341), çift-düzeltme riski → doğrula.
- - [ ] **B-83** ORTA — exception sızıntısı `_error = ex.Message` UI'da (tüm sayfalar) → generic mesaj + detay logger'a.
- - [ ] **B-84** ORTA — auth YOK (finansal + müşteri PII ağa açık). İç LAN/PWA bağlamı kabul ama karar gerek (basit auth?).
- - [ ] **B-86** ORTA — `GetDepoWmsAsync` boş-sonuç→0 "Depo Bugün" (RefQueries:399-410): gerçek-0 vs veri-yok ayırt edilemez.
- - [ ] **B-89** DÜŞÜK — hardcode hex → ApexCharts CSS-var (**Context7 doğruladı: `colors:['var(--p)']` çalışıyor**, ilk denetimin "API kısıtı" sonucu YANLIŞ): `AppAreaChart`/`AppBarChart` default `#4063e6`, axis renkleri, `charts.js` PAL → `var(--p)/--er/--su`.
- - [ ] **B-90** DÜŞÜK — dosya boyutu: `Home.razor` 535 (>500 kırmızı çizgi), `RefQueries.cs` 524, `Eticaret.razor` 411, `Queries.cs` 395, `Magaza.razor` 377 → split.
- - [ ] **B-91** Blazor prerender **double-render** (Context7) — prerender'lı InteractiveServer'da OnInitializedAsync 2× → masaüstünde TÜM sayfa SQL'i çift koşuyor (mobilde tek). `PersistentComponentState` ile persist→restore. Tüm sayfalar. DB yükü 2×→1×.
- - [ ] **B-92** /toplam **online (e-ticaret) kategori** V2 — B-75'te V1 mağaza-only yapıldı; online kategori için yeni e-tic SQL (J_ORDER_DETAILS→J_ITEMS→DERINSIS_ID→urnKtgr2 veya LOGOGRUP) gerekir + ayrı doğrulama.
- - [ ] **B-93** plan-12 WS-2 **faz-2** — konu-bazlı rule'ları fiziksel `.claude/rules/topic/` dizine taşı + skill-inject (gerçek system-prompt token düşüşü). **compact-survival smoke ZORUNLU** geçmeden yapma.
- - [ ] **B-94** plan-12 WS-1 **telemetri** — sema/skill kullanım sayacı (`.usage.json` sidecar). Şimdilik manuel yargı; veri-temelli stale tespiti istenirse.
- - [ ] **B-95** sema **last_verified geriye-doldurma** — şu an sadece encore-kampanya-kalem örnek aldı. Diğer <1.0 kayıtlar (bekleyen-siparis-il 0.9, hedef-kategori 0.95, salescampaign-sales 0.95, items-derinsis kontrol) dokunuldukça `last_verified` kazanmalı (toplu değil, footprint-ladder).
- - [ ] **B-65** Müşteri kayıp/risk segmenti 3-ay trendi → **Faz 3 /sadakat** (B-67 segment geçiş matrisi ile birlikte). Veri: ykSql 3× (t/t-30/t-60).
- - [ ] **B-66** Kohort retention matrisi — aylık edinim kohortu × N-ay-sonra geri dönüş oranı (heatmap). "Ocak'ta gelen müşterinin %X'i 3. ay hâlâ alıyor". En güçlü sadakat metriği. **(yüksek, L)**


---

## Oturum 5 — plan 13→17 + tahmin motoru + KDV-hariç + fiş drill (devasa)

> curator-check: 2026-06-16 (bu oturumda tekrar yapılmadı — sabah yapıldı)

### Ana Konu
Tek oturumda plan-13'ten plan-17'ye: Genel Bakış redesign, tahmin ay-seçimi/kayıt/takvim, **Python tahmin motoru + öğrenen katman**, **tüm dashboard KDV+kargo-hariç (28 sorgu)**, RFM iç-kart işaretleme, **müşteri→fiş→fiş-içeriği termal-fiş drill**, 2 skill + 1 agent, müşteri kazanım/kart istatistik.

### Tamamlananlar
- **plan-13** Genel Bakış redesign ✅ (hero nav, tahmin özet, toplam filtre, mağazalar bar, kat tahmin) — `git mv archive`
- **plan-14** tahmin ay-seçimi + kaydet/MAPE + takvim etmenleri ✅ (çarpan KAPALI başlar)
- **plan-15** Python tahmin motoru ✅ — `scripts/forecast/` 6 modül: data/calendar_reg(gömülü TR tatil 2021-27)/models(6 aday)/backtest(rolling-origin)/learn(AEC ensemble+de-bias)/run. Heuristik MAPE %7,3 en iyi, ensemble %7,5. `ForecastOkuService` + dashboard panelleri. `run_forecast.bat` + schtasks BKM-TahminMotoru 10:00.
- **plan-16** KDV+kargo-hariç birleştirme ✅ — 28 sorgu (EncoreMerkez `GrossTotal-DiscountTotal-VatTotal`, e-tic direkt JOKER `SELLINGPRICEWITHOUTVAT`). Headline -%7,1/-%9,2. sema 14.06 KDV-dahil kararı SÜPERSEDED.
- **plan-17** Python eksik portlar: WP-1 fiş drill+termal-fiş ✅ (`RefQueries.GetFislerAsync/GetFisIcerikAsync`, müşteri→fiş→içerik 3-katman modal, kampanya adı + manuel etiket), WP-3 saat fiş+ciro ✅, WP-4 AppSozluk sözlük ✅.
- **iç-kart elle işaretleme** ✅ — `IcKartService` (data/ic-kartlar.json), RFM/sadakat NOT IN filtre, tıkla→çıkar/geri-al. Uçtan uca doğrulandı.
- **2 skill + 1 agent** ✅ — `sema-sorgu` (NL→SQL), `forecast-yorum` (motor JSON), `sql-denetci` agent (91 SQL tara).
- **müşteri kazanım + kart oranı stat** — build yeşil, RUNTIME SMOKE BEKLİYOR.

### Build / Test Durumu
- Build: yeşil (son: müşteri stat 0 hata).
- Smoke: plan 13/14/15/16 + fiş drill canlı doğrulandı. **müşteri stat smoke EDİLMEDİ.**

### Commit Durumu
- ~30 commit (plan 13-17). Uncommitted: 2 (`.mcp.json`, `scripts/_kumbara_scan.py` — ikisi de izlenmiyor).

### Yarım Kalan / Yarına
#### Üst öncelik
1. **müşteri kazanım/kart stat RUNTIME SMOKE** (commit cf9e7ab, build yeşil ama çalışmadı). /musteri aç → kazanım grafiği + kart oranı render + AppSozluk aç/kapa doğrula.
2. **plan-17 WP-2 lokasyon filtresi** — `GetInventoryAsync` ~10 noktada `ehMekan IN (1,4477,4478)` hardcoded; 5-lokasyon (FSM/Özl/İst/Depo12/ODAK) toggle için parametreleştir + "stok=şube+Depo, ODAK hariç" (Python urunler_query_sql). Dedike iş.
3. **Kumbara/manuel indirim sebebi** — `scripts/_kumbara_scan.py` arka planda koştu (sonuç görülmedi). KEŞİF: manuel indirim sebebi EncoreMerkez şemasında saklanmıyor (SPC Source=1 boş, BasketDiscounts boş, PriceChangeReasonId=0). Kumbara=müşteri kartı (DerinCrm.Customer.Name), indirim değil.

### İşe YARAMAYANLAR / Dersler
- **_pColor yapıya bakmadan geri-eklendi** → B-87'de ölü kod diye silinmişti; AppAreaChart zaten default renk (#4063e6) veriyor → gereksiz. DERS: component default'larını + silme geçmişini kontrol et, körü körüne field ekleme. (Kullanıcı haklı uyardı.)
- **Manuel indirim sebebi şemada yok** → SPC Source=1 (CampaignName boş, CampaignId NULL), BasketDiscounts tablosu BOŞ, PriceChangeReasonId hep 0, Description boş. Sebep POS'ta seçiliyor ama EncoreMerkez'e yazılmıyor.
- **statsforecast Windows** WinError 6 (AV/roaming kilit) → kullanıcı global Python312'ye kurdu.
- **Synthetic el.click() Blazor'da güvenilmez** → `dispatchEvent(new MouseEvent('click',{bubbles:true}))` gerekti.
- **location.assign /musteri Home'a düşüyor** bazen → nav link click daha güvenilir.

### Kararlar
- **KDV+kargo-hariç** (CFO direktifi) — 14.06 "ana ciro KDV-dahil DOKUNMA" İPTAL.
- Tahmin motoru **Python** (C# değil — Prophet/statsforecast yok). GLM saf-numpy log-ölçek (enflasyon).
- Çarpan **kapalı başlar** (MAPE validasyon sonrası aç).
- Manuel indirim → "Manuel indirim" etiketi (gerçek sebep veride yok).

### Kullanıcı feedback verbatim
> "her şey kdvsiz olmalı" / "kargo da olmasın" — KDV+kargo-hariç direktifi (plan-16).
> "paldır küldür yapıyorsun yapıya bile bakmadan" — _pColor körü-körüne eklemesi eleştirisi. DERS: yapıyı incele, acele etme.
> "3'ü dışındaki her şey" — plan-17 kapsam (FSM dönüşüm #3 hariç).

### Dikkat
- müşteri stat commit'li ama SMOKE EDİLMEDİ — ilk iş onu doğrula.
- `scripts/_kumbara_scan.py` izlenmiyor + arka plan koşumu yarım — gerekirse temizle.
- AppAreaChart Color paramı opsiyonel (default var) — _pColor gerekmez.

### Yarına Başlangıç Noktası
1. `dotnet run` → /musteri smoke: kazanım grafiği + kart oranı + AppSozluk toggle.
2. plan-17 WP-2 (lokasyon filtresi) veya B-81 (iade-netleme, en yüksek değer).
