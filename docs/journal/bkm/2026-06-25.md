# Oturum Günlüğü — 2026-06-25

## Ana Konu
İki iş: (1) "Dashboard hata veriyor" — autostart **eski Release DLL** çalıştırıyordu (envanter adet değişikliği canlıda yoktu); Release rebuild + Task restart ile çözüldü. (2) Yevmiye fiş detay sayfasına (`/yevmiye-fis`) **audit bilgisi** (kim/ne zaman: giren/değiştiren/onaylayan) + her satıra **üst hesap adı** eklendi; eski boş imza kutusu kaldırıldı.

## Tamamlananlar

### Dashboard autostart — stale Release fix
- Kök sebep: `scripts/start_dashboard.ps1` **Release** DLL çalıştırıyor (`bin\Release\net10.0\GmDashboard.dll`) ama kod değişiklikleri yalnız Debug derlenmişti. Çalışan kopya 18.06 17:16 tarihli Release'di → envanter adet (RefQueries.Envanter.cs) canlıda yoktu.
- İlk belirti `dotnet run` → `address already in use :5112` (autostart kopya zaten ayakta). Asıl sorun port değil, **eski binary**.
- Çözüm: Task durdur → çalışan `dotnet GmDashboard` öldür → `dotnet build -c Release` → `Start-ScheduledTask "BKM-Dashboard-Sunucu"` → HTTP 200 doğrulandı.

### Yevmiye fiş audit + üst hesap (B-117 drill UI)
- `Models/MuhasebeModels.cs`: `YevmiyeFisSatir`'a `UstAd` eklendi; `YevmiyeFis`'e audit alanları (`Giren/GirisT/Degistiren/DegisT/Onaylayan/OnayT`, opsiyonel param).
- `Data/MuhasebeQueries.cs` `GetYevmiyeFisAsync`: sorguya 3× `drn1` join (gKisi/kKisi/oKisi → `insAd+insSoyad`) + üst hesap self-join (`hspKod` prefix) eklendi. Audit header-level, `rows[0]`'dan okunuyor.
- `Components/Pages/Yevmiye.razor`: boş imza kutusu (Düzenleyen/Kontrol/Onaylayan) **kaldırıldı** → yerine Giren / Son Değiştiren / Onaylayan + tarih-saat. Hesap adı altına `↳ üst hesap` satırı.
- Build Debug+Release yeşil, canlı (Task restart, HTTP 200). İçerik doğrulaması login-arkası — SQL MCP'de doğrulandı.

## Keşif (sema borcu — henüz yazılmadı)
- **Fiş audit köprüsü:** `mhs.mhsFisBaslik.gKisi/kKisi/oKisi (int) → dbo.drn1.insID`; tarih `gTarih/kTarih/oTarih` (smalldatetime). Kişi adı = `insAd + ' ' + insSoyad`. (g=giriş/oluşturan, k=kayıt/son değiştiren, o=onay.)
- **Üst hesap:** `mhs.mhsHsp` parent = `hspKod` bir üst kırılım (`740.30.002` → `740.30`), self-join prefix ile. `hspAnaID` (smallint) = TDHP grup no (740/760), hspID DEĞİL — parent satırı yok, prefix kullan.

## Build / Test Durumu
- Build: yeşil (Debug + Release).
- Test: çalıştırılmadı (UI değişikliği, otomatik test yok).
- Smoke test: HTTP 200 (login-arkası içerik kullanıcı doğrulayacak); SQL MCP'de doğrulandı (isim + üst hesap çözülüyor, fiş 15466371).

## Commit Durumu
- Uncommitted: 6 (bu oturum: `Yevmiye.razor`, `MuhasebeQueries.cs`, `MuhasebeModels.cs`; önceki: `.gitignore`, `app.css`, `scripts/make-dashboard-cert.sh`).
- Bugün yeni commit: yok (kullanıcı commit istemedi).

## Yarım Kalan / Yarın'a
### Üst öncelik
- **Sema borcu:** fiş audit köprüsü (`mhsFisBaslik.*Kisi → drn1.insID`) + üst hesap (`mhsHsp` prefix parent) `sema/bridges.yaml`/`entities.yaml`'a yazılacak. (B-117 (c) zaten "keşfi sema'ya yaz" borcu — bununla birleşir.)
- Yevmiye fiş sayfası login-arkası görsel doğrulama (audit kutusu + üst hesap satırı render).
- Uncommitted işler kullanıcı onayıyla commit'lenecek (Yevmiye audit+üst hesap = `feat(bkm): yevmiye fiş detayı kim/ne zaman + üst hesap`).

## İşe YARAMAYANLAR
- Çalışan dashboard'ı statik `Invoke-WebRequest` ile içerik doğrulama → panel auth nedeniyle hep **login HTML** (len 1597) döner; render edilen veriyi göstermez. Doğrulama login-arkası veya SQL MCP ile yapılmalı.
- `dotnet build -c Release` çalışan instance DLL'i kilitlerken → `MSB3027 file locked by .NET Host`. Önce process öldür, sonra build.

## Kararlar
- Üst hesap = `hspKod` prefix parent (bir üst kırılım), `hspAnaID` DEĞİL (o sadece TDHP grup no, parent satırı yok).
- İmza kutusu (statik 3 boş başlık) kaldırıldı — yerine gerçek audit verisi (kim/ne zaman) konuldu.

### Kullanıcı feedback verbatim
> "eski versiyon sanırım envanter raporuna adet eklemiştik çalışan sürümde yok" — stale Release teşhisi
> "muhasebe fiş detayında kim düzeltti ne zaman düzeltti gibi bilgileri de ekler misin" — audit talebi
> "hesap adı açıklama kısmına üst hesabı yazalım" — üst hesap talebi
> "değişiklik kısmını imza kısmını kaldırıp ekle ... ayrıca üst hesap adını da ekle" — imza→audit yer değişimi

## Dikkat Edilmesi Gerekenler
- **Dashboard kod değişikliği canlıya yansımıyorsa:** autostart **Release** çalıştırır. Debug build yetmez → `dotnet build -c Release` + `Stop/Start-ScheduledTask "BKM-Dashboard-Sunucu"` (çalışan dotnet'i öldür, DLL kilidi). İleride start_dashboard.ps1'e "her başlangıçta rebuild" eklenebilir (kullanıcıya soruldu, bekliyor).
- Bu fişte gKisi=kKisi=oKisi (pinar.muhasebe2, hepsi aynı an) — giren=değiştiren=onaylayan; farklı kişi düzeltince ayrışır.
- `drn1.insAd` bazı kayıtlarda kullanıcı-adı formatı (`pinar.muhasebe2`) — soyad boş, olduğu gibi gösterilir.

## Yarına Başlangıç Noktası
1. Sema yaz: fiş audit köprüsü + üst hesap (sema-ogren) — B-117 (c) borcuyla birlikte.
2. Yevmiye audit+üst hesap değişikliğini commit'le (kullanıcı onayıyla).
3. start_dashboard.ps1 "her başlangıçta Release rebuild" eklensin mi — kullanıcı kararı.

## Yardımcı Dosyalar
### Değişen
- `dashboard/Models/MuhasebeModels.cs` — YevmiyeFisSatir.UstAd + YevmiyeFis audit alanları.
- `dashboard/Data/MuhasebeQueries.cs` — GetYevmiyeFisAsync: drn1×3 + üst hesap join.
- `dashboard/Components/Pages/Yevmiye.razor` — imza kutusu → audit kutusu, üst hesap satırı.

> curator-check: 2026-06-23 (2g < 7g — hafif: bu oturum sema güncellenmedi, fiş audit + üst hesap köprüsü sema borcu olarak işaretlendi; tam dry-run gerekmedi)

---

## Oturum 2 — Dashboard genişleme: ODAK paneli + Cari/Risk + Excel bileşeni + Baskısı Yok + tray (20+ commit, 15:22–23:31)

### Ana Konu
Tek uzun oturum, ~9 yeni özellik. (1) Dashboard başlatıcıya **sistem tepsisi (tray) ikonu** + "Yeniden Derle ve Başlat". (2) Yevmiye/Fatura **audit + üst hesap + çift-yön köprü** tamamlandı. (3) **Haftalık muhasebe denetim** zamanlı görevi (kuruldu+tetiklendi, mail gitti). (4) **ODAK Stok paneli** (B-123, 3 sekme: marka özet / ürün dökümü / usulsüz sipariş + cari→fatura→yevmiye drill). (5) **Cari/Risk Özeti** finans sayfası (B-124). (6) **Genel Excel export bileşeni** (B-125, reusable). (7) **Baskısı Yok** e-ticaret raporu (B-126) + 30g trend grafiği.

### Tamamlananlar
#### Tray ikonu (commit e9c2cb3, 5772013)
- `scripts/start_dashboard.ps1` → NotifyIcon: çift-tık paneli açar, sağ-tık menü (Paneli Aç / Yeniden Başlat / **Yeniden Derle ve Başlat** / Durdur ve Çık). Restart-loop timer'a taşındı, Application.Run mesaj döngüsü. İkon wwwroot/icon-192.png. **UTF-8 BOM zorunlu** (PS 5.1 Türkçe menü).
- "Yeniden Derle ve Başlat" → kod değişince tek tıkla Release derle+restart (eski-sürüm derdine kalıcı çözüm; logon'u yavaşlatmaz; derleme patlarsa eski sürüm + uyarı).

#### Yevmiye/Fatura audit + köprü (d1e9464, e62ed27)
- Fatura sayfası audit kutusu (giren/değiştiren/onaylayan = fat.gKisi/kKisi/oKisi → drn1) + **çift-yön köprü** `fat.eMhsFisID = mhsFisBaslik.fisbID` (fatura↔yevmiye gezinme). Muhasebe Kontrol detay listesine alt TOPLAM.
- Keşif: aynı fatura no'ya 2 yevmiye = aslında 2 ayrı fatura (eNo "25225839" vs "25225839." nokta farkı); eMhsFisID temiz eşliyor. Alan-bazlı değişiklik log'u YOK (sadece g/k/o damgası; "düzeltildi mi"=kTarih>gTarih).

#### Haftalık muhasebe denetim zamanlı görev (b030604)
- `scripts/haftalik_muhasebe_denetim.py` (SP salt-okuma + kural-bazlı Türkçe yorum → HTML) + `send_muhasebe_denetim.bat` + `register-muhasebe-denetim-task.ps1`. **Görev BKM-Muhasebe-Denetim Pazartesi 08:00 KURULDU + tetiklendi → mail gitti (sonuç 0).**
- **Düzeltme:** bulut/headless LAN DB'ye (192.168.40.201) ULAŞAMAZ → zamanlı işler YEREL Task Scheduler olmalı.

#### ODAK Stok paneli — B-123 (9cb40fa + 7 düzeltme/iyileştirme)
- `/odak-stok`, OdakQueries + OdakModels + Sayfalama bileşeni. 3 sekme: Marka/Yayınevi Özeti · Ürün Dökümü (server OFFSET/FETCH 50/sayfa, e-tic dahil, marka/yazar filtre, StkID→stok-hareket drill) · Usulsüz Sipariş (ODAK stoğu ≥6ay/satışsız + ≥2 cariden alım; cari→fatura→/fatura drill).
- "ODAK stok"=`ent.odak_depo_Stok`, alış kaynağı `fat eTip=0 → eFirma`. Veri tarih aralığı notu. StkID kolonu. Drill'ler **yeni sekmede** (geri-tuş reset çözümü).

#### Cari/Risk Özeti — B-124 (4e17abc, e6eef09, 1e2727b)
- `/cari-risk`, FinansQueries. `bakiyeVDGG_vw + frm`: borç/alacak/net bakiye + vadesi geçmiş (GecenB) + yaklaşan (GelecekB) + limit aşımı. Filtre Satıcı(320)/Alıcı(120)/Hepsi + arama (arama varken TOP 5000). **frm.frmTip → dbo.frmTipTnm lookup** (hardcode kaldırıldı; Tip 8=Hizmet) → sema/codes.yaml.

#### Genel Excel export — B-125 (e7d81cb)
- **Reusable mimari:** `ExcelButton` bileşeni + `ExcelExport.Olustur` (MiniExcel, satır-sözlüğü) + `bkmDownload` JS (base64→Blob). Her grid tek satır: `<ExcelButton Rows="..." />`. Cari/Risk + ODAK 3 sekme (+ Baskısı Yok) eklendi.

#### Baskısı Yok e-ticaret raporu — B-126 (72d7a1f, bb7daa9, 3cec5c0, 9d09c11)
- `/baskisi-yok`, JOKER `BASKISIYOK × J_ORDER_DETAILS × J_ITEMS × EM_USERS` (direkt OpenJokerAsync). **Gün bazlı** özet (çeşit/sipariş/miktar/tutar+TOPLAM) + ürün detayı + Excel. Tarih aralığı (Bugün/Son 7g/Bu ay). E-ticaret sayfasına **30 gün trend grafiği** (çeşit+adet, 2 alan grafiği). Kullanıcının çalışan SQL'inden uyarlandı.

### Build / Test Durumu
- Build: yeşil (her commit Release derlendi + Task restart, HTTP 200 doğrulandı).
- Smoke: HTTP 200 her deploy; içerik login-arkası kullanıcı doğruladı ("aktif", "geldi mail").
- SQL: Cari/Risk + ODAK parçaları MCP'de doğrulandı (CTE'ler MCP wrapper'da çalışmaz, parça parça). BASKISIYOK MCP'de doğrulanamadı (ODAKJOKER linked down) → kullanıcı canlı doğruladı.

### Commit Durumu
- Bu oturum (06-25): ~22 commit (e9c2cb3 … 9d09c11). Uncommitted: 3 (`.gitignore`, `raporlar/`, `scripts/make-dashboard-cert.sh` — önceki, dokunulmadı).

### İşe YARAMAYANLAR
- **Razor attribute içinde iç içe çift tırnak** `@($"...{x}")` → CS1513 parse hatası (2 kez: FaturaAc onclick, ExcelButton FileName). Çözüm: helper metot/property (`FaturaAc`, `MarkaDosya`/`UrunDosya`).
- **Dapper positional record tip eşleşmesi:** SQL `decimal`/`tinyint` → record `int` mismatch (stokSonAltDepo_vw.stok SUM, mhsFisBaslik.fisbSirketID). Çözüm: `CAST(... AS int)`.
- **Sayfa sınıfı ↔ model isim çakışması:** `CariRisk.razor` (component) vs `CariRisk` (record) → record `CariRiskRow` yapıldı.
- **Razor edit'te `</div>` yutma** → markup dengesi bozuldu (PageSize "used before declared" + CS1513). Header düzenlerken kapanış div sayısını koru.
- **Blazor InteractiveServer geri-tuş** → sayfa sıfırdan kurulur (state kaybı). Drill = `<a target="_blank">` (NavigateTo değil).
- **TipAd hardcode** (0=Satıcı...) yanlıştı → kullanıcı "doğru dosyayı bul" dedi → `dbo.frmTipTnm` kanonik.
- **MCP CTE:** `bakiyeVDGG_vw` ~8s ağır; MCP auto-TOP wrapper CTE'yi (`WITH`) bozar → tam CTE sorgu MCP'de test edilemez, parça parça doğrula.

### Kararlar
- Zamanlı agent işleri **YEREL** (LAN DB) — cloud değil.
- Excel = tek genel `ExcelButton` bileşeni (kullanıcı direktifi: "her yerde aynısını kullan, mimari böyle olmalı").
- Cari Tip = `frmTipTnm` lookup (hardcode YASAK).
- ODAK/finans drill'leri yeni sekmede (geri-tuş koruması).

### Kullanıcı feedback verbatim
> "genel bir excel export bileşeni yapıp direk gridi veriyorsun değil mi heryerde aynısnı kullanıyorsun tüm mimari böyle olmalı aslında"
> "Tip 8 ne demek bu eşleşmeyi neyle yapıyorsun hardcode mu doğru dosyayı bulman lazım"
> "stok harekete gidince geri tuşuna basınca herşeyi sıfırlıyor"
> "ilk 200 müşteri geliyor ... hepsi deyip arama yapınca eksik oluyor"
> "marka bazlı olanı o sayfadan ayırmalısın altta ürün bazlı olan kullanışlı olmuyor"
> "özette kullanıcı bazında değil gün bazında yap"

### Dikkat Edilmesi Gerekenler
- **BASKISIYOK tablosu sema'ya yazılmadı** (ODAKJOKER MCP down, doğrulanamadı) — JOKER ayağa kalkınca entity+kolon teyidi + sema kaydı borç.
- ODAK kod yorumlarında "B-122" yazıldı ama TODO'da B-123 (B-122 = SP perf, çakışma — kozmetik).
- bakiyeVDGG_vw ~8s — Cari/Risk açılışı yavaş (spinner var). İleride materialized table düşünülebilir.
- Drill'lerde NavigateTo yerine `<a target="_blank">` kullan (geri-tuş reset).

### Yarına Başlangıç Noktası
1. **Sema borçları:** (a) BASKISIYOK entity (JOKER ayağa kalkınca), (b) `fat.eMhsFisID→fisbID` köprüsü + üst hesap prefix bridges.yaml'a (B-117 c kalanı).
2. İstenen diğer finans raporları hazır bekliyor: Ödeme Takvimi (`carOdemePlan_vw`), Çek/Senet (`cek_vw`), Vadesi Geçen yaşlandırma kovaları.
3. ODAK kod yorumu B-122→B-123 düzeltme (kozmetik, opsiyonel).
4. Excel export'u diğer mevcut tablolara da ekle (envanter/mizan) — aynı `ExcelButton`.

### Yardımcı Dosyalar — Oturum 2
#### Yeni
- `dashboard/Data/OdakQueries.cs` · `dashboard/Models/OdakModels.cs` · `dashboard/Components/Pages/OdakStok.razor`
- `dashboard/Data/FinansQueries.cs` · `dashboard/Components/Pages/CariRisk.razor`
- `dashboard/Data/ExcelExport.cs` · `dashboard/Components/Shared/ExcelButton.razor` · `dashboard/Components/Shared/Sayfalama.razor`
- `dashboard/Components/Pages/BaskisiYok.razor`
- `scripts/haftalik_muhasebe_denetim.py` · `send_muhasebe_denetim.bat` · `scripts/register-muhasebe-denetim-task.ps1`
#### Değişen
- `scripts/start_dashboard.ps1` (tray) · `dashboard/Components/App.razor` (bkmDownload JS)
- `dashboard/Data/EticQueries.cs` (Baskısı Yok ×3 metot) · `dashboard/Components/Pages/Eticaret.razor` (30g trend)
- `dashboard/Data/MuhasebeQueries.cs` (Fatura audit+köprü) · `dashboard/Components/Pages/Fatura.razor` · `Yevmiye.razor` · `Muhasebe.razor`
- `dashboard/Models/MuhasebeModels.cs` · `PeriodModels.cs` · `NavRegistry.cs` · `ServiceRegistration.cs`
- `sema/codes.yaml` (frm.frmTip → frmTipTnm)

> curator-check: 2026-06-23 (3g < 7g — hafif: bu oturum sema'ya frm.frmTip eklendi [frmTipTnm]; BASKISIYOK + fat.eMhsFisID köprüsü sema borcu olarak işaretlendi; tam dry-run gerekmedi)
