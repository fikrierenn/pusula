# Oturum Günlüğü — 2026-06-18

## Ana Konu
Dış-repo turu (CodeGraph + learn-claude-code, asistan gözüyle) → **BKM-Asistan (B-45) Faz-1 implementasyona başlandı**. plan-20 master finalize (yetenek matrisi + öğrenen katman + PII + repo sömürüsü + 2× deep-research). LLM kararı: **Gemini Flash + Groq fallback** (Anthropic değil — kullanıcı). Faz-1 LLM katmanı (provider+guard) commit'li; araçlar+loop yazıldı ama BUILD DOĞRULANMADI.

## Tamamlananlar
### Dış-repo değerlendirme (asistan gözüyle)
- **CodeGraph** — dup (cwm-roslyn-navigator var, Roslyn semantic üstün) → EKLENMEDİ. (`Pusula.sln` oluşturuldu — roslyn-navigator workspace için, ama MCP restart gerek.)
- **learn-claude-code** — B-45 harness referansı (loop/permission/memory/compact/subagent).

### plan-20 finalize (BKM-Asistan master)
- Yetenek matrisi (veri/görev→mail/takvim/rapor→ajanda) · öğrenen katman (PanelAsistanBellek: pinned/archival/self-edit) · PII maskeleme · repo sömürüsü (learn-claude-code çekirdek + Hermes disiplin + OpenClaw yetenek)
- 2× deep-research (rate-limit'e rağmen): **golden-record few-shot** (sorgular/*.sql retrieve — HIGH) + memory mimarisi (Mem0 ADD-only+temporal / Letta pinned+archival+self-edit) + Graph mail delegated scope (primary-doc doğrulandı). Mem0/Letta/Graph WebFetch ile doğrulandı.
- LLM=Gemini Flash + Groq fallback; `ILlmProvider` agnostik; PII-maske zorunlu; Gmail/GCal ekosistem (Faz-2).

### B-45 Faz-1 LLM katmanı (commit 9783efd)
- `dashboard/Data/Asistan/`: `ILlmProvider` (LlmTur/LlmArac/LlmYanit, id-opsiyonel) · `GeminiProvider` (REST generateContent+functionDeclarations) · `GroqProvider` (OpenAI-uyumlu chat/completions+tools) · `FallbackLlmProvider` (Gemini 429→Groq) · `SaltOkumaGuard` (SELECT-only).
- DI kayıt (Program.cs). Db.LoadEnvStatic().
- Canlı doğrulandı: **Groq "Merhaba" OK**, Gemini auth-OK (429=o anki 5RPM quota, geçici).

### B-45 Faz-1 araçlar+loop (YAZILDI, BUILD YOK, uncommitted)
- `AsistanAraclar.cs`: sql_sorgu (SaltOkumaGuard + PII maske: müşteri ad/tel/kart → ***) · sema_oku · ornek_sql_bul (golden-record) · gorev_ekle/listele.
- `AsistanService.cs`: tool-use loop (max 6 tur) + sistem-prompt (sema kuralları gömülü: KDV-hariç/iade-netleme/DMY/mekanID/stkKod≠barkod/salt-okuma).
- Program.cs DI (AsistanAraclar+AsistanService).

## Build / Test Durumu
- Build: LLM katmanı (9783efd öncesi) YEŞİL. Araçlar+loop+DI sonrası **DOĞRULANMADI** (build çalıştırılmadı — kullanıcı handoff'a geçti).
- Test: Groq REST canlı OK. Gemini 429 (quota, auth-ok). Asistan uçtan-uca test YOK.

## Commit Durumu
- Uncommitted: AsistanAraclar.cs, AsistanService.cs (yeni) + Program.cs (DI) — **build doğrulanmadı, bilinçli commit'lenmedi**.
- .env: GEMINI_API_KEY + GEMINI_MODEL=gemini-2.5-flash + GROQ_API_KEY + GROQ_MODEL (gitignore).

## Yarım Kalan / Yarına
### Üst öncelik (B-45 Faz-1 devam)
1. **Build doğrula** (AsistanAraclar/Service/DI) → hata düzelt → commit (B-45 Faz-1 araçlar).
2. **Asistan.razor veri-sorgu modu** — mevcut görev-taslak (yerel qwen) + yeni soru-cevap (AsistanService.SorAsync). Intent ayrımı (soru mu/not mu). Cevap balonu + araç-izi.
3. **Gemini-içi çok-model rotasyon** (kullanıcı 18.06 isteği): GeminiProvider GEMINI_MODELS virgüllü liste → model-A 429 → model-B (RPD havuzu çoğalt) → sonra Groq. FallbackLlmProvider çok-aşamalı.
4. **Canlı test:** "dün ciro?" → sema_oku→ornek_sql_bul→sql_sorgu→Türkçe cevap. Güvenlik: yazma denemesi reddi (SaltOkumaGuard). Rakam dashboard ile mutabık.
5. Öğrenen katman çekirdeği (PanelAsistanBellek + bellek_yaz aracı + recall) — Faz-1 sonu.

## İşe YARAMAYANLAR
- deep-research workflow 2× rate-limit'e takıldı (session-limit + 429) → çoğu doğrulama fail. Çözüm: hedefli WebFetch (Mem0/Letta/Graph primary-doc) ile düşen açıları elle doğruladım.
- Anthropic varsayımı → kullanıcı Gemini (+Groq fallback) seçti. Anthropic SDK yazılmadı, ILlmProvider sayesinde sorunsuz.
- cwm-roslyn-navigator hâlâ "NotStarted" (Pusula.sln eklendi ama MCP restart gerek — başlangıçta solution path okuyor).

## Kararlar
- LLM = **Gemini Flash birincil + Groq yedek** (Anthropic değil — maliyet/erişim). ILlmProvider agnostik.
- Fallback zinciri: Gemini-içi çok-model rotasyon → Groq (RPD havuzu çoğalt — 18.06).
- Python↔C# ortak DB (önceki), asistan PWA-içi (Telegram değil), kod dashboard/Data/Asistan/ ayrı namespace.
- PII maske baştan (KVKK) — sql_sorgu sonucu müşteri ad/tel/kart maskeli LLM'e.

### Kullanıcı feedback verbatim
> "illa antropic mı" → Gemini.
> "fallback içinde grog var" → Groq yedek (gsk_ key).
> "gemini'yi kendi içinde de döndür önce sonra grog fallback" → çok-model rotasyon.

## Dikkat
- ⚠️ Asistan kod (Araclar/Service/DI) BUILD DOĞRULANMADI — yarın ilk iş build+düzelt.
- ⚠️ 5112 instance hâlâ eski auth'suz build (B-84/önceki) — restart + /setup bekliyor.
- Gemini free-tier: 2.5-flash 20 RPD / 5 RPM (az) → çok-model rotasyon + Groq fallback şart. Groq cömert.
- API key'ler .env (gitignore) — commit'lenmez.

## Yarına Başlangıç Noktası
1. `cd dashboard && dotnet build -c Release` → Asistan araçlar+loop derleniyor mu, düzelt, commit.
2. Asistan.razor veri-sorgu modu (SorAsync entegre).
3. Gemini çok-model rotasyon (GEMINI_MODELS).
4. Canlı test "dün ciro?" + güvenlik (yazma reddi) + rakam mutabakat.

## Yardımcı Dosyalar
### Yeni
- `dashboard/Data/Asistan/`: ILlmProvider.cs, GeminiProvider.cs, GroqProvider.cs, FallbackLlmProvider.cs, SaltOkumaGuard.cs (commit'li) · AsistanAraclar.cs, AsistanService.cs (uncommitted)
- `plans/20-bkm-asistan-pwa.md` (master), `Pusula.sln`
### Değişen
- `dashboard/Program.cs` (LLM+araç DI), `dashboard/Data/Db.cs` (LoadEnvStatic), `.env` (Gemini+Groq key)

> curator-check: 2026-06-17 (<7g — gerekmedi)

---

## Oturum 2 — Genius asistanı: Faz-1 tamam + UI redesign + Faz-2 Gmail/Takvim + OpenRouter + öğrenen katman (plan-22) (~35 commit)

### Ana Konu
B-45 BKM-Asistanı ("Genius") devasa ilerleme: Faz-1 araçlar+loop build+commit → veri-sorgu modu → **UI tam yeniden tasarım (chat)** → **LLM-yönetimli tek akış** (niyet heuristiği kaldırıldı) → **Faz-2 Gmail+Takvim** (Google OAuth + etkinlik/mail araçları, onay-kapılı) → **OpenRouter LLM zinciri** (OpenRouter→Gemini→Groq) → **plan-22 öğrenen katman** (Hermes-uyarlı bellek + gecmis_ara) → **Genius ismi + lambadan-çıkan-cin maskotu** → **99-komut esini repertuar**. Oturum sonunda tüm free LLM kotaları tükendi → test durdu.

### Tamamlananlar
**Faz-1 + UI (sabahki LLM katmanı üstüne):**
- Araçlar+loop build yeşil + commit (`2766100`); Gemini çok-model rotasyon (`2e9cbf1`); Veri Sor kartı (`184c72a`).
- Sales şema düzeltme — ciro boş dönüyordu: `dbo.Sales` = `Date`/`DiscountTotal`/`VatTotal` (SaleDate/DiscountTotalDirect YOK; sema entities sat.56 yanlıştı) + sql_sorgu loglama (`9411c5e`). Canlı: dün ciro **1.278.364,92 ₺** (MCP mutabık).
- **UI chat redesign** (`1c8063e`) + stres-test sertleştirme (`9bbfaae`: GeminiProvider defansif parse, niyet v2, token kırpma).
- **LLM-yönetimli tek akış** (`75107a4`): niyet heuristiği KALDIRILDI → tüm mesaj SorAsync; `gorev_taslak_oner` onay aracı. "mısın→not" + "evet→Özlüce few-shot sızması" bug'ları çözüldü.

**Faz-2 Gmail + Takvim (plan-21):**
- plan-21 + Google OAuth rehberi (`6aad54b`); OAuth iskeleti (`21b11c7`: GoogleAuthService + `/auth/google` + PanelGoogleToken); Takvim+Gmail araçları + onay kartları (`130c9b3`); Meet + davetli-sor (`a2e9949`); online/yüz-yüze sor (`eed78be`); BUGÜN tarih enjekte (`886e5db`).
- Canlı: kullanıcı Google'a bağlandı (token DB) · "yarın 14:00 toplantı" → doğru tarih + onay kartı (auto-exec YOK) · "gelen kutusu özeti" → gerçek inbox. Etkinlik-oluştur/mail-gönder = kullanıcı onaylar (classifier engelledi, doğru).

**OpenRouter + kalite:**
- LLM zinciri OpenRouter→Gemini→Groq (`6370877`); rakam/kanıt/mağaza-zorunlu + iç-rotasyon (`9ea9ce2`); pano AI yerel→cloud (`eea0020`); secenek_sun (`a32ed8d`); görüşme persistence (`444eac9`); markdown+yerel-kaldır+Qwen3-Coder (`f8859b1`); mağaza-join prompt (`3f701fe`).

**plan-22 — Genius öğrenen katman (Hermes-uyarlı):**
- Bellek çekirdeği (`73c87f6`): PanelAsistanBellek satır-tablo (Tip=tercih|gercek, char-limit 1400/2200, no-auto-compact→LimitAsildi, GuvenlikTara, ASLA-delete→arsiv) + Snapshot (FROZEN prompt enjeksiyon) + bellek_yaz/gecmis_ara.
- Görev SonTarih + ham-not zenginleştirme (`f170f3f`); Genius ismi+maskot (`5964999`); repertuar 99-komut esini (`ab77724`).
- Canlı: Genius maskot+isim render · bellek_yaz → DB Id=1 · yeni konuşmada tercih uygulandı.

### Build / Test Durumu
- Build: **her commit yeşil** (~35 commit). Canlı test çok şey doğruladı (yukarıda). Sonunda **tüm free LLM kotası dolunca durdu**.

### İşe YARAMAYANLAR
- **`google/gemini-2.0-flash-exp:free` → 404** — OpenRouter'dan kalkmış. OpenRouter'da ücretsiz Gemini YOK.
- **Niyet keyword-heuristiği** (IsSoru) → "mısın" soru-ekini not sandı, "evet" örnek sızdırdı → KALDIRILDI.
- **GeminiProvider naif parse** → candidates-yok'ta çöküyordu → defansif+rotasyon.
- **Free-model SQL kalite varyansı** — Nex bazen 0/hatalı. Asıl fix = prompt store-join; güvenilir = $10 kredi.
- **50/gün model-başına sanıldı** → HESAP-GENELİ. Model değiştirmek kotayı açmaz.

### Kararlar
- LLM zinciri **OpenRouter→Gemini→Groq**; niyet=LLM-yönetimli (heuristik reddedildi); dış-aksiyon onay-kapılı (auto YASAK).
- İsim=**Genius** (Vezir reddedildi); maskot lambadan-cin. Bellek=satır-tablo (saf 2-blok reddedildi). SQL ikincil ama rakam+mağaza zorunlu. 99-komut=prompt-repertuar (palet değil).

### Kullanıcı feedback verbatim
> "sql/veri-sorgu önemli değil şu an" · "notu zenginleştirip göreve döndürme" · "muzip bir isim" → Genius · "lambadan çıkan cin resmi" → maskot · "plan modunu gene iplemiyorsun" → plan-22 · "öğrenmeyi hermesten kazı" → Hermes bellek · "99 komutu esinlenip özellik ekle" → repertuar.

### Dikkat Edilmesi Gerekenler
- ⚠️ **OpenRouter 50/gün DOLDU** → reset **19.06 03:00 TR**, ya da **$10 → 1000/gün**. Tüm free LLM bu an kapalı.
- ⚠️ **Auth şifresi sıfırlandı** (`fikrieren` — geçici şifre kullanıcıda, değiştirilecek).
- ⚠️ `.env` yarına: OPENROUTER_MODEL=qwen/qwen3-coder:free; rotasyon qwen→Nemotron-Ultra→Nex→Gemma-4→Llama. Restart şart.
- plan-22 Adım-8 (opsiyonel) atlandı: "Bellek olarak sakla" butonu + consolidate-sema bellek-curator.
- LlmService (yerel qwen) DI'dan çıktı (kod korundu, boot hızlandı). LLamaSharp tam-kaldırma sonraki tur.

### Yarına Başlangıç Noktası
1. **Kota:** 03:00 sonrası VEYA $10 → test açılır. `dotnet run` (qwen3-coder primary).
2. **Canlı-test:** bellek gecmis_ara + no-auto-compact + güvenlik-reddi · görev due-date uçtan-uca · pano AI derin-özet · markdown · SQL kalitesi (qwen mağaza-kırılım).
3. **Sen (dışa-dönük):** Faz-2 etkinlik Oluştur + mail Gönder (Chrome onay) · auth şifre değiştir.
4. **Karar:** free-SQL yetersizse $10 kredi veya Gemini paid.
5. plan-22 Adım-8 (ops.) + repertuar canlı doğrula.

### Yardımcı Dosyalar
**Yeni:** AsistanBellekService.cs, GoogleAuthService.cs, TakvimMailAraclar.cs, GorusmeService.cs, OpenRouterProvider.cs · genius.svg · plans/21 · docs/asistan-google-oauth-kurulum.md · plan-22 (`~/.claude/plans/expressive-gathering-nebula.md`).
**Değişen:** AsistanService.cs (Genius persona+repertuar+snapshot+MetinUretAsync), AsistanAraclar.cs (bellek/gecmis/secenek/takvim/mail+son_tarih), GorevService.cs (SonTarih), Asistan.razor (chat+maskot+markdown+onay+persistence), Home/Sadakat.razor (cloud özet), Gemini/Fallback/GroqProvider.cs, Program.cs, Gorevler.razor, GmDashboard.csproj (Google.Apis+Markdig).

### Handoff-sonrası API'siz iş (kota dolu — LLM'siz, plan-22 Adım-8)
- **Bellek yönetim paneli** `/bellek` (`28533c3`, GeniusBellek.razor): gör/ekle/doğrula/arşivle + doluluk + stale bayrağı. Canlı doğrulandı (LLM'siz): Id=1 tercih görünür, ekle %1→%2, arşivle geri.
- **Due-date uçtan-uca doğrulandı** (LLM'siz): düzenle→tarih→DB(SonTarih=15.06)→"Gecikti · 📅" kırmızı pill.
- **"Bellek olarak sakla" butonu** + **sidebar nav (Genius+Bellek)** + **consolidate-sema bellek-curator adımı** (`4e83307`). Nav canlı doğrulandı.

> curator-check: 2026-06-17 (<7g — gerekmedi)

---

## Oturum 3 — B-111 WMS bekleyen keşfi (kota dolu, LLM'siz, kod yok)

### Ana Konu
Asistan kod-bitti, kota kapalı → API'siz değerli iş arandı. B-111 (WMS bekleyen-sipariş doluluk göstergesi) "metrik hazır, sadece kart" sanılıyordu. **Fact-Force Gate keşfi: veri kirli, B-111 düşük-efor DEĞİL.** Kod yazılmadı; bulgu sema'ya + SQL arşive yazıldı, karar parked.

### Keşif (MCP canlı — DerinSISBkm)
- `depo.emirAyr` bekleyen (`emTamam=0`): **2.422 emir / ~2M satır / 4,3M kalan adet**; header `kTarih` **2021'e** kadar gidiyor → zombi pile (WMS açık emirleri hiç temizlemiyor).
- `depo.emir.emDurum=1` (açık) hem 2021 zombi hem 2026-06-17 taze emri kapsıyor → **temiz açık/kapalı bayrağı YOK**. Ayraç = recency (kTarih), durum değil.
- Gerçek aktif backlog minik: 1-3g **5 emir/877 adet**, 4-7g 1 emir, bugün ~0. Ham `emTamam=0` saymak 4,3M çöp gösterir.
- `emirAyr` yapısı: toplama emir **satırı**; `emAdet`=planlanan, `emAdetTop`=toplanan (tamamlanınca dolar → bekleyende 0), FK `emAyrID→depo.emir.emID`.
- `metrics.yaml wms_bekleyen_toplama` aslında **J_ORDERS/J_ORDER_DETAILS (e-tic)** kaynağına dayanıyordu — emirAyr değil.

### İşe YARAMAYANLAR
- Ham `depo.emirAyr.emTamam=0` sayımı → 4,3M kalan adet (2021 zombi dahil), gerçek backlog değil.
- Header `emDurum` ile zombi/aktif ayırma → emDurum=1 ikisini de kapsıyor, ayırmaz.

### Kararlar
- B-111 **parked** (bu gece dur). İki yol: **A)** emirAyr recency-filtreli (son ~7g) + "2.422 eski açık emir takılı" hijyen-uyarısı (düşük efor) · **B)** J_ORDERS e-tic bekleyen (iş anlamı temiz "bekleyen sipariş" ama sorgu karmaşık, referans SQL repo'da yok → Tier-2/3). Önerilen: A (hızlı değer).

### Yardımcı Dosyalar (yeni — uncommitted, handoff commit'ine dahil DEĞİL)
- `sorgular/2026-06-18-wms-bekleyen-kesif.sql` — 3 keşif sorgusu (ikiz yükümlülük arşiv)
- `sema/metrics.yaml` — wms_bekleyen_toplama'ya `UYARI_emirAyr` + last_verified eklendi

### Yarına Başlangıç Noktası
1. (Önceki turdan) Kota 03:00 reset / $10 → asistan canlı-test (bkz Oturum 2 listesi).
2. **B-111 kararı:** A mı B mi? A seçilirse: `RefQueries.Envanter.cs` GetDepoWmsAsync'e bekleyen-bloğu (emTamam=0 + emTarih son 7g, emAdet-emAdetTop) + Operasyon.razor mini-kart + zombi-uyarı badge. SQL hazır (arşivde).
3. `sema/metrics.yaml` + `sorgular/2026-06-18-wms-bekleyen-kesif.sql` commit'le (B-111 işine başlarken veya ayrı docs commit).

> curator-check: 2026-06-17 (<7g — gerekmedi)
