## Sema sözleşmesi (plan-44) — F1 tamam

**Ölçüm (scratchpad/sema_profil.py, 2026-09-11):** sema 305 kayıt · entities'te ~100 alan adı
yalnız 1 kayıtta · kanıt 4 ayrı adla (evidence/olcum/dogrulama/kanit) · 140/312 kayıtta kanıt
yok · 102 kayıtta `last_verified` yok · 81 çapraz referansın tamamı prose'a gömülü · kolon
açıklaması 1/124 entity · metrik semantiği (tip/additivity/birim/taban) 0/71 ·
`metrics:hediye_ceki` tek başına 20.205 karakter.

**Yapısal keşif:** entities.yaml'ın 124 kaydının ~25'i tablo DEĞİL — sahte tablo adı altına
park edilmiş not (`irsHrk.MERKEZ_DEPO_CIKISI`, `urn.IADE_HAKKI_IZLI_DEGIL`,
`EncoreMerkez.dbo.SalesProducts.TUTAR_SEMANTIGI`). 88 gerçek entity, 22 şüpheli (10'u
4-parçalı JOKER adı, gerçek), 14 konu notu.

**Üretildi:**
- `plans/44-sema-sozlesme-ve-temellendirme.md` — 6 fazlı plan, 2 alternatif reddedildi
  (dbt/Cube = dönüşüm motoru getiriyor, BKM'de dönüşüm katmanı yok · otomatik toplu göç =
  yorum/blok-skaler kaybı + kanıt uydurma riski).
- `sema/_sozlesme.yaml` — şemanın şeması. Alan adları uydurulmadı, her dosyada ZATEN baskın
  olan ad kanonik seçildi (evidence 149× · note 165× · last_verified 210×) → mevcut
  tüketiciler kırılmıyor. Zenginlik silinmiyor, `ayrinti:` altına kapsülleniyor.
  `tip: tablo|view|sp|tvf|openquery|sema|kural|olcum` + not'lar `kullanir:` ile hedefini
  bildirmek zorunda. queries'e kısmi muafiyet (confidence/evidence) — gerekçesi YAZILI:
  golden SQL'in kanıtı ayrı alan değil, sorgu dumanında KOŞUYOR olması.
- `tools/sema_denetim.py` — DB'siz saf yapı denetimi. exit 0/1/**2**.

**İlk koşu:** 9 KIRIK (codes'ta values/lookup ikisi de yok) · 824 UYARI.

**Kırılabilirlik kanıtlandı:** queries.yaml'a sahte referans + `dd.MM.yyyy` damga kondu →
`cozulmeyen_referans` + `tarih_bicimi` KIRIK verdi, exit 1; geri alındı (diff boş). Sözleşme
gizlendi → `KOSAMADI` + exit 2 (koşamamak yeşil sayılmıyor).

**Kapsam dışı bırakıldı (kullanıcı direktifi):** asistan tarafı. `AsistanAraclar.SemaOku`
6.000 karakter sessiz kırpması ÖLÇÜLDÜ (metrics.yaml 157.581 byte → asistan 41 metriğin 4'ünü
görüyor) ama asistan kullanılmadığı için düzeltilmiyor — bilinen borç, TODO'ya.

**Açık:** F2 alan göçü · F3 referans grafı · F4 canlı kolon temellendirme (sys.columns) ·
F5 metrik semantiği · F6 atomikleştirme.

## F4 — canlı kolon temellendirme: ilk koşuda ÜÇ gerçek yalan

`tools/sema_kolon_cek.py` (yeni): sema'nın iddia ettiği kolonları canlı `sys.columns` ile
karşılaştırır. exit 0/1/2. Sorgu aracı `sqlcli` (kural gereği ad-hoc snippet değil).

**Bulunan ve ÖLÇÜMLE düzeltilen (2026-09-11):**
1. `dbo.irsAyr` — sema `pk: "(ehID, satir)"`. **`satir` diye kolon YOK.** Gerçek: `ehSira`
   (column_id 2). Ayrıca tabloda **PK kısıtı hiç yok** (`is_primary_key` kaydı 0) — dedup için
   PK'ya güvenen yanılır.
2. `DerinSISBkm.dbo.fatAyr` — sema `pk: ehID_satir`. Böyle kolon yok; irsAyr ile **aynı şekil**
   `(ehID, ehSira)`, PK kısıtı yok.
3. `BKM_GENEL.dbo.vw_PuanBil` — `key` listesi `Ucretsekli` + `Tahakkuksekli` sayıyordu,
   **ikisi de view'de yok**. En çarpıcısı: kaydın KENDİ prose'u (`ucret_kurali`) zaten
   _"view `Ucretsekli`'yi TAŞIMAZ"_ diyordu → **kayıt kendi içinde çelişiyordu** ve bunu
   ancak canlı katalog yakaladı. Doğru yol `dbo.puanbil.Ucretsekli` (ham tablo).
   Silinmedi, `key_duzeltme` ile beyan edildi (bi-temporal: çürüyen iddia iz bırakır).

**Kendi aracımın iki sahte bulgusu da yakalandı ve düzeltildi** (başka-ne-olabilir kapısı):
`(çek)` içinden `ek` çıkarılması (unicode `\b` eksikti) ve kendi yazdığım açıklama metninden
`MANTIKSAL`/`KISITI` kolon sanılması. Kural: `pk` saf tanımlayıcı listesi değilse **kolon
iddiası türetilmez** — emin olunamayan yerde iddia etme.

**Çapraz doğrulama:** drift aracı, entity sınıflandırmasının bulduğu sahte-tablo kayıtlarının
8'ini bağımsız yöntemle teyit etti ("NESNE CANLIDA YOK": `irsHrk.MERKEZ_DEPO_CIKISI`,
`urn.IADE_HAKKI_IZLI_DEGIL`, `EncoreMerkez.dbo.SalesProducts.TUTAR_SEMANTIGI`, `DerinSISBkm.iky.*`,
`depo.emir + emirAyr` …). İki ayrı yöntem aynı kayıtları gösteriyor.

Son durum: **drift 0** (exit 0). Kapsama boşluğu raporlanıyor (eksiklik, yalan değil):
`perbilgi` +177 · `frm` +95 · `urn` +80 kolon sema'da anılmıyor.

## F2 — eş-anlamlı alan göçü uygulandı

`tools/sema_goc.py` (yeni): satır-bazlı cerrahi göç, **eşdeğerlik kapısı** olmadan yazmıyor
(safe_load(yeni) == beklenen_dönüşüm(eski) değilse hiçbir şey yazılmaz; bu kapının ateşlendiği
`donustur` bilerek bozularak kanıtlandı → exit 2). Eşleme tek kaynaktan: `_sozlesme.yaml`.

59 alan katlandı (entities 4 · metrics 49 · queries 6), 0 çatışma, satır sayıları birebir aynı
(4/4, 49/49, 6/6 — yalnız anahtar belirteci değişti), CRLF korundu. Denetçide `esanlamli`
uyarısı **76 → 0**.

**Tasarım düzeltmesi:** ilk taslak `olcum`'u `evidence`'ın eşanlamlısı sanıyordu. 20 kaydın
içeriği okundu: `olcum` = ÇIKAN SAYILAR, `evidence` = YÖNTEM/KAYNAK. `kanit` ve `dogrulama` da
sayı taşıyor → `olcum`'un eski adları. Birleştirmek bilgi yok ederdi. Ayrım
`olctum-mu-cikardim-mi` kuralının yapısal karşılığı olarak sözleşmeye yazıldı.

## plan-45 — repo-ötesi öğrenen çekirdek (taslak, onay bekliyor)

Çatallanma ÖLÇÜLDÜ: pusula/bel/fifo üç ayrı sema şekli. bel `kardinalite` + `canli-degismezler`
icat etmiş, pusula sözleşme/göç/queries+duman. Kimse ötekinin kazanımını almıyor — `sqlcli`
fork vakasının tekrarı (CLAUDE.md'de "fork'lamayın" yazılı).

Araştırma: GATE (yürütmeden öğrenme) · Zep/Graphiti (bi-temporal, sil-me/geçersizleştir) ·
A-MEM (Zettelkasten bağ evrimi) · dbt 2026 (%90→98,2) · Cube (+17-23 puan) · Spider2-snow
(%94,15) · power-platform-skills (shared/ + template) · sqlcli (mimarinin modeli).

Önerilen: `D:\Dev\sema` tek kanonik motor + repo-yerel veri/profil + adaptör sınırı
(çekirdek SQL BİLMEZ). Öğrenme döngüsü 4 sinyal, **otomatik yazma YOK** (öneri + onay).
Açık kararlar plan §9.

## Akşamki sipariş oturumunun devralınması (transkript analizi + üç düzeltme)

Kullanıcı: *"akşam diğer bir kullanıcıda bir şeyler yaptık orada token bitti onları görebiliyor
musun"* → başka Claude HESABI görülemez; ama iş bu makinede duruyordu. Yerel transkript
`b5f79eaa` (sipariş oturumu) okunup analiz edildi.

### Akşam ne yapılmıştı
1. **Sipariş listesi kohortu ÇOK DARDI** — kullanıcı yakaladı (*"çok çok az bu sipariş
   rakamları emin misin"*), ölçümle doğrulandı: Kırtasiye'de satan 18.292 çeşitten liste
   **796**'sını gösteriyordu (`ToplamStok = 0`), kapağın altına düşmüş gerçek ihtiyaç
   **1.687 çeşit / 14.528 adet**. Liste gerçeğin ~**1/13'ü**.
2. **Ağu-Eyl 2026 alım röntgeni**: net alım 151,2M ₺ / 34.140 çeşit. Üç tuzak ayıklandı —
   cold-start (aşırı görüntüsünün %65'i yeni ürün) · "tabanda yok" 25,4M = Sınav Okulları
   ithal kurs kitabı · sağdan sansür (fazlalık ÜST SINIR).
3. **İki kaçış hipotezi ölçülüp çürüdü**: Mopak A4 kağıt (iç tüketim 12 ayda 8 adet →
   ~3,5 yıllık stok, **finansal karar**) · Yanıt 22 sınav kitabı (kurumsal bulk yok, içerik
   ömrü riski).
4. Kural pekişti: `before-major-change.md` → **SEMA İLK, canlı keşif SONRA** (tedarikçi
   köprüsü `irs.eFirma → frm.frmID` sema'da tanımlıyken canlı keşfedilmiş, 2 tur kayıp).

### Bugün yapılan üç düzeltme
**(a) Kohort genişletildi — `scripts/siparis_onerisi_excel.py`.** Ölçüt artık
`ToplamStok < hız × (temin + 30)` (kapak altı); öneriden **eldeki stok düşülür**, tavan da
stoğu sayar, satırlar **ACİL (rafta yok)** / **İKMAL (kapak altı)** diye ayrılır, öneri 0
olan satır listeye girmez. Ölçülen etki:

| | eski (stok = 0) | yeni (kapak altı) |
|---|---|---|
| aday | 1.582 | **3.303** |
| A-SÜREKLİ | 416 çeşit / 2.291 adet / 376.516 ₺ | **1.549 çeşit / 29.295 adet / 1.942.711 ₺** |
| A 12-hafta plan cirosu | 1.350.193 ₺ | **11.772.433 ₺** |
| B-SEZON | 1.166 çeşit / 3.368 adet | 1.728 çeşit / 9.255 adet / 1.270.638 ₺ |

**(b) Arşiv borcu kapandı** — `sorgular/2026-09-10-agu-eyl-alim-arz-gunu.sql` (4 blok:
arz günü dağılımı · cold-start ayrımı · yaş ayracı düzeltmesi · kohort darlığı ölçümü ·
tedarikçi köprüsü). Emitter scratchpad'den repoya alındı: `scripts/asiri_alim_excel.py`
(mutlak `KOK` yolu dosya konumundan türetilir oldu), yeniden koşturuldu — üretti.

**(c) 21,9M RAKAMI YANLIŞTI — düzeltildi.** Akşamki yaş ayracı `IlkGiris >= … THEN 'YENİ'
ELSE 'ESKİ'` yazılmıştı; **`IlkGiris NULL` sessizce ESKİ sayılıyordu**. Ölçüldü:

| Yaş | Çeşit | Adet | Tutar |
|---|--:|--:|--:|
| YENİ (<1 yıl) | 4.599 | 316.523 | 39,33M ₺ (cold-start, meşru) |
| **ESKİ (≥1 yıl, yaşı BİLİNEN)** | **4.501** | **161.497** | **18,27M ₺** ← hesap sorulur |
| YAŞI BİLİNMİYOR (NULL) | 307 | 78.990 | 3,65M ₺ ← ayrı kova |

Yani hesap sorulacak dilim **18,3M ₺ (%12,1)**, 21,9M değil. Fark, iki emitter'ın ayrışmasından
çıktı: Excel `IlkGiris < DATEADD(YEAR,-1,…)` yazdığı için NULL'ları zaten dışarıda bırakıyordu
(18,27M), özet sorgusu içeri alıyordu (21,9M) — `veri-dogrula` § cross-source sweep tam bu.
Ders: **`ELSE 'ESKİ'` deseni NULL'u yutar**; üç kova ayrı yazılır.

### Doğrulama
`python scripts/siparis_onerisi_excel.py` → exit 0, 3.303 aday · `python scripts/asiri_alim_excel.py`
→ 4.501 çeşit / 18.273.141 ₺ (ilişkili taraf 6,63M · dış 11,64M) · `sema_degismez.py` 43/0 kırık.

### Ayrıca bugün (denetim sertleştirme)
- `tools/sema_degismez.py`: **NULL→0 sessiz fallback KALKTI** (eskiden `esit 0` bekleyen kayıt
  hiç ölçülmeden YEŞİL veriyordu) + **çıkış 2 = KOŞAMADI** geldi (11 çıkış noktası). İkisi de
  kırılabilirlikle kanıtlandı: olmayan id → exit 2; satır dönmeyen sorgu → exit 2, sonra geri alındı.
- `.git/hooks/pre-commit` **kuruldu** (kullanıcının kendi terminal commit'inde de koşsun) +
  izlenen kaynak `.claude/hooks/git-pre-commit-panel.sh` + kurulum `tools/git-hook-kur.sh`.
  Claude hook'unun tetik deseni genişletildi: `SatisAnalizi(Queries|Models|Hucre|Tablo)` →
  `SatisAnalizi` (eski desen `SatisAnaliziTabanService.cs` ve `SatisAnalizi.razor`'u kaçırıyordu).

## F5 + tip temellendirme — iki yalan daha, ve 287 semantik alan

**Karar (kullanıcı):** motor `D:\Dev\sema`, **.NET**. Python önerimi değiştirdim; belirleyici
olan ÖLÇÜLMÜŞ hataydı: `sema_kolon_cek.py` sqlcli'yi alt-süreç olarak çağırıp stdout'undan
JSON ayıklıyor ve sqlcli'nin ANSI renk kodundaki `[` JSON başlangıcı sanıldığı için patladı.
Dil sınırının semptomu. .NET'te sqlcli'nin profil/`${ENV}`/salt-okuma/retry kodu doğrudan
paylaşılır. Odoo/XML-RPC gerekçem de geçersiz çıktı (Odoo `/jsonrpc` sunuyor).
Port kapısı plan-45 §9'da: yeni araçlar önce, koşucular (43 değişmez + 15 sorgu) en son ve
**birebir aynı çıktı** verene kadar paralel.

**F5 — metrik semantiği: 72/72 dört alanda dolu.** Anahtar-kelime çıkarımı denendi, ELENDİ
(kendi çıktısı `gider_konvansiyonu`'na `birim: TL` + `additivity: tam` verdi; kuralın birimi
olmaz). 76 kaydın içeriği okunarak elle sınıflandırıldı:
- `tip`: kural 39 · basit 13 · oran 10 · turev 10
- `birim`: yok 39 · TL 17 · oran 8 · adet 6 · gun 1 · kisi 1
- `taban`: yok 53 · kdv_haric 11 · **belirsiz 8**
- `additivity`: yok 51 · tam 17 · **yari 4**

Yeni sözleşme değeri **`taban: belirsiz`** — ölçüldü ki 32 para metriğinin yalnız 7'si KDV
tabanını beyan ediyordu. Tahmin etmek yerine SESSİZ eksik → BEYAN EDİLMİŞ açık soru yapıldı.
`additivity: yari` (stok/kadro anlık kesiti) 4 kayıtta: zaman boyunca toplanırsa 12× şişik
rakam üretir ve kimse fark etmez.

**Entity `tip`'i canlı katalogdan türetildi** (GATE deseni — gerçek yürütmeden gelir, elle
yazılmaz): `sema_kolon_cek.py --tip-oner` → `sys.objects.type_desc` → 59 entity. Bu koşuda
**iki yalan daha** çıktı ve ikisi TAM TERS takılmıştı:
- `BKMDATA.ent.odak_urun_tam` — sema `view`, canlı **tablo**
- `BKMDATA.ent.OdakSatisDurumlari` — sema `tablo`, canlı **view**

Fark maddi: view "her zaman taze", tablo "yenilenmesi gereken snapshot". `odak_urun_tam`
ürün master'ı olarak kullanılıyor.

**Diğer düzeltmeler:** 6 `gozlem`/`dogrulama_damgasi` kaydı içeriği okunarak
`olcum` (3) / `kural` (2) / `olcum+superseded` (1) olarak ayrıldı ve hedef nesnelerine
bağlandı. Toptan eşanlamlı göçü yapılsaydı 5'i yanlış sınıflanacaktı.

**Araç kazanımı:** `sema_goc.py --alan-ekle <json>` genel ilkeli eklendi (satır-içi/flow
harita desteğiyle), aynı eşdeğerlik kapısı. `sema_denetim.py --oneri-baglar` (salt-okuma
öneri) ile yazma tarafı ayrı — tespit mantığı tek yerde.

**Durum:** denetim KIRIK **0** · drift **0** · değişmez 43/43 · katalog 15/15.
Kalan uyarı: `tip` yok 121 (JOKER/INTER_BOS/pdks ve sahte-entity'ler) · kanıtsız 139 ·
damgasız 61 · atomiklik 9 · skaler gövdeli 4 kayıt (F6).

## Kırtasiye derinleşmesi — "çok az"ın İKİNCİ kök sebebi: hız tabanı

Kullanıcı: *"Kırtasiye kategorisine derinleş, tek tek göster"*. Liste çıkarılırken sayılar
tutarsız göründü (sezon ayları `Satis365` penceresinin içinde olmasına rağmen ondan büyük) —
sunmadan önce doğrulandı ve **gerçek bir ölçüm hatası** çıktı.

### Bulgu 1 — düz 365g hızı SEZON ürününde talebi 4 kat az sayıyor
Kırtasiye'de sezon-yoğun (sezon satışı > 365g satışının yarısı) **285 çeşit** için 50 günlük
pencere talebi: düz hızla **7.257 adet**, geçen yılın AYNI takvim penceresiyle **29.057 adet**.
Sebep ölçüldü: aylık dağılım **Ağu 10.510 · Eyl 29.193 · Eki 10.437** — zirve EYLÜL, yani tam
şu an; `Satis365` penceresi (09.09.2025→09.09.2026) o zirveyi dışarıda bırakıyor.

Bağımsız doğrulama (irsHrk, taban değil): `Maxx Mx-616 Tahta Kalem Silgi Seti` →
**Eylül 2025: 1.617 adet · Ekim 2025: 20 · son 365 gün: 484**. Yani düz hız (1,3/gün) eylül
talebini göremiyor. Şüphe çürüdü, düzeltme doğrulandı.

**Uygulandı:** `kapak = max(365g düz, sezon penceresi) × (temin + 30)`; sezon penceresi =
geçen yılın aynı takvim aralığındaki satış (Ay1/Ay2/Ay3'ten gün-orantılı).

### Bulgu 2 — "geçen yıl kadar al" bir VARSAYIM; ölçülmeden uygulanamaz
Bu yıl / geçen yıl aynı takvim penceresi (1 Ağu → kesim, adet):
**Kırtasiye 0,82 · Oyuncak 1,50 · Hediyelik 1,25 · Elektronik 0,88.**
Kırtasiye'de geçen yılın sezonunu birebir sipariş etmek **%18 fazla** almaktır. Sezon penceresi
talebi artık kategori oranıyla çarpılıyor; bu yıl < 200 adet satmış kategoride oran
GÜVENİLMEZ sayılıp 1,0 alınıyor (uydurma yok).

### Bulgu 3 — sezon sonunda emniyet stoğu TERS çalışır
Emniyet tavanı kapağın %100'üydü (sıçramalı koruması). Ama taban sezon penceresiyse sezon
BİTİYOR demektir ve asimetri ters döner: eksik almanın bedeli kaçan satış, fazla almanınki
**ölü stok** (mal gelecek sezona kalır, kırtasiyede model/desen değişir). Sezon tabanlı
satırlarda emniyet **%25'e** indirildi.

### Kırtasiye sonuç (kesim 09.09.2026)
| | Çeşit | Adet | Yatırım | 12-hafta plan |
|---|--:|--:|--:|--:|
| A-SÜREKLİ (bugün yazılır) | **776** | **38.618** | **1.695.545 ₺** | 4.926.189 ₺ |
| — ACİL (rafta yok) | 185 | 2.145 | | |
| — İKMAL (kapak altı) | 591 | 36.473 | | |
| B-SEZON/SEYREK (gelecek sezon) | 864 | 9.685 | 888.087 ₺ | 2.121.113 ₺ |

Üç düzeltmenin bileşik etkisi (kitapdışı A listesi): 2.291 adet / 376K ₺ → **57.846 adet /
3.986.801 ₺**. Ara adımlar: kapak altı kohort 29.295 adet · sezon penceresi 78.100 ·
yıl oranı + emniyet tavanı **57.846** (son).

⚠ KALAN SINIR: sezon penceresi geçen yılın AYNI ürününe dayanır — ürün kartı yenilendiyse
(model/desen değişimi) geçmiş yoktur ve o ürün düz hıza düşer. Yıl oranı KATEGORİ düzeyinde;
ürün düzeyinde oran ölçülmedi (n yetersiz).

## F6 — atomikleştirme: 20.258 karakterlik dosya 20 atomik bulguya ayrıldı

**Zarar boyut değildi.** 20K karakterlik bir kayıtta 20 ayrı bulgu TEK `last_verified` altında
duruyordu: 2026-06'da ölçülen gerçek, 2026-09'da ölçülenle aynı damgayı taşıyor → **decay
çalışamıyor**, biri bayatlarken öteki taze görünüyor. Alan adlarında gömülü tarihler
(`KASA_GOCU_11072025_...`, `olcum_2026_09_02`) bunun kanıtıydı.

**Ayırma kuralı ÖLÇÜLDÜ:** metrics.yaml'da BÜYÜK_HARF alan adı = ayrı bulgu/kural;
küçük_harf = kaydın kendi tanımı. entities/codes'ta BÜYÜK-harf alan **0** çıktı → oradaki
uzun kayıtlar tek nesnenin dosyası, kural onlara UYGULANMADI. Sınıflandırma uydurulmadı,
sayıldı.

4 ebeveyn → **50 atomik çocuk** (`hediye_ceki__kanal_kurali` gibi). Her çocuk:
`kullanir: [metrics:<ebeveyn>]` ile grafa bağlı · tip/birim/taban/additivity/confidence
devralınmış · `last_verified` devralınmış AMA **`damga_devralindi: true`** ile işaretli —
bulgunun kendi doğrulama tarihi bilinmiyor; sessizce ebeveynin damgasını takmak yalan olurdu.

`hediye_ceki` 20.258 → 4.591 karakter.

**Kapının kendi boşluğu bulundu ve kapatıldı:** ilk gate ebeveynin kalan alan ADLARINI
karşılaştırıyordu, DEĞERLERİNİ değil — çok satırlı bir değerin yarısı silinse alan kümesi
değişmeyeceği için hata SESSİZ kalırdı. Değer karşılaştırması eklendi, sonra kapı bilerek
bozuldu (çocuk notu 40 karaktere kırpıldı) → 5 kayıtta "note farkli" verip exit 1 etti,
geri alındı.

**4 skaler gövdeli fifo kaydı** haritaya çevrildi (`yapi` uyarısı 4 → 0).

**Sözleşmeye iki yeni alan, ikisi de "beyan edilmiş istisna > sessiz istisna":**
- `damga_devralindi: true` — devralınan damga görünür.
- `atomiklik_gerekcesi` — tavanı aşan kayıt BÖLÜNMEMELİYSE nedenini YAZMALI. Gerekçesiz
  muafiyet yok. 6 kayda yazıldı (vw_PersonelDepartman tek view'in dosyası, irsHrk.ehTip
  34-kodluk lookup bölünemez, vb.).

**Durum:** denetim **KIRIK 0** · atomiklik **0** · yapi **0** · drift **0** ·
değişmez 43/43 · CRLF korundu. metrics 76 → 126 kayıt (50'si atomik çocuk).

## Motor kuruldu — `D:\Dev\sema` (.NET), `sema denetle` Python ile birebir

**Dil kararı .NET** (kullanıcı onayı). Python önerimi belirleyen ölçülmüş hata değiştirdi:
`sema_kolon_cek.py` sqlcli'yi alt-süreç çağırıp stdout'undan JSON ayıklıyor ve sqlcli'nin
ANSI renk kodundaki `[` JSON başlangıcı sanıldığı için patlamıştı — dil sınırının semptomu.
.NET'te alt-süreç/ANSI temizleme yok, sqlcli'nin profil/`${ENV}`/guard kodu doğrudan paylaşılır.

**PORT KAPISI GEÇİLDİ:** pusula'nın gerçek semasında (373 kayıt) `sema denetle` ile
`python tools/sema_denetim.py` çıktıları **birebir aynı** (`diff --strip-trailing-cr` → 0
satır, aynı exit). Python SİLİNMEDİ — plan-45 §9 gereği paralel duruyor.
Kırılabilirlik .NET'te de kanıtlandı: sahte referans → KIRIK + exit 1 · sözleşme gizli →
KOSAMADI + exit 2.

**Port GERÇEK BİR KUSUR ortaya çıkardı:** YamlDotNet, PyYAML'ın tolere ettiği **25 satırı**
reddetti — çok satırlı çift-tırnaklı skalerlerin devam satırları sütun 0'daydı. Yani sema
dosyaları katı YAML'a göre GEÇERSİZDİ ve Go/JS/.NET ayrıştırıcılarıyla taşınamıyordu; bunu
kimse bilmiyordu. Düzeltildi (eşdeğerlik tam). İlk düzeltme denemem heuristikle yapıldı ve
tırnak içindeki `Belge_Tipi:` satırını üst-seviye anahtar sandı → beyaz listeye çevrildi.

**Repo-ötesi çalışıyor:** sözleşme ÇEKİRDEĞE taşındı (gömülü kaynak). bel'de hiçbir kurulum
yapmadan koştu: 172 kayıt, **21 KIRIK buldu**, `queries.yaml` yok diye "DENETLENMEDI" yazdı.
bel Türkçe bölüm adı kullanıyor (`kopruler`/`kodlar`/`metrikler`) → verinin yeniden
adlandırılması İSTENMEDİ, sözleşmeye `bolum_esanlamli` eklendi, motor uyum sağlıyor.

**GÜVENLİK OLAYI ve dersi:** ilk `PackageId` `Sema` seçilmişti;
`dotnet tool update -g --add-source ./nupkg Sema` benim 0.1.0'ımı değil **nuget.org'daki
YABANCI `Sema` 1.2.4 paketini** kurdu (yerel kaynak eklemek nuget.org'u kapatmaz, yüksek
sürüm kazanır). Tedarik-zinciri riski; paket derhal kaldırıldı (`dotnet tool list -g` temiz),
PackageId `BkmSema` yapıldı, kurulum akışına sürüm sabitleme + **kurulum sonrası sürüm
doğrulama** eklendi. Ders: "kuruldu" yazması DOĞRU paketin kurulduğunu kanıtlamaz.

Commit'ler (D:\Dev\sema): `345068e` v0.1 · `a6a1dc6` çekirdek sözleşme + repo-ötesi.

## Sipariş önerisi PANELE girdi (plan-46)

Kullanıcı: *"plan yaz ve panele ekle"*. Plan `plans/46-siparis-onerisi-panelde.md` (2 alternatif
reddedildi: Python'u panelden çağırmak · hesabı tabana yazmak — öneri kesime bağlı, tabana
yazılırsa bayatlar).

### Ne eklendi
- `dashboard/Data/SatisAnaliziQueries.Siparis.cs` — **sipariş çekirdeği** (kapak · sezon
  penceresi · yıl oranı · emniyet · tavan · öneri). Yıl oranı kesim başına BİR kez ölçülüp
  30 dk cache'lenir, SQL'e literal CASE olarak gömülür.
- KPI: yeni **SATINALMA** grubu + **Sipariş İhtiyacı** kartı (maliyetle) + karşı-metrik
  **ACİL · hiç stok yok**.
- Liste: `Sipariş Önerisi` · `Kapak` · `Hız Tabanı` kolonları (varsayılan kapalı) ·
  durum filtresi **Sipariş ihtiyacı** ve **Sipariş — ACİL**.

### Panelde ölçülen (kesim 10.09.2026, tüm evren)
**40,7 milyon ₺ · 13.246 çeşit · 272.382 adet · ACİL 3.151 çeşit.**
Kart → filtre → liste tutuyor: 265 sayfa × 50 = 13.246 ✓ (plan done kriteri).

### ÜÇ RUNTIME HATASI — hiçbirini build yakalamadı
1. **SQL 8632 "deyim hizmetleri sınırına ulaşıldı"** — KPI sorgusu 56 agregayla zaten
   sınırdaydı, sipariş ifadeleri eklenince SQL Server sorguyu REDDETTİ ve panel HİÇ açılmadı.
   → Sipariş özeti AYRI sorguya alındı (`SiparisOzetAsync`).
2. Ayrı sorguda da 8632 — ay-örtüşmesi zinciri (DATEFROMPARTS/DATEADD/DATEDIFF iç içe,
   3 ay × 4 tekrar) tek başına sınırı aşıyordu. → **Takvim matematiği C#'a taşındı**
   (`KesimP`: @pbas · @a8 · @a9 · @a10 · @a11) ve hesap **CROSS APPLY zincirine** bölündü:
   her adım satır başına bir kez, sonraki adım önceki takma adı okuyor.
3. **SQL 4104 "Çok parçacı sp.Oneri bağlanamadı"** — sayım sorgusuna (`SayAsync`) APPLY
   eklenmemişti; karta tıklayınca liste "Veri alınamadı" verdi. → düzeltildi.

Ders: **"derlendi" çalışıyor demek değil.** Üçü de yalnız çalışma anında çıktı.

### Çapraz mutabakat (panel çekirdeği ↔ Python emitter, aynı kesim 09.09, kitapdışı)
İlk koşuda AYRIŞTI: panel 5.415 çeşit / 115.887 adet · Excel 3.277 / 74.621. Sebep ölçüldü:
**Python'un SQL ön-süzgeci yalnız düz kapağa bakıyordu** ve sezon talebi yüksek olan ürünü
okumadan eliyordu. Ön-süzgeç cömertleştirildi (ham sezon toplamının 2 katı; yıl oranı 1'in
üstünde olabiliyor — Oyuncak 1,50 — ve emniyet stoğu kapağın üstüne biniyor).

Son durum: **adet %0,9 · maliyet %0,6 · çeşit %1,5 fark** (Excel 5.495 / 116.907 ·
panel 5.415 / 115.887). Kalan sapma öneri=1 sınırındaki yuvarlamadan (SQL `CEILING` float ·
Python `math.ceil`); tutarların aynı olması bunu doğruluyor. Tolerans BEYAN EDİLDİ, kapatılmadı.

### Doğrulama
build 0 hata · panel açılıyor · kart↔liste tutuyor · 375px taşma **0** ·
`panel_kolon_denetimi` 0 kırık (1 uyarı: sipariş kolonları string birleştirmeyle eklendiği
için sıra denetiminde KÖR — 4/40 parametre; risk düşük, üçü de SELECT'in sonunda).
