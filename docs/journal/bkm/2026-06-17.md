# Oturum Günlüğü — 2026-06-17

> curator-check: 2026-06-17 (consolidate-sema dry-run + A1 uygulandı bu oturum)
> 16.06 Oturum 6'dan kesintisiz devam (gece yarısı aştı). Önceki handoff df54be4 (Oturum 6 plan, kod commit BEKLİYORDU). Bu oturum o kodu commit'ledi + çok daha fazlası.

## Ana Konu
Kumbara keşfi → iç-kart tek kanonik filtre (plan-18) → tüm müşteri raporları FİŞ bazlı (CFO direktifi) → full-scan (4 ajan + roslyn deneme + file-size) → denetim fix'leri → **disiplin hook aktivasyonu** → **yetenek-uret meta-skill (çatı self-development)** → **curator (consolidate-sema) uçtan uca** → /learn 3 ders → context7 MCP doğrulama + B-91 detaylı plan. **~18 commit.**

## Tamamlananlar
### Kumbara + müşteri (commit b332469, d594b4a)
- **Kumbara = `EncoreMerkez.dbo.RefundReasons` Id=17 "Okul Kumbara Projesi İndirimi"** (Type=1 indirim tipi). RefundReasons çift görevli (Type=0 iade / Type=1 manuel indirim). Type=1 HİÇBİR işlem kolonuna bağlı değil (RefundReasonId/PriceChangeReasonId/Sales.* hepsi elendi). Otomatik rapor POS e-postası. **İşlem-bağı tablosu kullanıcıda — sabah verecek.** sema/codes.yaml status:teyit bekliyor.
- **plan-18 iç-kart tek kanonik filtre** (`IcKartService.cs` § IcKartFiltre.Sql + SqlCols) → 9 müşteri sorgusu. isim(Mağaza/Kumbara COLLATE)+tel(599/699)+elle liste. AKL(7476)/RGR/AGH 599-kartları yakalandı.
- **B-103 tüm müşteri raporları FİŞ bazlı** (sql-server-conventions § kural). Sayım=DocType1, ciro=(1,3). Fatura/Personel/Sınav HARİÇ. /musteri+/sadakat "📄 Fiş bazlı" rozet.
- **B-102 kartlı/kartsız FİŞ** — Sınav(8) kirliliği + anonim eksik → 5.627→**529.428 kartsız fiş** (anonim dahil, sepet/fiş). Kartlı 675₺ > Kartsız 539₺.
- **B-101 /sadakat LLM yorumu** (LlmService.SadakatYorumUret + koyu kart, RendererInfo.IsInteractive guard).
### Full-scan denetim fix (commit d594b4a)
- `Queries.cs:349` kasiyer Net asimetrik KDV (satış kolu -VatTotal eksikti) · kargo/il `ODAKJOKER` linked → direkt jconn (M-12) · `10_01/10_02` NetCiro=TotalAmount **çöp** (adet) → net · G1/C1 KDV-dahil→hariç + C1 e-tic SELLINGPRICEWITHOUTVAT.
- ParetoRow.Dilim int↔NTILE bigint → CAST int (B-102 sırasında /sadakat kırıktı, düzeldi).
### Disiplin + self-development (commit 6ccfb6b, 7fc5a36, +meta)
- **Skill/rule fix** (6ccfb6b): semantic-layer § keşif-SQL ikiz-yükümlülük · journal-yolu (bkm/) · KDV-stale · fiş-bazlı çapraz-ref.
- **H-09 disiplin hook'ları aktif** (7fc5a36): session-start journal-path bug FIX + curator-due + disiplin yüzeyi; pre-commit-antipattern BKM-adapte + **wire** (dormant'tı).
- **H-12 yetenek-uret meta-skill** — skill/agent/hook/rule üret+güncelle (footprint-ladder + scaffold + kayıt + test). İnsan-tetikli.
### Curator + ders (commit ffa5b09, REPORT, A1 8628c45, d3b97f1)
- **Stale TODO temizliği**: C-11/C-07/C-01-05 done→[x], B-90→M-13, Mayıs-kampanya(B-NEW) → ## Arşiv.
- **consolidate-sema uçtan uca**: REPORT-2026-06-17 (sema sağlıklı: 0 stale) + **A1 J_ORDERS.STATUS dup birleştirildi** (umbrella). Curator handoff≥7g koşulsuz + session-start yüzey otomatikleşti.
- **/learn 3 ders**: aggregate-CASE subquery yasak · Sales.TotalAmount≠ciro · Razor @code-markup→code-behind geçersiz.
### M-10/M-11/M-13 + context7
- **M-11** RFM geçiş kayan pencere (hard-coded tarih → DECLARE @bugun/@anchor1). **M-10** LlmService 3×inference→Infer() (Mekan/Q<T>/LoadEnv bilinçli bırakıldı). **M-13** RefQueries 638→334 partial split (Home/Tahmin ertelendi — code-behind geçersiz).
- **context7 MCP doğrulandı** (.mcp.json commit'li) — resolve+query çalışıyor, B-91'i çözdü.

## Build / Test Durumu
- Build: **yeşil** (her değişiklikte 0/0). Python syntax OK. COLLATE runtime OK.
- Smoke: /musteri (kart-oranı İst.Yolu %53→%50,6) + /sadakat (5 panel + LLM kartı) + RFM sliding-window MCP doğrulandı.
- Hook'lar test edildi (session-start çıktı, pre-commit exit 0).

## Commit Durumu
- ~18 commit (b332469 → d3b97f1 + 8628c45). **Tree TEMİZ** (0 uncommitted). pre-commit hook devrede.

## Yarım Kalan / Yarına
### Üst öncelik
1. **Kumbara işlem-bağı tablosu** — kullanıcı verecek → İndirim Kaynak paneli + Kumbara satırı + RefundReasons referans. sema teyit→kalıcı.
2. **B-91 PersistentComponentState** — TODO'da DETAYLI plan var (prerender:false REDDEDİLDİ=mobil regresyon; PersistentComponentState DOĞRU, 12 sayfa, mobil-test zorunlu). Tier-3 taze oturum.
3. **M-13 Home/Tahmin sub-component** (code-behind geçersiz — markup RenderFragment) · **B-97 lokasyon filtresi** · **B-81 iade-netleme** (en yüksek değer).

## İşe YARAMAYANLAR / Dersler
- **`SUM(CASE WHEN ... NOT IN (subquery))`** → SQL aggregate-içi-subquery yasak. WHERE'e taşı / SqlCols join-form. (Build geçer, runtime patlar — smoke yakalar.)
- **Razor `@code` markup RenderFragment → `.cs` code-behind GEÇERSİZ** (CS1525). Sub-component gerek. (M-13 Home/Tahmin ertelendi.)
- **`prerender:false`** → mobil B-47 regresyon (SignalR flaky → boş "Yükleniyor"). B-91'de YOL 1 reddedildi.
- **`Sales.TotalAmount` ≠ ciro** (adet-benzeri küçük değer). Net = Gross-Disc-Vat.
- **WS-2 faz-2 token-move** → 4/7 rule karar-bağlı (paths: kapsamaz), 3/7 dosya-bağlı .razor/.cs (hep yüklü) → kazanç ~sıfır + compact-fragility. Değer<risk, ertelendi.
- **roslyn MCP** → repo'da .sln yok (navigator başka solution'a bağlı) → init olmadı. Mimari scan grep/agent ile yapıldı.

## Kararlar
- **Müşteri raporları FİŞ bazlı** (CFO direktifi, 16.06'nın devamı).
- **Disiplin = hook** (mekanik), beni-hatırlamama-bırakma değil. **Self-improving = insan-onaylı üçlü döngü** (yetenek-uret + /learn + consolidate-sema), otonom daemon DEĞİL (Claude çağrı-bağımlı, plan-12 §5).
- **B-91 = PersistentComponentState yolu** (prerender:false mobil-regresyon nedeniyle reddedildi).
- **M-10 cross-file dedup BIRAKILDI** (trivial/stabil, footprint-ladder).
- **faz-2 token-move ertelendi** (değer<risk, analiz net).

### Kullanıcı feedback verbatim
> "hiçte disiplinli değilsin tüm hepsini aktif et katı disiplin" — hook aktivasyonu tetikledi.
> "hermes gibi istiyorum skill yazsın kendini geliştirsin" — yetenek-uret + döngü.
> "neden yapmıyorsun beni ikna et" → "sana inanıyorum" — faz-2 analiz kabul.
> "kartsız çok daha fazla olmalı" / "müşteri raporları hepsi fiş ile çalışmalı" — B-102/B-103.

## Dikkat
- `.mcp.json` commit'lendi (context7, sır yok).
- Kumbara sema kaydı `status: teyit bekliyor` — tablo gelince doğrula, körü körüne kullanma.
- B-91 mobil-regresyon tuzağı — prerender:false YAPMA.
- pre-commit hook artık aktif (BKM-adapte) — commit'lerde bare-except/şifre/ex.Message bloklar.

## Yarına Başlangıç Noktası
0. **🥇 İLK İŞ (kullanıcı direktifi): tüm açık TODO tara + öncelikli iş listesi** (HK-0). todo-verification ile canlı doğrula (stale [x]-değil-açık + atlanmış). TODO-yönetim skill GEREKİYOR mu: plan-tracker+consolidate-sema kapsıyor mu? Boşluk → `yetenek-uret` ile `todo-tara` forge (önce mevcut genişlet). 89 açık [ ] var.
1. Sonra HK-1..6 hızlı kazanımlar (mobil Diğer menü / sql-denetci test / DepoWms guard / ApexCharts var / gitignore / Express doc).
2. Kumbara işlem-bağı tablosu (kullanıcı verince) → İndirim Kaynak paneli.
3. Büyük: B-91 PersistentComponentState (detaylı plan + mobil-tuzak) · M-13 Home/Tahmin sub-component · B-81 iade-netleme.

## Compact Snapshot --- 09:44

### Son 5 Commit

- e917e5e chore(bkm): TODO B-25/B-24 tamamlandi isaretlendi
- 8e573f4 docs(crossproject): B-25 session-protocol SEMANTIK_KATMAN adim + B-24 ADR-004 ehAdet-NEGATiF kural
- 9c5f179 fix(bkm): RefQueries.Envanter:153 IIF→CASE WHEN (EncoreMerkez compat-110 ihlali — sql-denetci bulgusu)
- 6ef6f54 chore(bkm): TODO temizlik — HK-2/B-99/B-86/B-89 done-but-open [x] + B-66=R-4 dup işaretle
- d839b6f chore(crossproject): HK-0 S1-S4 TODO disiplin sertlestirme — commit→[x] kural + stale-pass handoff + dup-grep + session-start sayac

### Uncommitted Dosya Sayısı

- 1 uncommitted dosya

### Aktif Planlar

- 02-fsm-trafik-analiz.md
- 03-brief-otomasyon.md
- 04-gunluk-kar-zarar-maliyet-karsilastirma.md
- 05-envanter-verim-kpi.md
- 06-mcp-server-ayiklama.md
- 07-dashboard-react.md
- 08-apexcharts-migration.md
- 09-dashboard-rafine.md
- 10-dashboard-icerik-genisleme.md
- 17-python-eksik-portlar.md
- 18-ic-kart-tek-filtre.md

### Aktif TODO (ilk 15 açık madde)

- - [ ] **B-97 plan-17 WP-2 lokasyon filtresi** — `GetInventoryAsync` ~10 noktada `ehMekan IN (1,4477,4478)` hardcoded → 5-lokasyon (FSM/Özl/İst/Depo12/ODAK) toggle param + "stok=şube+Depo, ODAK hariç" (Python urunler_query_sql). Dedike, mutabakatlı.
- - [ ] **H-10 (ERTELE — bilinçli) WS-2 faz-2 fiziksel rule taşıma** — on-demand rule'ları `topic/` + `paths:`/skill-inject → gerçek token tasarrufu. RİSK: compact-survival (rule post-/compact düşer → sessiz konvansiyon kaybı = BKM'nin EN korktuğu sessiz-yanlış-rakam). Tek-kullanıcıda token ağrı değil → değer<risk. plan-12 §4 test-gate'li. Hook'lar zaten disiplini aktifledi → faz-2 GEREKMİYOR. Açılırsa: ayrı plan + compact-smoke şart.
- - [ ] **H-11 (ERTELE) WS-1 telemetri sidecar** — sema/skill kullanım sayacı. plan-12 reddi: tek-kullanıcıda over-engineering, manuel yargı yeter.
- - [ ] **R-1 Ay-seçimli kazanım drill** — kazanım grafiğinde aya tıkla → o ayın yeni müşterileri listesi (ad/tel/ilk fiş/ilk tutar). Şu an 13-ay seri sadece adet.
- - [ ] **R-2 Kart oranı dönem seçimi** — kart-fiş oranı şu an 30g sabit. Günlük/haftalık/aylık pill ekle.
- - [ ] **R-3 E-ticaret kazanım** — yeni müşteri kazanım şu an YK (yazarkasa) only. JOKER CUSTOMERREF ilk-sipariş ile e-tic kazanım serisi ekle (ayrı evren).
- - [ ] **R-4 Kohort retention matrisi** (= B-66) — aylık edinim kohortu × N-ay-sonra geri dönüş % (heatmap). "Ocak'ta gelenin %X'i 3. ay hâlâ alıyor."
- - [ ] **R-5 Segment geçiş matrisi UI** — `SadakatQueries.GetRfmGecisAsync` SQL VAR (B-67), dashboard'da panel YOK. 3-6 ay önceki segment → bugünkü segment akışı (kim Şampiyon'dan Risk'e düştü).
- - [ ] **R-6 Müşteri LTV / yaşam boyu değer** — segment başına ort. yıllık harcama × tahmini ömür. Kart sahibi vs kartsız ATV farkı (GetKartliAsync var, kazanca bağla).
- - [ ] **R-7 Churn / tekrar-alım oranı** — ilk alıştan sonra 2. alış yapan % (aktivasyon), 90g sessizleşen (churn riski) adedi/trendi.
- - [ ] **R-8 Müşteri fiş drill polish** — fiş listesinde indirimli/kampanyalı fiş işareti (rozet), ET sipariş durumu, geri-navigasyon UX (şu an seviye state).
- - [ ] **B-47 ⚡ DRILL MOBİL FIX** — mkcert güvensiz cert (Android kırmızı X) → WSS/SignalR kurulmuyor → mobil HTTPS'te interaktivite/drill YOK (sayfa SSR açılır, tıklama ölü). Kanıt: HTTP'den (http://192.168.1.61:5112) drill çalışmalı. **Çözüm: Cloudflare tunnel** (gerçek cert → WSS+PWA install+standalone+drill hepsi). Kullanıcı tüneli 2× reddetti ama mkcert Android'de yetmiyor. VEYA iç sunucu+Let's Encrypt. **(YENİ 14.06)**
- - [ ] **B-81 🔴 KRİTİK iade-netleme (~%0,9 ciro şişik)** — irsHrk sorguları `ehTip IN (4,100)` alıp iade `(101,5,3)` DÜŞMÜYOR: `RefQueries.cs` devir(84-88)/marka(115)/cve(312-317)/rotasyon(505-508)/marj(478) + EncoreMerkez `ReturnAmount` netleme `Queries.cs` katSql/skatSql + `MagazaQueries.cs` katSql + `RefQueries.GetUrunlerAsync`. sema `metrics.yaml net_ciro`/`encore_irshrk_mutabakat` zaten "DÜZELTME BEKLİYOR". **Her sorgu MCP eski/yeni mutabakatlı** düzeltme — dedike iş, aceleye gelmez. (En yüksek değer.)
- - [ ] **B-82** ORTA — Kasiyer net `ABS(DiscountTotal)` formülü diğer sorgulardan farklı (Queries.cs:148-160, 333-341), çift-düzeltme riski → doğrula.
- - [ ] **B-83** ORTA — exception sızıntısı `_error = ex.Message` UI'da (tüm sayfalar) → generic mesaj + detay logger'a.

