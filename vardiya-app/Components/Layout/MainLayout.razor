@inherits LayoutComponentBase

@* Renkler DaisyUI semantic token — hardcode hex YOK (renk-standardi.md). *@

<div class="min-h-screen bg-base-200">
    <div class="navbar bg-base-100 border-b border-base-300">
        <div class="flex-1">
            <a href="/" class="btn btn-ghost text-lg font-semibold">BKM Vardiya</a>
        </div>
        <div class="flex-none">
            @* Adım 4'te kullanıcı adı + rol + çıkış buraya gelir. *@
            <span class="badge badge-warning badge-outline">Geliştirme — giriş yok</span>
        </div>
    </div>

    <main class="p-4 max-w-7xl mx-auto">
        @Body
    </main>
</div>

<div id="blazor-error-ui" class="hidden">
    Beklenmedik bir hata oluştu.
    <a href="" class="link">Yenile</a>
</div>
